------------
TODO Legend:
- to do
+ done
------------

+ In viewer.html, rename the "Road Metrics" info box to be "Parcel info". Add a checkbox to it saying [ ] Road. By default it should be unchecked. If I check it, that means the parcel is a road, add that information to the parcel object so next time the parcel is clicked it the box is checked. Also give the parcel on the map a different color.

- Add another button to the panel with controls. It should say "Combine roads". If I click it, it should combine all the road parcels (in the current map view... actually, it should combine all the road parcels in the entire map) into one big road parcel (one big polygon).

- Add a section to the panel with controls called "Road grid". It should have several settings and button called "Complete road grid" at the bottom. The settings are used to decide how to complete the road grid. One setting is "Maximum block size (meters)".
- It should also display "Umbilicus coordinates:" which is the coordinates
- If I select "Roads", it should complete the road grid by adding the missing parts of the road grid.

(select different types of road with their widths shown in brackets. A road consists of sections reserved by usage: Shared (S), Pedestrian/Human (H), Bike/Micromobility (B), Parking (P), Car lane (R), Transit lane (T). In one road the sections can be combined and each can be of different widths. Examples:

- H1m (Humans, 1 meter) (1 meter)
- H1m+B2m+R2m (Humans 1 meter + Bicycle 2 meters + Car Lane 2m) (5 meters)
- S2.5 (Shared 2.5 meters) (2.5 meters)))

+ Let's make another tool similar to the measurement tool. Road drawing tool. (Note: there are no curves in the cadastral parcel definitions, it is always polygons. A curved road, for example, would approximate curvature with a polygon). There is a "Draw road" button in the UI. When clicked, a dropdown menu next to it allows user to select road width in meters.
Similar to the measurement tool, the next (first) click on the map marks the beginning of the road (more precisely, the centreline of the road). The road is then constantly being drawn between the first point and the mouse cursor (more precisely, the user is determining the centreline of the road. The road section itself that is being drawn is a rectangle). Clicking on the map confirms the next point of the centreline of the road. If the road consists of more than two points (start and end) we need to ensure it is one polygon by not drawing the closing line of the section rectangle unless it's the last rectangle. There is a "Finish" button where the user can click to finish the road drawing. The resulting road is a polygon. The road polygon intersects various parcels. We need a "Road Info" panel which will be displayed from the moment we started to draw a road. It will have these fields: Road length: (length of the centreline of the road), Road area: (area of the road polygon), Parcels intersected: (list of parcels that are being intersected by the road polygon. Next to the parcel number there should be, in brackets, the area of the parcel that is taken by the road.). The "Road Info" panel should change dynamically as we move the cursor. The parcels that are being affected should be highlighted in green.

When a road is finished, we need to recalculate the affected parcels. The road is a polygon on the map, and it is a parcel.

1. It needs to be given a parcel number which needs to be a derivative of the first parcel name that it affects and that doesn't have a / (slash) in the name. It should become that parcel number / 1. It shuld also have isRoad=true.
2. We need to go through affected parcels one by one and change their polygons so that the intersection of the road polygon and the parcel polygon is removed from the parcel polygon. We need to investigate whether this splits the parcel polygon into multiple unconnected parts. If so, each of them needs to become a separate parcel with a parcel number that is derived from the original parcel number and is a number following a slash.

- when a completely new parcel is created, it should get correctly the click handler
- when "finish road" is triggered 1) the mouse should be released (no more road preview to the mouse cursor) and 2) the parcel highlighting for the affected parcels and the road itself should be removed

- I also wish the road parcel to retain/inherit no other styling apart from that which all other road parcels have. At the moment it seems that it ends up with a hue of green that is darker than roads generally. Maybe it is inheriting some style, or maybe it is because it is drawn over an existing parcel which is red. After forming the road parcel I wish to re-render it, so that it has the same color as other roads on the map, and is not darker.

+ Let's add button "Apply government road plan" to the road section of the sidebar. When clicked, the button will fetch the government road plans from the source (if localhost or api: planned_road table joined with government_plan table in the db) for government road plans and parse the planned roads one by one and create a proposal for each one and apply it on the map. The major complexity here is that government road plans typically include roads that already exist (while not having all roads that exist). This means we have to combine it with a source of existing roads and subtract the existing ones to get only the newly planned ones.
There should not be conflicts...?

+ Add route GET /planned-road which will be triggered by "Apply Government Road Plan" and will fetch rows from planned_road table. These rows will one or more giant polygons representing all planned roads merged into one big polygon. We need to loop through them. I need a smart way to merge the plan onto the map. The map will already have parcels, some of which will be defined/marked as roads. We need to subtract those from the giant road proposal polygon before drawing it. We should only draw the planned road polygon(s -- it may end up broken into many pieces) where there is no existing road on the map. We'll color the planned road polygon(s) yellow.

+ Let's add button "Apply government road plan" to the road section of the sidebar. Don't wire it up yet.
+ Let's plit road buttons in the Roads sidebar section into three groups:
1) Detect existing roads. Most current buttons are in this group. Two strategies exits: one is to have a trustworthy source of all parcels and just detect which of the existing parcels are roads. The other one is when we do not have a source of parcels, we can then detect roads and form parcels for them. This can serve as source/anchor for further parcellization (say, in refugee camps).
2) Apply roads from government plan.
3) Draw (propose) a road manually.

- OK we are now drawing the government road plan on the map. Let's separate the button into two buttons. "Draw government road plan" and "Apply government road plan". The first does what it does now, draws the plan. The second needs to find differences between plan and existing map state. It should draw blue polygons in areas that are covered by the government road plan but are not already roads. These will be parts of parcels usually, or entire parcels. The map will already have parcels, some of which will be defined/marked as roads. We need to subtract those from the big government road plan polygon and draw the differences in blue.

+ Have all "Detect" buttons be different class from "Draw" buttons and make them different color. Detect should be a lively green/blue linear gradient. Move "Clear roads from local storage" outside the subsections and put it right below the section heading.

+ Don't do intersections with govt road plan on zoom leves where we don't show parcels, in such cases just draw the full (unmodified) road plan polygon from the source, in yellow.

+ Maybe offload govt plan rendering/intersection to a thread so UI is more responsive?

- I think are intersecting plans with parcels even at lower zoom levels (when we are more zoomed out). Moving the map becomes sluggish and choppy. When we enter a map zoom level where we don't show parcels we shouldn't be doing intersect of govt road polygon with road parcels, instead, we should just draw it over the map in yellow (if the checkbox is checked, of course). Also, maybe we should move the processing for govt road polygon into a thread that won't block UI?

- How do we apply the road plan partially, while we don't have all the parcels loaded? Can we maybe load the parcels in its path? Still, even loaded some won't be marked as roads... is that an issue?

- OK let's now make the government roads plan a fully fledged proposal in the system. The first question is one proposal or many smaller ones for segments? Let's try first with one proposal. Secondly, the question is when to create the proposal and when to add parcels to it, and when if ever to subtract from it (probably yes if parcel is marked as road). Probably best to add a button for applying. Should it be one huge parcel (multipolygon) or many smaller segments? Because it will be broken up into pieces. Let's start with multipolygon? And how do we add parcels to the proposal and proposal to parcels? Analyze this first and offer a solution, don't implement right away.

- Is is possible to define small bboxes so as to load parcels that intersect the road plan on the whole map, but not by a big outer bbox but by tracing segments?

- Should proposal have two geometries, per parcel geometry and general, total geometry? We need to store that right?

- Upon toggling "is road" on or off for a parcel, check for descendants and warn if any are found.

+ Drawing the road plan should not draw it over road parcels. Can we skip those without incurring a rendering speed penalty?
- We should also not be applying a plan over existing roads, currently we do (but it doesn't matter much, a road is a road, maybe just leave it so we don't slow down with handling that)

+ Add "Detect existing roads" button to the Road section, make it gold or green/gold gradient, and add hover info to it "Detect existing roads using all available methods (OSM, DGU, GUP...)". Clicking the button will call in sequence all the different detection methods (as of now two: OSM and DGU). When done it should uncheck both the OSM lines and DGU polygons checkboxes, we want a clear view of the map.

- add GUP as source of roads (streets). Meaning we get two more buttons in the road section: "Draw GUP existing roads" and "Detect existing roads using GUP". Model the approach on how we do buildings. When localhost is source use GET /streets/ (add the route to the server, it should use the street table and get multilinestrings from it). Maybe add optional bbox parameter to limit to current view. When web is the source use this URL with proper retries:
"https://services8.arcgis.com/Usi0jGQwMmBUpFjr/arcgis/rest/services/Ulice_200409/FeatureServer/1/query?where=1%3D1&outFields=*&outSR=4326&f=geojson&resultOffset=0&resultRecordCount=2000"

+ Also add the same effect for "Analyze roads OSM". Also make the button trigger the drawing of roads from OSM if they haven't been drawn, instead of outputting the error message. Also put the analyze button on the bottom of the section, and center on it when it is clicked (scroll the sidebar view to it). When alaysis finishes the legend shouldbe below the button and visible.

+ OK now let's work on making all the govt plans either a) proposals or 2) force applied... probably proposals, applied by default, but unappliable.

- Should a stretch of govt road be one proposal or several if it affects more than one parcel? For now a multitude of proposals. It doesn't matter as that is now what people work with. The only complication is ancestry reproducibility on machines where proposal is shared.

- When drawing roads manually add various "snap to" tools. For example a common approach for road forming is forming it along a border, extending equally to both sides and parcels on the sides. We also need to be able to snap to sides of existing road polygons so that we don't have to cut into them.


+ When creating/finishing a road, we need to make sure we are creating a proper proposal, like when we create buildings. Instead of just the popup for name, we should get the full proposal modal.

+ When a road is finished by pressing F the same has to happen as when mouse leaves the viewport. The current road segment preview has to be removed.

+ When road drawing is cancelled (Esc is pressed or button is clicked) the circles marking centreline inflections need to be removed as well

- Add a road list to the sidebar, like we have for block list.