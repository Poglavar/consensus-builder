# Memory

- 2026-07-16: **Proposal `status` was split into two independent axes — the overloaded field is GONE.** It mixed the marketplace/on-chain LIFECYCLE with the map-APPLICATION state, so a chain-sync writing `'Active'` silently un-applied a road. Now: `lifecycleStatus` (`Active`/`Executed`/`Cancelled`/`Expired`/`draft`) + `applied` (boolean, mirrored on every sub-proposal). Canonical accessors `getLifecycleStatus(p)` / `isApplied(p, sub)` in `frontend/js/proposals/status.js` are the ONLY way to read status; the legacy `status` column, its JSONB copies, and every read/write were purged (laptop + dev + prod). The carve gate (`corridor-carve.js`) keys on `applied`; execute sets `applied=true`; chain-sync (`blockchain-sync.js`) ONLY ever writes lifecycle; spatial proposals default `applied=true` on create; the `?status=` list/count filter is renamed `?lifecycle=`. DB (`proposal` table): dropped `status`, added `lifecycle_status` + `applied`. Migration = `backend/scripts/split-status-applied.js` (idempotent, dry-run default, `--drop-status` opt-in): derives both axes, RESCUES stuck roads that are Active/unapplied but carry demolition records (e.g. proposal 474 → applied), strips nested `status` from JSONB, then drops the column. Prod migrated **zero-downtime**: (1) add+backfill columns, (2) deploy code, (3) drop column — never single-shot. Rationale: the walk-sim yellow-man showed a road with no building cuts because 474 was `Active/unapplied` and the carve gate rejected it (0 carves → 38).

- 2026-07-16: **`proposal-manager.js` decomposed 8,210 → 4,821 lines.** Carved into focused, load-order-correct modules: `proposals/apply/{route,finalize,road,buildings,structures,parcels,unapply}.js` + `proposal-road-geometry.js` + `proposal-parcel-identity.js`. Per-type apply/unapply are UMD mixins `Object.assign`ed onto `ProposalManager` (methods keep using `this._x()` + bare-global helpers, bodies unchanged — the file is a classic script so its top-level funcs are already globals; moving them just changes which file declares them). NEW shared modules are UMD-wrapped (NOT bare top-level functions) to satisfy `frontend-duplicate-globals.test.js` — a bare `normalizeGoalKey` would have shadowed `core.js`'s. Pure `classifyApplyRoute` (`route.js`) owns the goal→apply-path decision (delegated to by `_normalizeGoalKey`/`_isBuildingProposal`). The apply methods do NOT share a collapsible skeleton — per-type work is genuinely distinct and even the tails differ (road persists/indexes mid-method), so a "pipeline" would be conditional soup; only the truly-shared tail (`persistAppliedProposal` + `refreshProposalUIAfterApply`, `finalize.js`) was extracted (4 of 5 methods). First node-runnable coverage of an apply path: `backend/test/apply-buildings.characterization.test.js` stubs collaborators as spies + a fake `this` and asserts observable effects — stayed green across the extraction. Note: some helpers are exposed as ProposalManager shorthand props, so moving them broke node until the node-branch published them onto `globalThis` (guarded by `typeof module !== 'undefined'`; inert in browser).

- 2026-07-14: **Buildings are GDI, not DGU.** Cut/tunnel/demolish detection now scans `gdi_building_footprint` (`object_id`) — the same objects the 3D view and the walk sim render. It used to scan the DGU cadastre (`zgrada_id`) while rendering GDI meshes: two independent surveys that genuinely disagree (same bbox: 112 GDI objects vs 66 DGU buildings), bridged by ~110 lines of footprint-overlap guessing, a graze bar and a `dgu_gdi_building_match` lookup. All deleted (carve paths 505 → 300 lines). Detection reads the DATA POOL (`window.buildingFeaturePool`), never the Leaflet display layer, so a visibility checkbox can never change what gets cut. `GET /buildings?bbox=` serves GDI; DGU is explicit via `?source=dgu`; unknown source → 400, never a silent wrong survey. Rationale: the buildings we cut must be the buildings we render — a corridor detected 10 buildings, all `object_id`, **0 of 10 valid as `zgrada_id`**, and 8 of 113 stored prod records named buildings GDI has never contained. The cadastre is now a reference overlay only (two independent sidebar checkboxes; B toggles the current choice and only explains itself when nothing is on).

- 2026-07-14: **Proposal thumbnails render server-side** (`backend/thumbnails/`, node-canvas), at upload, after the insert commits, with a 20s deadline — a failed render never fails the upload. The "Generate" button and the whole client capture path are gone. Rationale: they were tile-stitched in the user's browser, so an image only existed if someone happened to have the app open in the right city — 31 of 52 prod proposals had none. NOTE for anyone tempted by "we tried server-side and Leaflet broke it": `map-screenshot.js` has TWO paths. `leafletImage` screenshots a LIVE Leaflet map and genuinely cannot run on a server. `captureViaTileStitch` is pure tile math + HTTP with no Leaflet at all — that is the one that ports (client/server frames are pixel-identical). Prod backfilled: 2 → 49 hosted images; the 19 base64 `screenshotDataUrl` blobs moved to files and stripped from `proposal_data` (they bloated every `GET /proposals/:id` by ~60%).

- 2026-07-14: **`PUBLIC_API_BASE_URL` pinned in `ecosystem.config.cjs`.** Without it `resolveThumbnailBaseUrl()` fell back to `req.get('host')` — client-controlled — so a spoofed `Host` on `POST /proposals` would permanently store an attacker's origin as that proposal's `screenshot_url` (and its og:image). Also: `deploy-backend.sh` now does `pm2 restart ecosystem.config.cjs --only <app> --update-env`; `pm2 restart <name>` reuses the config PM2 saved at first start, so **every env change ever made to the ecosystem file had been silently discarded** while the deploy printed "Deployment complete" and passed its smoke test.

- 2026-07-14: **One corridor, not a road and a track.** `isTrack` is DERIVED — a corridor is a track iff its lane list contains a `rail` lane (a stored `metadata.isTrack` still answers for legacy). Both buttons survive as profile seeds over one engine, so tracks inherit snapping, junctions and branching. Rails became a lane-type rendering concern. Rationale: the fork had drifted far enough to break things — `corridor-render.js` excluded tracks from the corridor renderer outright, so an applied track had no hit targets (clicking it did nothing), its cross-section was never drawn, and it was absent from 3D. −3,638 lines against +1,740. Rail gauge: 1000 mm (tram, 2.75 m/track) or 1435 mm (railway, 3.5 m, default); a road may contain a rail lane.

- 2026-07-14: **Cross-section width is DERIVED from the lane list**, not a control. The width slider is gone. Rationale: the model enforced "total width is invariant" — every lane edit paid for itself out of the driving lanes, so once those hit their minimum `withLaneInserted` returned null and **"Add lane" silently did nothing**. Adding a lane now cannot fail; the road just gets wider. Seam-drag survives as the one gesture that deliberately holds the total (it transfers width between neighbours, so the footprint does not move).

- 2026-07-14: **The test suite could not fail.** A deleted `const` left `isRoad: currentIsRoad` behind — a `ReferenceError` on EVERY parcel click — and 119 unit tests, 30 e2e and `node --check` all stayed green. Because: 30 of 41 spec files never simulated a user action (they asserted functions exist); the panel spec passed because `showParcelInfoPanel()` runs BEFORE the throw; and nothing failed a test on an uncaught page error (`collectPageErrors()` existed with zero callers). Fixed: the shared fixture now FAILS on any `pageerror`, giving all 41 files a real failure signal. Six tests were calling functions that **do not exist** (`window.addProposal`, `window.applyBasemap`, `_applyCanonicalChildParcelIds`, five wallet fns); basemap switching had been silently untested since the move to `BasemapManager`. Browser suite 188 → 129 tests / ~300s → ~147s; pure logic moved to vitest (693 → 759). Only the screenshot capture caught the bug, because it drives real flows.

- 2026-07-14: **One copy of each shared global helper**, enforced by `backend/test/frontend-duplicate-globals.test.js` (ASTs every frontend script, fails on any top-level name declared in more than one file; allowlist can only shrink). Rationale: these are classic scripts, so a top-level `function foo(){}` is a GLOBAL and the last file loaded wins for every caller — the version you read is not the version that runs. `escapeHtml` was defined SIX times with four behaviours, one of which did not escape quotes while rendering `href="${escapeHtml(url)}"` (a live attribute-injection hole), and `sharing-routes.js`'s 8 codec copies were permanently shadowed by `sharing.js`. 39 collisions found; 27 remain allowlisted, 10 of them with genuinely divergent bodies.

- 2026-06-13: Frontend now defaults `window.STORAGE_PROVIDER='walrus'` (was `''`). ParcelNFT made **soulbound** (overrode `_update` to block owner-to-owner transfers; mint/burn allowed) — real ownership is EAS-attested (ProposalNFT acceptance), the holder is just a registry custodian. Mint harness: `PARCEL_REGISTRY_ADDRESS` env mints all parcels to one address instead of the ACCOUNT pool. Redeployed soulbound ParcelNFT `0x20e0A2897c642565905c0B45BBfaf7D8F96D1639` + ProposalNFT `0x9699AcBc70a32E5a7Ff30566dD496e080a4FAE2F` (real schema UIDs) on Base Sepolia; frontend `contracts/*.json` repointed. Supersedes ParcelNFT `0xa5eE…` / ProposalNFT `0xC23D183B…`.

- 2026-06-13: Added Walrus (Sui) as a config-selectable storage backend alongside IPFS/Pinata + local FS. Backend `POST /walrus/upload` + `backend/storage/walrus.js`; frontend `window.STORAGE_PROVIDER='walrus'` flips `target:'auto'` uploads, `walrus://<blobId>` resolved via `window.WALRUS_AGGREGATOR_URL` (public testnet default). Mint scripts: `--storage=walrus` + CJS `scripts/walrus-storage.js`; new `mint-parcels-nyc.js` (one NFT per `swis_sbl_id`, `US-NY-<id>`). On-chain pointer is canonical `walrus://<blobId>`; contracts unchanged. Spec in `feature-walrus.md`.
- 2026-06-13: Redeployed ParcelNFT to Base Sepolia at `0xa5eE0e1ae2cC2582731a1234FD8cDb7aD9225a50` (old `0x191Bb541E185f8C4fBF1eF4CE12a28acCFA6b35d` predated current source and reverted all new mints). NYC parcel data lives ONLY in the local `zagreb` DB, not `consensus` — mint runs need `PGDATABASE=zagreb`. base-sepolia mint needs `ETHEREUM_RPC_URL=https://sepolia.base.org` (harness prefers that env over RPC_URL). Public RPCs strip revert data, so `--no-check` / the `isParcelMissingError` null-data handling matters. Verified: NYC parcels minted with Walrus metadata, tokenURI resolves through aggregator. ~0.00116 WAL/parcel (~$4 for full 42k); testnet free.
- 2026-06-13: ProposalNFT redeployed to Base Sepolia (wired to new ParcelNFT + EAS predeploy `0x4200…21`). CityMemeToken churned to `0x3F7FF9fC46F32b462f4Cc2d02D07Ac3b6c0c2AeE` via hardhat-deploy recompile. `mint-proposals.js` now resolves NYC geometries (`US-NY-…` → `parcel_nyc_*`) and needs `LENS_ADDRESSES`. End-to-end proposal-on-Walrus verified. Walrus uploads: ~30 parcels/min vs public publisher (rate-limited); full 40k ≈ ~1 day as-is, ~2–4h with self-hosted publisher. `WALRUS_CONCURRENCY` (default 8) + retry/backoff added to mint harness.
- 2026-06-13: Wired REAL EAS schema UIDs into ProposalNFT → final Base Sepolia address `0xC23D183B51e0290f6B93a60a791bC02b7aA37Bc4` (supersedes the zero-UID `0x85732f…`). Registered 3 schemas on Base Sepolia SchemaRegistry `0x4200…20` (ENDORSE already existed): OWN_THIS `0x6ea2f878…` (`string I_OWN_THIS,string TARGET_CHAIN,string TARGET_ADDRESS,string TARGET_ID`), ENDORSE `0x90140d09…` (`bool THIS_ATTESTATION_IS_TRUE`), OWNER_LIST `0xdbcb52ec…` (`string TARGET_CHAIN,string TARGET_CONTRACT,string TARGET_TOKEN_ID,(string name,address owner,string dptoNumber,uint256 shareBps)[] owners`). UIDs deterministic = keccak256(schema, resolver=0x0, revocable=true); schema strings match the attest scripts' encoders. UIDs persisted to `blockchain/.env` (gitignored); frontend `contracts/{addresses,contracts}.json` point at the new ProposalNFT.

- 2026-03-26: Area monitor drawing now sets its own global drawing-mode flag and temporarily removes parcel click handlers while active. Rationale: parcel layers were intercepting clicks that needed to reach the map, so matching the existing road/track interaction model was the least risky fix.
- 2026-03-26: Area monitor rendering now separates “reapply styles” from “fly to monitor”, and monitor open/close flows go through explicit routing helpers plus a list endpoint/modal. Rationale: parcel refreshes were causing unintended camera jumps and monitor cleanup needed one reliable path that resets overlay and parcel highlighting together.
- 2026-03-27: The Docker dev backend now runs `npm install` before `nodemon` on container start. Rationale: the bind-mounted `/app` plus separate `/app/node_modules` volume was leaving containers with stale dependencies after backend package changes.
- 2026-03-27: The backend now boots through `backend/server.js`, and production PM2 runs it in `fork` mode instead of `cluster` mode. Rationale: the import-safe app module and PM2 cluster setup left the API process online without binding port 3000, while the explicit server entrypoint plus fork mode restored reliable startup.
- 2026-03-27: The backend deploy script now uses `pm2 startOrRestart ecosystem.config.cjs --update-env`. Rationale: plain stop/start was preserving stale PM2 app definitions instead of reliably applying ecosystem changes like `fork` mode and new script paths.
- 2026-03-28: Zagreb parcel bbox reads keep `parcel_info` ownership, but now fetch the latest details row per parcel key with lateral lookups instead of precomputing a whole-table latest-version CTE. Rationale: the whole-table `parcel_info` join timed out in production under map tile fan-out, while key-based lookups preserved the richer ownership source without reverting to `parcel_detail`.
- 2026-03-28: Backend deploys now run the public `/health` plus `/parcels?bbox=...` smoke test after PM2 restart, with bounded retries before the deploy is considered failed. Rationale: the recent production outage was a post-restart/runtime regression that ordinary file sync plus PM2 success did not catch.
- 2026-03-28: The area monitor detail panel now supports a header-only minimized state and dynamically stacks above the parcel info panel when that panel is open. Rationale: on mobile, closing the monitor panel discarded the monitor context, while leaving it full-height blocked too much of the map and collided with parcel info.
- 2026-03-31: Area monitors now persist a canonical `city_id`, legacy monitors infer Zagreb on read, and monitor routing blocks cross-city loads until the user switches cities or cancels. Rationale: shared monitor URLs were loading the wrong parcel datasource after city changes, so storing and enforcing monitor city was the cleanest fix.
- 2026-04-14: Synthetic descendant parcel IDs must stay rooted at the base parcel ID, and road replay only skips a remainder when the area delta is within a tiny absolute tolerance. Rationale: shared-plan replays were regenerating nested synthetic IDs and dropping valid small remainders, which broke old proposal dependency chains.
- 2026-04-14: Frontend deploys now stamp the build token into `index.html` and version every local script URL from the HTML bootstrap. Rationale: Cloudflare was caching plain `js/*.js` paths after deploy, so the live app kept booting stale runtime code even though the origin files were updated.
- 2026-04-14: Shared-plan replay now treats server-provided child parcel IDs as canonical for road and reparcellization proposals, and synthetic ID composition strips inherited synthetic suffixes defensively. Rationale: reusing locally-regenerated child IDs caused stale nested descendants and missing downstream prerequisites even when the shared payload already had the correct canonical IDs.
- 2026-04-14: The `/planned-road` route now clips `planned_road.geom` to the request bbox before `ST_Difference`, and builds props from explicit non-geometry columns instead of `to_jsonb(pr) - 'geom'`. Rationale: the old query was serializing a citywide geometry for small viewports and, after `ST_Dump`, repeatedly re-encoding the discarded source geometry into props, turning the government road plan toggle into a multi-second stall.
- 2026-04-14: Loading the government road plan overlay now defers plan hashing and full Turf union until the apply path actually needs it. Rationale: eager remaining-plan initialization made the simple “show plan” checkbox do heavy main-thread geometry work at zoomed-out views, freezing the page even though the user only asked to draw the overlay.
- 2026-05-11: Solana proposal authorization now validates parcel ownership against the fixed ParcelNFT program ID instead of storing a parcel program on each proposal. Rationale: this closes spoofed-acceptance risk without changing the serialized proposal account layout.
- 2026-06-09: Local DB lives in the docker `db` container (`consensus-builder-db-1`), database `zagreb`, table `proposal`, user `zagreb_user` (per `backend/.env`, `PGHOST=db`). Reach it via `docker exec consensus-builder-db-1 psql -U zagreb_user -d zagreb`. Rationale: host `psql -h localhost -p 5432` can hit an SSH ControlMaster tunnel that forwards loopback :5432 to the prod server, so host-port psql may silently target prod instead of local docker.
- 2026-07-08: Building footprints (2D + measured heights) are an OPTIONAL per-city provider capability: a provider in `backend/buildings/` that implements `footprints(geometry)` is served by `POST /buildings/footprints`; anything else reports `supported: false` (route resolves cities exactly, no Zagreb fallback). Zagreb implements it from `building` JOIN `building_3d_match.height_m`. Rationale: powers the urban-rule "Based on existing buildings" mode (two exclusive radio-picked rules — extrude every footprint to an exact proposed height, or add N floors on top of each current height, with an adjustable floor-height slider 2.5–5 m; one Feature per building with its own `properties.height`) and makes new cities work the moment their provider adds the method. Known caveat: Zagreb cadastre footprints don't truly match the `building_3d` meshes — at some point we may need to reverse-derive footprints from the 3D data.
- 2026-07-10: Corridor junctions, repeated surface symbols, and planted verge trees are derived from the shared centerline/profile model in both 2D and 3D; green strips persist `landscape: grass|trees`. Rationale: drawing-time edits and every renderer must interpret one corridor specification identically.
- 2026-07-10: Applying a copied/extended road parks its applied source as superseded, and removing the replacement restores the source. Rationale: a combined corridor must render once without destroying the proposal dependency chain needed for rollback.
- 2026-07-10: An applied corridor parcel is an interactive proposal surface: clicking it opens collapsed proposal details, while its base parcel uses a subdued footprint beneath the rendered cross-section. Rationale: applied roads should behave like proposals without a second near-black road fill competing with their lane design.
- 2026-07-10: Clicking an applied corridor completes normal parcel selection and also opens collapsed proposal details; the chain picker always offers No network, which deactivates Canton and disconnects wallet connectors. Rationale: road parcels retain both identities, and every network mode needs an explicit escape hatch.
- 2026-07-10: Synthetic descendant allocation is keyed by cadastral root ID, not root ID plus inconsistent parcel-number metadata. Rationale: a road strip and its residual parcel could otherwise receive the same ID, causing the residual geometry to replace the road's clickable map layer.
- 2026-07-10: Editing a placed road's cross-section applies the result to a seeded road drawing instead of opening proposal creation immediately. Rationale: geometry/design editing is a draft phase, while creating an immutable replacement proposal remains an explicit later action.
- 2026-07-11: Corridor drawing uses one locally autosaved active road/track draft, guarded at discard, replacement, city-switch, wipe, and unload boundaries; successful proposal persistence alone consumes it. Rationale: ordinary map/2D/3D navigation stays interruption-free without risking unsaved geometry.
- 2026-07-11: Proposal editing now uses one versioned, multi-city `ProposalDraft` store and adapter registry for corridors, every building typology, reparcellization, structures, ownership, and financial/expiry facets. Draft geometry tools bind to an explicit design-session draft ID, preserve review/error state across reload and city/view changes, and only explicit discard or successful immutable proposal persistence removes a draft. Rationale: the former single-corridor/pending-global lifecycle could overwrite unrelated work and could not provide universal undo, review, recovery, or multiple concurrent drafts.
- 2026-07-11: Published edits carry `sourceProposalId`/`replacementOfProposalId`; creating one leaves its source untouched, applying it marks the source superseded, and removing it releases/restores the source. Applied and unapplied proposal geometry now has selectable 2D/3D interaction surfaces, with source/draft comparison rendered in separate high-z layers. Rationale: replacement provenance, map selection, preview, and rollback must follow the same immutable lifecycle for every proposal type.
- 2026-07-11: Finishing a corridor drawing shows a small playful chooser ("Keep editing" / "Propose") instead of opening the editor shell or create dialog directly, and the editor shell hides while corridor drawing is active. Rationale: geometry work stays uncluttered; the full proposal complexity appears only after an explicit "Propose".
- 2026-07-11: `renderGeometrySection` preserves the submitted-geometry state when re-rendered for the same goal, and `releaseReplacementSource` reapplies the source only for a committed ('active') supersession. Rationale: facet changes (or the shell's facet prefill) were wiping drawn/draft geometry and disabling Create, and a failed apply's pending lifecycle could double-apply an untouched source.
- 2026-07-12: "Wipe ALL local data" now calls `proposalDraftStore.wipeAll()` (empties the in-memory envelope and removes the keys), and `persist()` skips writing when the store is empty and its key is absent. Rationale: the draft store's pagehide/beforeunload flush was re-persisting every draft from memory right after the wipe's `localStorage.clear()`, silently resurrecting road drafts.
- 2026-07-12: Closing the corridor drawing tool (X/Escape) shows a self-dismissing "saved as a draft — press R or open Drafts" toast, and the post-draw chooser hides the parcel info panel that multi-parcel seeding pops open. Rationale: X read as a destructive cancel even though the geometry autosaves, and the selection panel was noise between drawing and proposing.
- 2026-07-12: Road drawing vertices support long-press (350 ms) drag with live corridor rebuild (strips deferred to drop; drop recomputes parcels and autosaves); a short click keeps its add-point/continue meaning. Saved road/track drafts render on the 2D map as a slate dotted "sketch" overlay (js/draft-road-layer.js, dedicated 460-z pane, invisible fat hit-stroke, click resumes the draft; hidden while that draft is being drawn). The road width picker lost its sidewalk slider in favor of a "profile is editable later" note. Rationale: geometry should be directly manipulable, drafts should be visible but unmistakably distinct from blue-dashed proposals, and cross-section detail belongs in the profile editor.
- 2026-07-12: SimCity lifecycle adopted: locally created proposals AUTO-APPLY on creation (create.js) and finishing a corridor drawing instantly creates an auto-named applied object via `instantCreateProposalFromDraft` (no dialog, no chooser). Occupied-parent conflicts auto-park the occupying proposal when cleanly unappliable (`applyProposal(..., { autoParkConflicts: true })`); dependency chains still get the modal. Imports/shared/chain arrivals stay parked (importProposal path). Details panel actions are now Park ("Park — keep for later") / Edit / Share / Delete, and unapplied proposals render nowhere (2D or 3D) except as a preview while selected. Rationale: two visible states — on the map or parked in the list — with the formal proposal step (terms/offer/mint) as a later, optional act on an existing object.
- 2026-07-12: map-navigation zoom e2e tests poll for the zoom outcome instead of fixed 800 ms sleeps. Rationale: Leaflet animated zoom runs on requestAnimationFrame, which crawls under parallel-worker CPU contention (and pauses entirely in a backgrounded real-browser tab), so fixed sleeps flake.
- 2026-07-12: Corridor interaction model split by mode: R-drawing draws only (long-press vertex drag removed; F finishes AND instantly creates the applied object — C key/button removed); selecting an applied local road outside drawing enters node-edit (draggable centerline handles via js/road-node-edit.js + "Edit cross-section" in the details panel, both mutating in place through updateLocalCorridorGeometry with unapply→re-apply). Clicking an applied corridor during drawing places a point on it. Finishing a drawing that touches local unminted corridors of the same kind ABSORBS them into one body (oldest donates name+profile; absorbed records removed) — minted corridors are immutable and never absorbed or node-edited. Rationale: SimCity model — one connected piece = one road, geometry edited directly, proposal/terms complexity deferred to minting.
- 2026-07-12: Corridors have NO editor dialog: the details panel shows "Create proposal" (opens the create dialog prefilled via proposeExistingProposal → stageProposalDraftForPublishing) instead of Edit; on submit an unminted local source object is ABSORBED (record removed, lifecycle refs cleared) so exactly one thing remains — only minted sources stay behind as superseded replacements, and only they get the "Create replacement proposal" label. Node drags redraw the cross-section live mid-drag (throttled strip refresh; footprint/parcel re-cut on drop), and clicking a dashed draft road resumes its DRAWING rather than opening the editor. Rationale: selection is node-edit mode; the draft editor shell remains only for non-corridor types and the Drafts list.
- 2026-07-12: The draft editor dialog is fully retired from the UI: corridor drafts are created LAZILY (first autosave at two drawn points creates the draft and saveCurrentCorridorDrawingDraft adopts it into the design session — pressing R and walking away leaves nothing); the Drafts list has no New/Resume — clicking an item centers the map on the draft's dashed silhouette (cross-city via the pending-resume key, now also just centering); details panel and proposals list show "Create proposal" (proposeExistingProposal) for every unminted object instead of Edit, and instant-create failures keep the drawing in Drafts with a status line instead of opening the editor. The shell rendering code stays dormant in proposal-editor-shell.js (stage/instant-create/design-session helpers still live there). Rationale: the only place details/terms are edited is the create-proposal dialog; drafts are just unfinished drawings.
- 2026-07-12: Corridor geometry changes re-derive their parcels: `collectParcelsIntersectingFootprint` runs against the restored parcel fabric between unapply and re-apply (node drag / cross-section width via updateLocalCorridorGeometry) and after absorbing targets in merge-on-connect. Rationale: parentParcelIds captured at draw time went stale the moment a node moved, leaving parcels under the new footprint uncut.
- 2026-07-12: Road graph operations completed the SimCity model: crossings insert shared vertices into both segments (insertCorridorCrossingNodes, at merge and on every geometry edit); connected components are recomputed after each edit and a disconnected body SPLITS into separate proposals (largest keeps name/identity via corridorConnectedComponents + createRoadProposalFromComponent — split-off parts get auto-names, empty roads delete themselves); node-edit mode gained bulldozing (red midpoint handle deletes an edge, ⌥-click deletes a vertex). One proposal per connected body stays the rule because proposals are the acceptance/mint unit. Angle/intersection constraints deliberately deferred.
- 2026-07-12: Corridor obstacle prompts are three-way "Unapply existing proposal / Tunnel-or-Build through / Choose another route" (showStyledChoice in city-config.js, stacked buttons): buildings via resolveBuildingObstacles in corridor-tunnel.js (obstacle set now includes ALL applied building-proposal typologies from storage, not just proposedBuildings), parks/squares/lakes via corridor-structures.js (build-through approvals last one drawing session). Rationale: user picked unapply/tunnel/reroute over the old two-option confirm.
- 2026-07-12: Building tunnels stay a covered structure the WHOLE tunnel length visually, but acquire nothing: parents and parcel cuts are computed from the surface-only footprint (corridorSurfaceRuns skips tunnelled edges — used in Proposal.calculateChildFeatures and every collectParcelsIntersectingFootprint site), so parcels under a tunnel stay whole. Proper under-building-only tunnel semantics deferred to corridor-elevation slice 3 (noted in corridor-elevation.md).
- 2026-07-12: A road built through a park/square/lake CUTS it at render time only (structures.js subtracts applied corridor footprints from base + decoration zones; corridor-render triggers re-cut on every strip refresh): the structure stays ONE proposal whose stored geometry is never mutated, so unapplying/moving the road heals it. Known gap: 3D still renders the uncut structure. Rationale: user chose one multipolygon park over splitting into two proposals.
- 2026-07-12: Cross-section editor gained a total-width slider for BOTH modes (0.1 m steps, min 5 m, max 80 m = the Boulevard preset; withCorridorWidth lets traffic lanes absorb the change; the drawing mode's number input was replaced). Post-placement widening previews live and is checked (debounced, per edge) against buildings + applied structures: hitting a building the road did not touch at its opening width BLOCKS Apply ("tunnels are made while drawing" — no tunnels from widening), while crossing parks/squares/lakes only lights an informational chip since the render-time cut handles it. Rationale: user wanted fine-grained width editing after placement but no tunnel creation outside the drawing flow.
- 2026-07-12: The road-width picker modal is GONE (modal HTML, showRoadWidthPicker, thumb SVGs, legacy roadWidthSelect dropdown all removed; roadwidth-* CSS kept — the track-speed modal shares it): pressing R starts drawing immediately at the last-used width ('lastRoadWidth' in PersistentStorage, persisted by setRoadDrawingProfile; 7.5 m Alley on first use) and collapses the sidebar. The drawing panel header is "Draw road"/"Draw track" (localized) with a Cross-section button showing the current width (· X m) that opens the profile editor before or during drawing — this wired up the previously-dead editRoadCrossSectionButton/openRoadDrawingCrossSectionEditor pair. Rationale: widening now works post-placement, so the upfront modal was friction.
- 2026-07-12: DRAW-FIRST for every buildable type via the parcel panel's new Build palette (parcels/ui/proposal-actions.js: 8 icon buttons above the kept "Create proposal" button, which remains the path for terms-only types like purchase/as-is): building typologies + reparcellization go through startInstantProposalDesign (fresh draft seeded with the selection → openProposalDraftDesign → commit-on-close via the geometry-edit rail → applied object), park/square/lake are ONE-CLICK via instantCreateStructureFromSelection (selection-union geometry, lakes need contiguity, straight to instantCreateProposalFromDraft). The create dialog is now exclusively the "Create proposal" terms screen. Phase 2 (map-native design surfaces replacing the tool modals, structures first) deliberately deferred.
- 2026-07-12: The user-facing DRAFTS UI is gone (sidebar entry, floating map button, drafts-list overlay, their CSS — all removed): what is on the map IS the draft, editable until proposed. The proposalDraftStore stays as invisible plumbing (design sessions, instant create, publish staging, mid-drawing autosave) and unfinished corridor drawings keep their dashed on-map silhouettes (click to resume; close-toast reworded to point at the outline instead of the menu). Parked/unapplied proposals live in the existing proposals list with status badges — no separate "stash" menu was added (considered, judged redundant).
- 2026-07-12: Terminology pass: the block tool is "Block" everywhere user-visible (Build palette label + adapter label driving auto-names; primaryType 'Urban Rule' kept in DATA untouched); the details-panel "Stash" button is now "Unapply" (one term for one concept — the auto-park toast says "Unapplied ... — still in your proposals list" too); Row houses REQUIRE ≥2 parcels (palette gate); the Share-one-proposal modal shows an identity header (name + type + stored screenshot thumb) so links are never anonymous. Open taxonomy question (rename/collapse parcel-based vs block/row/building) still under discussion.
- 2026-07-12: Build tool names settled: parcelBased = "Freeform" (ruler icon; the only rules-not-form tool: limits + builder freedom), single = "Detached" (samostojeća), buildings = "Block" — internal keys unchanged. Stale-thumbnail fix: proposals/screenshot-refresh.js regenerates the stored screenshot in the background (debounced 1.5 s per proposal, silent failures) hooked into instantCreateProposalFromDraft, updateLocalCorridorGeometry and split-offs. Reparcellization manual mode: pentagon icon (was pencil), selecting Manual auto-arms the polygon tool, and both polygon and line drawing rubber-band to the cursor between clicks.
- 2026-07-12: Photoreal (Cesium) mode got the same Built/Planned two-state control as 3D mode (Solid/Transparent/Off per row, panel inside #cesium-container; Built = Google tileset via show + Cesium3DTileStyle alpha 0.5, Planned = proposal entities re-rendered at alpha 0.85/0.35) plus "Hide real buildings under proposals": ClippingPolygonCollection carves the Google mesh under every proposedBuildings footprint (guarded by isSupported; hole floor shows globe satellite terrain; auto-off when either row is Off). NO radius on purpose — Cesium streams tiles by screen-space error, so distance culling is automatic and a hard 1000 m crop would just look like a broken world.
- 2026-07-12: Photoreal polish: the display panel reuses 3D mode's exact classes (three-mode-ui-panel/segmented/segment — map.css selectors extended to #cesium-container) so both views have the identical upper-right control; a spinner pill (upper-left) shows "Streaming 3D tiles… N" from Cesium's tileset.loadProgress in-flight count (no percentage exists for view-dependent streaming); a one-time rotate hint (bottom-centre, desktop ctrl-drag / mobile two-finger wording, 4 langs) fades after 12 s or on first rotation and never returns once done (PersistentStorage photorealRotateHintDone). NOTE: Google photoreal mesh is unclassified — "keep only greenery" is impossible; footprint-level carving is the finest control available.
- 2026-07-12: Test/coverage pass for the SimCity batch: backend corridor-tunnel.test.js gained 7 corridorSurfaceRuns cases (644 backend tests green); e2e proposal-editor.spec.ts REWRITTEN for the new system (Build palette incl. one-click park + no legacy button, proposeExistingProposal→prefilled dialog, getProposalsForParcel ancestry, design-session scoping, instant create, 3D pick — the palette lives in the parcel panel's Proposals TAB, tests must switchParcelTab first); drafts-list/editor-dialog UI tests deleted with the UI. Full Playwright suite green (180 tests). The Proposals-tab "Create proposal" buttons were removed in favour of an "Offer" palette entry (fa-handshake) that opens the terms dialog for purchase/as-is types.
- 2026-07-12: Proposed-state model: dialog-created proposals get `termsConfirmed: true` (create.js both paths); the details panel shows "Proposal details" (expands the collapsed panel) instead of "Propose" for proposed/minted objects, and an edit strips the flag so Propose returns. One-jump undo: absorbing a source (instantCreate AND the propose path) stores the ORIGINAL record as `revertSnapshot` on the replacement (oldest snapshot carried through edit chains, statuses reset to unapplied); Delete on such objects offers Restore previous version / Delete everything / Cancel via showStyledChoice, restore re-adds+applies+selects (window.revertProposalToSnapshot). All instant-created objects now prefill description (generateDefaultProposalDescription) and a random offer (generateRandomRoadOffer). Parcel panel compaction: palette lost the "Build" title, park/lake/square/offer buttons tinted, proposal list items show parcel/owner acceptance as inline green-fill mini-bars on the ID/Author rows (exact numbers only in details). Buttons: Propose/Share shortened; road_source_incorporated toast fixed (key lives under alerts.messages, not ephemeral.messages).
- 2026-07-12: Details-panel buttons have FIXED slots (Edit · Propose/Details · Apply-or-Unapply · Share · Delete; Buy/Drive append) with a greyed disabled Edit placeholder when reshaping is unavailable; role colors: blue propose/details, green apply, YELLOW unapply (btn-warning, freed up since "Create proposal" became blue Propose), red-outline delete. Acceptance mini-bars show the exact count (3/40) centred over the fill. Propose-on-split-parent bug fixed at the root: prepareParcelSelection now substitutes a hidden parent's live descendant slices (excluding the corridor slice) and only seeds multi-select after resolution succeeds; stage failures alert visibly instead of rendering into the dormant shell. Revert stays SNAPSHOT-based (not keep-record-with-hidden-flag): identical information, but zero "exclude archived" filtering burden across list/sync/share/counts.
- 2026-07-12: Shared-link robustness: (1) the apply gate relaxes DRIFTED derived parents — a missing "root#p-token-N" parent no longer blocks when the root parcel's fabric exists in any form (_missingDerivedParentHasLiveFabric; slice ids drift between devices when load order or cutting rules differ); (2) pure geography conflicts get failure code 'parcel-conflict' (never retried by the shared-plan queue — previously 120 futile retries) and land in a friendly "stays unapplied (space occupied)" summary bucket; (3) the shared-plan result modal shows ONLY for genuine failures — duplicates and overlaps are a toast, so reloading a shared 3D link no longer blocks on a popup; raw prerequisite-id dumps moved to console. 3D is a viewing mode for now: the proposal details panel never opens in 3D (showProposalInfo guard), clicking a proposed object selects it (keepHighlightsWithoutUi=true — else the no-visible-UI safety net in selectAndHighlightProposal clears the selection!) and ISOLATES the proposal (click again to exit); e2e updated.
- 2026-07-12: Six 2D/3D interaction fixes: (1) 3D "whitening" root-caused — rebuildProposalInteraction3D's invisible-pick override crashed on MULTI-MATERIAL meshes (array .clone()), aborting the traverse and leaving visible white 'source' copies over the coloured mesh; now array-safe with per-proposal try/catch and visible=false last resort. (2) Parcel-click auto-open now EXCLUDES road proposals (every slice lists its road, so parcels next to corridors selected the road + node handles). (3) Node-handle hover tooltips render in the road-node-handles pane (z 660) — Leaflet's default tooltipPane (650) sits BELOW corridor strips (655). (4) Delete cleanup is key-based (ProposalSelection.getKey) + clears preview layers/panel — object-identity check missed and left blue outlines. (5) THE HOLES: parcels/ingest.js skipped any parent claimed by an applied proposal even when its replacement slices don't exist on this device (slice drift) — permanent unclickable holes, surfacing after 3D round-trips (resize→moveend→re-ingest); now the parent is only hidden when a live child layer (#p-/legacy _ prefix) exists. (6) Details panel was already closed on 3D entry (closeAllPanelsAndModalsFor3D) + never opens in 3D per the showProposalInfo guard.
- 2026-07-12: Round 2 of 3D/roads polish: (1) striped/whitened 3D blocks finally root-caused — buildProposalBuilding drew the FULL building coplanar OVER partial slices when coverage <95% ("drawing remainder"); now slices commit only at full coverage, else the building draws unsliced INSTEAD (never both). (2) Multi-select leak: prepareParcelSelection marks its seeding (__seededByEditor) and releaseEditorSeededMultiSelection disarms it on design-session finish AND closeProposalDialog — a cancelled Propose/tool no longer leaves "Multiparcel selection" armed (this is what looked like "dragging enables multi-select"). (3) Node drags into buildings get the unapply/tunnel/reroute prompt via updateLocalCorridorGeometry (definition snapshot reverted on reroute; tunnel records per hit edge). (4) Share-one-proposal modal has a "Share the whole plan" escape hatch → shareAppliedProposals. (5) requirePersonalizedUser now gates ONLY proposing/dialog flows (proposeExistingProposal, showProposalDialog, Offer palette, legacy road/track modals) — drawing, palette builds, geometry/cross-section edits are gate-free local editing. Policy decision on road edits vs dependent overlays: moving/removing a road KEEPS overlay proposals applied (geometry doesn't move; ancestry matching re-anchors them; "one parcel, two rules" allowed while geometries don't overlap) — only NEW footprint intersections prompt.
- 2026-07-12: Old-proposal stress test (proposals/8..35) triage — THREE general bugs found and fixed, rest is data drift: (1) updateLocalCorridorGeometry now ROLLS BACK the definition when unapply/re-apply throws (before: strips redrew from the mutated centerline while parcels never restored — "bulldozed segment, no parcels back"); status panel.road.editRevertedStatus. (2) _unapplyProposalConfirmed no longer hard-throws on unfetchable SYNTHETIC ancestors (they can't be fetched by definition; old imports drift) — it throws only when a real cadastre parcel can't be recovered, so old roads are editable again. (3) structureProposalsCoveringFeature (corridor-structures.js): parks/squares/lakes match parcels by GEOMETRY (centroid-in-shape) in the panel list and the click auto-open — declared-id drift no longer hides them. Plus: 3D buildings loader moved to upper-left with a pending-model counter (single /near POST has no incremental total), and the nearby-buildings context buffer is now max(200 m from proposal edges, ≈slider radius from centre) via halfDiagonal subtraction.
- 2026-07-12: Prod stack-overflow fixed: _unapplyProposalConfirmed recursed forever on CYCLIC child-proposal links (old imports carry mutual A<->B childProposalIds; a proposal's status only flips at the END of its frame, so the applied-check never breaks the loop) — now threads a visited set (unapplyWholeFamily already had one). The rollback-on-failure from earlier today contained the damage (roads reverted cleanly each time). Also: the map-navigation zoom-out e2e was persistently red, not flaky — Leaflet silently IGNORES setZoom issued while a previous multi-level zoom animation is still running; the zoom tests now pass { animate: false } everywhere. "[road centerline] insufficient points (bulk path)" is a rate-limited cosmetic warning for degenerate old centerline data.
- 2026-07-12: R-key building layer explained + fixed: ensureCorridorBuildingFootprintsLoaded (tunnel detection preload, from the corridor-tunnel feature) fetched footprints AND ticked showBuildings — it no longer flips visibility (data loads silently; layer removed from map if the checkbox is unticked, detection reads the layer object either way). fetchBuildings now MERGES viewport fetches into a deduped pool (cap 12000, oldest dropped) instead of replacing the layer with the current viewport — panning no longer "loses" buildings. Fully-tunnelled corridor bug: the surface-footprint parent filter emptied parentParcelIds when NO surface remained, failing validation and stranding the drawing as a dashed draft (drafts UI is gone, so it looked like a ghost) — now a fully-tunnelled corridor keeps its declared parents (applies+splits like a normal one, matching calculateChildFeatures' fallback). keepAsDraft now shows WHY (first validation error) as an error toast. OPEN UX QUESTION (user asked, advice given: keep the choice, add "Demolish" as a third option for REAL buildings later — needs demolishedBuildingIds on the road definition + render filtering; auto-destroy rejected as too surprising for a consensus tool).
- 2026-07-12: DESTROY-FIRST obstacle model + NO CORRIDOR DRAFTS AT ALL. Building obstacles now offer Demolish (primary/default) / Tunnel / Reroute; demolitions are stored on the road definition as demolishedBuildings [{id, geometry}] (footprint kept because 2D DKP_ZGRADE and 3D building_3d ids differ — 3D filters by centroid-in-footprint, 2D by id against the pooled buildingFeaturePool via rebuildBuildingLayerFromPool, refreshed on every corridor strip refresh so unapply restores buildings). Split-off components keep only demolitions their own footprint covers. showStyledChoice is keyboard-first: primary autofocused (Enter fires), arrows/Tab cycle, Esc = null. Esc while drawing cancels ONLY the stroke in progress (roadStrokeBaseCount trims to where the pen went down; committed segments stay); Esc/X with no active stroke APPLIES the road (finishRoadDrawing) — nothing is ever kept as a draft: dashed draft-road-layer.js deleted, mid-draw autosaves and the R-resume path removed (the draft store lives on ONLY as the invisible finish-time hand-off to instantCreate), "Draft autosaved" badge and saved-toast gone, keepAsDraft failures now console.error + error toast + REOPEN the drawing (draft deleted if reopen fails). Tunnels-only-inside-buildings (clip tunnel edges at building entry/exit) judged medium effort and deferred — Destroy default makes tunnels the exception.
- 2026-07-13: Three road-system features: (1) TUNNELS ONLY INSIDE BUILDINGS — clipCorridorEdgeThroughBuildings (corridor-tunnel.js, turf-injectable, footprints buffered by half road width so portals sit at the road-surface/facade meeting line) splits a tunnelled edge at entry/exit, portals become real centerline vertices, only inside sub-edges get tunnel records; wired into road/track click, finish-time recheck (splices live segments, edges with pre-existing records keep whole-edge semantics) and the node-drag path; degenerate no-interior-crossing falls back to whole-edge with the old behavior. (2) CURATED EXISTING ROADS — city-config `curatedRoads: {url:'/road-parcels'}` (Zagreb only; CityConfigManager.getCuratedRoadsConfig); backend /road-parcels now JOINs parcel on cestica_id to return parcelId ('HR-mbko-broj') + score; road-detection.js fetchCuratedRoadParcels marks parcels by id (addRoadParcel + updateParcelStyles → existing dark-asphalt roadStyle), auto-runs debounced 800ms on every 'parcelDataLoaded' event, and the Detect-existing-roads button uses the curated source INSTEAD of OSM/GUP/WFS when configured. Follow-up idea: full sidewalk+lane-stripe rendering needs centerlines — buildCorridorStripsForOsmFeature (corridor-profile.js) is built and unused, waiting for an OSM-centerline join. (3) CROSS-ROAD INTERSECTIONS — buildCrossCorridorJunctionTreatments (corridor-profile.js): vertices of one applied road lying on another's edge (≤0.75 m; snapping copies exact coords) become render-only nodes, junctions touched by ≥2 corridors get the standard asphalt+zebra treatment sized by the widest meeting profile, drawn once per refreshAppliedCorridorStrips on top of both roads' markings; local-road snaps still absorb into one proposal (unchanged), so this covers minted + coexisting roads. Vitest coverage added for clip + cross-junctions (not run per no-test-runs rule).
- 2026-07-13: TWO CITY VARIANTS ARE BOTH FIRST-CLASS, permanently: (A) full-coverage cadastres (Zagreb/HR) where EVERY piece of land is a parcel — roads are parcels with owners, kept as parcels (decision: do NOT erase detected road parcels; ownership/acquisition math, road supersession and merge-back-on-unapply all depend on them); (B) US-style fabrics (NYC) where roads are simply not in the parcel data and the "everything is a parcel" principle does NOT hold. Code must never assume a point lies in some parcel, nor that a road has an owning parcel. Related: cities with an authoritative/curated road source use it INSTEAD of OSM/GUP/WFS detection, but the open-source detectors stay for cities without one (road-detection.js branches on CityConfigManager.getCuratedRoadsConfig()).
- 2026-07-13: Boot gate for the early-click race ("toggleSidebar is not defined"): body starts with class `app-loading`; sidebar.css keeps #sidebar and .sidebar-toggle-button pointer-events:none (cursor: progress) while it's on; js/boot-ready.js — deliberately the LAST entry in index.html's script list — removes it, and a window 'load' listener in the head bootstrap is the fail-OPEN backstop should that one file ever 404. Panels/modals need no gate (hidden until a handler opens them). The MetaMask contentscript.js MaxListeners/ObjectMultiplex console noise is the extension's own, not ours.
- 2026-07-13: Zagreb field-test fixes: (1) show-buildings regression root-caused — rebuildBuildingLayerFromPool preserved "was on map before", but the corridor preload creates the layer OFF-map while the box is unticked, so the next tick inherited hidden; visibility now follows the showBuildings CHECKBOX (corridor-tunnel's post-fetch removal deleted as dead). (2) Road parcels get NO name tooltips ever (user: names irrelevant, only where+size); updateParcelStyles unbinds old road-name-tooltip bindings and sets feature.properties.isRoad=true so split children of a road parcel inherit the dark style (proposal-manager re-marks children carrying isRoad); curated fetch also persists isRoad. (3) Manual reparcellization "parcels overlap" false positive: booleanOverlap flagged hairline slivers from line cuts — now intersection AREA must exceed max(0.5 m², 0.1% of smaller plot). (4) fetchBuildings passes sourceSrid 3765 → no more bogus "backend transformation may have failed" warnings. OPEN DESIGN (user-raised, not yet built): one-owner land readjustment should be allowed by treating it as owner + synthetic "PUBLIC LAND" recipient (default opening = two plots); split-into-N-to-sell likely = assign new plots to public land, possibly a two-step flow (city acquires → reparcellates with none returning to the original owner); vanilla-3D camera stays locked for now (user: "probably fine").
- 2026-07-13: Batch 2 of Zagreb field-test feedback: (1) one-owner LAND READJUSTMENT allowed — the implicit second recipient is the existing PUBLIC-LAND pseudo-owner, opening at a 50/50 split (contribution accounting untouched: owner contributed all, public land 0, balance = worth of the ceded half). (2) 3D demolished buildings render as reddish "condemned ghost" material (buildingMaterials.demolished, opacity 0.3) following the Built control — never invisible, never mistaken for surviving fabric. (3) Freeform edits: modal retitled "Freeform" (was "Parcel-based Urban Rules"); generation now randomizes footprint SIZE (extra random inset up to ~18% of the envelope short side) and POSITION (random slide, containment-checked with retries) within the setback envelope, not just floor counts — setbacks are minimums, not the form. (4) All three design modals (Urban Rule/blockify, Freeform/parcelbased, Buildings/single-building) restructured: header row hoisted to span the FULL modal width with the X in the true top-right corner (new #*-body wrapper holds main|sidebar; mobile display:contents preserved). (5) "Detached opens urban-rules modal" root cause: adapter registry aliases are normalized through normalizeProposalGoalKey, and the buildings adapter's 'building(s)' alias normalizes to 'single', shadowing the real single adapter — registry.get now prefers direct adapter keys over aliases and the poisoned alias was dropped. (6) Repeat obstacle prompts fixed at THREE levels: corridorPayloadFromSeed dropped demolishedBuildings on finish-sync (proposals never carried their demolition records); tunnel-EDGE keys now short-circuit re-detection in updateLocalCorridorGeometry, corridorEditorCollectWidthHits AND the finish recheck (records list only draw-time-loaded building ids — later viewport fetches "discovered" new buildings under tunnel spans forever); edges with existing records absorb newly loaded ids silently. (7) Lane-explainer decorations (bike/pedestrian pairs) removed from 2D map rendering (drawn + applied corridors) — trees stay; lane meaning lives in the cross-section editor. IDEA (user): lane decals painted on the 3D ground surface would be acceptable — not built.
- 2026-07-13: Batch 3: (1) Offer palette entry opens Create Proposal with `ownershipOnly` override — land-use and parcels sections locked to "no change" and greyed (.proposal-goal-section--locked), only ownership choices live (build/parcel changes have their own palette tools). (2) Block detection fixed for curated roads: isCorridorParcel (parcel-blocks.js, the single choke point for all block flows) now also treats isRoadParcel() parcels as block boundaries — legacy detectors set isCorridor on road parcels but the curated source sets only isRoad, so flood-fill crossed streets. (3) Pedestrian footpaths: CORRIDOR_EDITOR_MIN_WIDTH 5→2 m and withCorridorWidth scales ALL strips proportionally when the profile has no driving lanes (delete every lane but a sidewalk in the cross-section editor → a 2 m footpath); vitest added.
- 2026-07-13: Two building-layer fixes: (1) obstacle prompts no longer depend on the "show existing buildings" checkbox — ensureCorridorBuildingFootprintsLoaded's early-exit used collectLoadedCorridorBuildings() which ALSO counts applied building proposals, so any nearby proposal made the preload skip fetching real footprints; it now gates on window.buildingFeaturePool specifically. Remaining honest limitation: the WFS fetch is zoom-gated (17–19), so drawing while far zoomed out still detects nothing until finish. (2) the 2D buildings layer is now interactive:false — it was intercepting parcel clicks; existing buildings are context in 2D, inspectable only in 3D. Building Info panel idea (area/height/floors/apartments/jobs/PPV value) noted as future work, judged overkill for now.
- 2026-07-13: Batch 4: (1) B hotkey toggles "show existing buildings" (same guard rails as the R hotkey: no modifiers, not while typing, not with a modal open). (2) hr "Samostojeća" → "Slobodnostojeće" (palette label for the single/Detached tool). (3) Edit-time obstacle checks now consider ONLY edges the edit INTRODUCED (pre-edit edgeKeys from definitionSnapshot are skipped, plus tunnel edges): bulldozing a stretch or profile-only edits never re-litigate remaining geometry — widening is still guarded by the cross-section editor's own width-hits check upstream. OPEN QUESTIONS discussed with user, not yet built: swap Freeform↔Detached names (single tool is true freeform manual placement; parcelBased is a detached-housing rule — agreed in principle, awaiting go); per-segment cross-sections on one road object (recommended model: one proposal per connected network + definition.segmentProfiles[segmentId] overrides, cross-section editor scoped to the clicked segment with "apply to whole road" option; footprint build needs per-segment widths).
- 2026-07-13: LABELS FLIPPED per user decision: parcelBased (setback+floors rules generating houses) is now "Detached"/"Detached houses" (hr/sr "Slobodnostojeće (kuće)", es "Aislada/Viviendas aisladas", fa-home icon); single (manual place/drag/rotate buildings) is now "Freeform"/"Freeform building" (hr/sr "Slobodna forma", es "Forma libre", fa-ruler-combined icon). Internal keys unchanged (parcelBased/single).
- 2026-07-13: PER-SEGMENT ROAD NETWORK MODEL (the big road sort-out): one proposal = one connected network of nodes and edges; the cross-section is a PER-SEGMENT property. Data: `definition.profile` stays the network default, `definition.segmentProfiles[segmentId]` overrides per segment (carried through every seed path: drawing context, drafts, corridorPayloadFromSeed, corridorSeed, corridorDefinitionFromSeed, create.js). Core helpers corridorSegmentProfile/corridorSegmentEntries/corridorHasSegmentProfiles (corridor-profile.js, exported+tested). RENDERING: strips/markings/tree decorations per segment with its profile; junctions reworked to PER-ARM treatments (corridorJunctionsWithArms + junctionTreatmentPerArm — each approach's asphalt patch and zebra span sized by ITS OWN profile; buildCorridorJunctionTreatments is now a uniform-profile wrapper; cross-corridor version keys corridor identity by proposal so segments of one road never form fake crossroads). FOOTPRINT/CUTS: buildRoadUnionPolygonWithWidths / buildRoadUnionPolygonForDefinition / buildCorridorAcquisitionPolygon (surface-only, per-segment; proposal-manager cuts use it); drawing preview + finish + drag-edit + merge + component-split all width-aware. EDITOR: clicking a road records the SEGMENT (corridorLastClickedSegment via per-segment hit targets); the cross-section editor opens scoped to it with a "This segment / Entire road network" toggle (i18n ×4) — segment save writes an override (deleted when equal to default), road save resets uniform (clears segmentProfiles); live preview is segment-scoped; width-hit check scoped too. MERGES: segment ids are minted fresh on collision (they collide across roads — every drawing counts s1,s2,...) so profiles follow THEIR geometry; the oldest body now donates only its NAME (no longer the cross-section); weldCorridorSegments welds only same-profile pieces (different profiles stay separate segments sharing a junction node) and keeps the id carrying the override; split-off components take only their own overrides. Obstacle checks (drawing recheck, drag path, editor) use each edge's own width. Tracks unchanged (uniform). Vitest: per-segment resolution, entries, per-arm junction sizing.
- 2026-07-13: Parcel Prijedlozi tab investigation+fixes: (1) descendant listing was NEVER abandoned — a slice lists proposals of its ancestor parcels via #p- suffix-stripping in getProposalsForParcel plus childParcelIds matching; sporadic misses were ID DRIFT cases, now rescued by geometry like structures already were: roadProposalsCoveringFeature (corridor-structures.js, applied corridors whose footprint intersects the parcel by >2 m² — area required so boundary-touching neighbours don't list). (2) "Prijedlozi (1) with empty list" was REAL: the tab count was set ~150 lines before the list content write, so any mid-render exception left a fresh count over stale content — count now updates only after the content lands in the DOM. (3) New "Ancestor" pill (Predak/Ancestro, panel.parcel.proposalsSection.ancestorPill) marks the proposal a parcel DESCENDS from (ancestorProposal prop or childParcelIds), shown only when the list has >1 entry. (4) Build palette: Blok/Niz/Slobodnostojeće wrapped in .parcel-build-rules-group (thin frame, grid-column span 3) marking the rules-based family; hr/sr "Nizovi kuća"→"Niz kuća". (5) Detect-button placement discussed: kept where it is.
- 2026-07-13: Per-segment follow-up fixes from field test: (1) absorbAppliedRoadIntoDrawing NO LONGER adopts the absorbed road's profile as the tool's active cross-section — that adoption silently overwrote a freshly set width (the "set 11, drew 7.4" bug); absorbed segments keep their widths via roadSegmentProfiles, the tool profile keeps driving new segments (extending a road's own polyline still continues at ITS width — the segment override covers the whole extended polyline). (2) redrawRoadStrips (live drawing asphalt) renders per segment with each segment's profile + ForEntries junctions — absorbed roads no longer repaint at the drawing width mid-session ("existing roads change width/outline while drawing"). (3) Esc stroke-cancel also removes roadPreviewPolygonLayer (the rubber-band outline lingered). (4) Selected road segment now paints an amber marching-ants outline over its exact footprint (renderSelectedCorridorSegmentHighlight in corridor-render.js, window.corridorLastClickedSegment + ProposalSelection gate, repainted on every strip refresh; dash-offset animation in corridor-render.css — Leaflet path options carry the colors, CSS only animates).
- 2026-07-13: Three features: (1) cross-section SEAM DRAGGING — the schematic's lane boundaries carry drag handles (col-resize, pointer-capture); withSeamMoved (corridor-profile.js) moves width between the two adjacent lanes (total fixed, 0.5 m lane minimum); during drag only widths/labels sync in place (corridorEditorSyncWidthsInPlace — a full render would kill the pointer capture), the commit on pointerup goes through corridorEditorApply. (2) RECESSED LAKES in 3D — parcel ground meshes get the lake footprint CUT OUT (cutLakesOutOfFeature in buildParcels3D), the basin renders below grade: beach solid from -1.6 to +0.26 (outer face closes the pit wall), shallow-shelf solid to -0.7, dark lake bed at -1.6, transparent water at -1.4, fish at -1.32 (was: shore 1.0/water 3.0 ABOVE ground). (3) ELEVATED EXISTING RAIL in 3D — ported from zagreb-isochrone's tram sim (station-3d/world/elevated-rail.js; source repo path is zagreb-isochrone-main) as js/elevated-rail-3d.js: merge-free (InstancedMesh shaft/cap/box-girder/twin-rails, no BufferGeometryUtils needed on three@0.147), Y-up→Z-up axis remap, resampleXY at 30 m bays, deck 7.5 m; data = frontend/data/zagreb_rail_tracks.geojson (97 KB OSM extract copied from the tram sim); city-gated via cityConfig.rail3d (Zagreb only, CityConfigManager.getRail3dConfig), fetched once/session, culled to min(7000, 1.5×content diagonal); follows the Built control and hides under isolation. Fancy girder styles (truss/plate) and pillar street-clearance skipped as Zagreb-sim polish.
- 2026-07-13: ROOT CAUSE of "merges repaint absorbed roads with the newest cross-section" (and most of the batch-2 field reports): insertCorridorCrossingNodes NULLED a segment's id whenever it inserted a crossing vertex — every merge has a crossing, so absorbed segments' profile overrides (keyed by id) orphaned at the junction step and everything fell to the merged default (= the newest drawing's profile). Ids now SURVIVE vertex insertion (inserting a node doesn't change segment identity). Belt-and-braces around the same class: (a) finish-merge stores an explicit override for EVERY surviving segment (no "equal to default, skip" pruning — rendering must never depend on the default); (b) drawing-session overrides are normalized at USE (roadDrawingSegmentOverride: invalid override → loud console.error + tool-profile fallback, never silently empty strips/dashed-line-only segments). Bug "connected road parcels lose dark grey": child road-marking in proposal-manager (both apply+restore sites) now also derives road-ness from window.isRoadParcel(parent ids), not only the parent feature's in-memory isRoad flag (timing-dependent before). NOTE: roads merged under the buggy builds carry the wrong profile PERSISTENTLY — no auto-repair; re-set their cross-section or redraw.
- 2026-07-13: Structures demolish by DEFAULT + "Surviving" 3D state: (1) parks/squares/lakes clear their ground WITHOUT prompting — demolishBuildingsUnderFootprint (corridor-tunnel.js, shared helper) unapplies proposal-owned buildings silently and records real ones as {id, geometry}; records live on structureProposal.demolishedBuildings (all three creation paths: palette instantCreateStructureFromSelection + both dialog paths in create.js); collectDemolishedBuildingRecords scans applied STRUCTURES alongside corridors, so 2D hiding + 3D condemned-red-ghost rendering + restore-on-unapply work unchanged; structure layer updaters (updateParks/Squares/LakesLayer) trigger the 2D building refresh. (2) The 3D Built control gained a 4th segment "Surviving" (hr/sr "Preostale", es "Restantes", threeMode.controls.stateSurviving) — solid existing fabric with the plan's demolished buildings NOT drawn at all (the after-the-plan ground truth); built-row only, buildNearbyProposalBuildings3D hideDemolished param.
- 2026-07-13: Redundant demolish prompt at F root-caused: absorbAppliedRoadIntoDrawing (snap-connect) copied the absorbed road's segments+profiles but NOT its tunnels/demolishedBuildings — the finish-time safety recheck then re-detected every building the absorbed road had already handled and prompted again. Absorb now inherits both record sets (deduped) and refreshes the drawing tunnel layer; click-time per-segment prompts remain THE decision point (user's preference — decisions can differ per segment), the F-time check is a pure safety net for buildings that never got asked (e.g. loaded mid-draw).
- 2026-07-13: Orphaned blue highlight while drawing root-caused: absorbing/merging a proposal that was currently SELECTED (continuing a selected road) removed the proposal but left its selection visuals (blue parcel highlights + details panel) orphaned; hover repainted them. clearSelectionVisualsForRemovedProposal (road-drawing.js, key-based like the delete path) now runs before every absorb/merge removal (snap-absorb, finish-merge, drag-merge). OPEN DESIGN (user asked, assessment given): PARTIAL building demolition for structures/building-forms — clip footprints at the proposal boundary (records gain remainder geometry; 2D replaces feature geometry with remainder; 3D approximates by swapping affected building_3d meshes for two extruded prisms — remainder normal + demolished part red ghost — real mesh CSG rejected); thresholds (clip <2m² ignore, remainder <15% → full); ROADS keep full demolition (a road through a building destroys it). Judged feasible, not too complex; awaiting go.
- 2026-07-13: PARTIAL BUILDING DEMOLITION shipped: splitDemolitionFootprint (corridor-tunnel.js, turf-injectable, vitest'd) clips a straddling building at the demolition region boundary — records gain demolishedPart+remainder (records WITHOUT remainder = full demolition, e.g. roads); thresholds: clip <2 m² untouched, remainder < max(10 m², 15% of footprint) → full. 2D: rebuildBuildingLayerFromPool swaps the feature geometry for the remainder instead of hiding. 3D: partially demolished buildings trade facade detail for truth — real mesh replaced by two extruded prisms at measured height (building3DHeightMeters): remainder in normal material + demolished part as red ghost (absent under "Surviving"). BUILDING TYPOLOGIES now demolish by default too: instantCreateProposalFromDraft computes the region from the proposal's parcels (parcelLayerById → buildGeometryFromParcels) and stores buildingProposal.demolishedBuildings; collector scans roadProposal + structureProposal + buildingProposal. Also: localhost:8080 = docker consensus-builder-frontend-1 with LIVE ./frontend volume mount (compose) — it serves the working tree; localhost cache token is Date.now() per load, so "stale fixes" reports on 8080 are timing (fixes landed after the page load), not staleness. Loud diagnostic added: a drawn segment producing no strips logs points+profile.
- 2026-07-13: Blue-outline-during-drawing FINAL root cause (console evidence: showProposalInfo re-rendered AFTER "Unapplied road proposal"): finishing a road SELECTS the new proposal (details panel opens); pressing R kept that selection alive through the drawing, and the absorb's key-based cleanup MISSED due to key-format mismatch (String compare vs ProposalSelection's normalized keys) — the unapply's UI refresh then repainted the blue highlight + panel. Fixes: clearSelectionVisualsForRemovedProposal now matches via ProposalSelection.is(proposalObject) (its own normalizer) with string compare only as fallback, callers pass the proposal OBJECT; entering a drawing session (requestCorridorDrawingTool) closes ANY open proposal selection outright (highlights, panel, amber segment ants — a drawing session owns the map). Also: absorb bumps nextRoadSegmentId past absorbed ids (duplicate-id bug → wrong finish widths → redundant F demolish prompt + profile mixing); refreshAppliedCorridorStrips got per-proposal try/catch (one corrupt road stripped the asphalt off EVERY road — the "roads reduced to white dashed lines"); showStyledChoice focuses the primary via rAF and treats Enter as the primary even when focus was stolen (the "demolish preselection works only on first popup" bug). Roads created before these fixes carry corrupted definitions — wipe and rebuild.
- 2026-07-13: THE RESURRECTION LOOP (root cause of "none of the fixes are in"): unapplyProposal runs _restoreReplacementSource — the one-jump-undo machinery — which RE-APPLIES the proposal's previous generation. An absorb-while-drawing unapplies the snapped road → its ANCESTOR resurrects (old profiles, old records, same name) → the next click absorbs that → grandparent resurrects... console signature: repeating "Unapplied road proposal"/"Applying proposal X ... done" cycles. Fix: unapplyProposal option skipRestoreSource, passed by ALL subsume paths (snap-absorb, finish-merge, drag-merge, and corridor-tunnel's destroy/cut/structure unapplies of building proposals). Restore-on-unapply remains for USER-initiated unapply/delete only. Note: an absorbed road's own revert lineage is dropped (subsumed) — acceptable.
- 2026-07-13: "CUT THROUGH" for roads/tracks — now the DEFAULT obstacle choice (Cut through / Demolish / Tunnel / Reroute): the corridor region is carved out of the building footprint via upsertCutRecord (corridor-tunnel.js, turf-injectable) — per-building accumulation: later segments crossing an already-cut building EXTEND the cut silently (decision stands); near-total cuts convert to full demolition (remainder < max(10 m², 15%)); records reuse the partial-demolition shape (demolishedPart+remainder → 2D remainder swap, 3D prism pair, Surviving hides the part). Wired into road/track clicks, finish recheck and drag path; session filters split full vs cut records. i18n modal.corridorTunnel.cut ×4, offer text reworded.
- 2026-07-13: Parks/lakes not auto-demolishing root cause: the one-click structure path scanned with whatever building footprints happened to be loaded (usually none — only the road tool preloads). Creation paths now await ensureCorridorBuildingFootprintsLoaded() before the scan, and _applyStructureProposal BACKFILLS the demolition list at apply time when it is missing/empty-unscanned (sp.demolitionScanned flag) — existing parks/lakes heal on their next unapply→apply.
- 2026-07-13: 3D built-context loading now frames THE WHOLE PLAN: appliedWorkFramingGeometry (three-mode.js) builds a bbox superpolygon over every APPLIED proposal's geometry (road polygons, structure geometry, building footprints, reparcel polygons) and feeds it to POST /buildings/near with the existing edges-apron formula max(200 m, slider radius − halfDiagonal). Framing priority: shared-link proposal frame → applied-work superpolygon → camera point. The fetch key is the bbox, so the load refreshes as the plan grows.
- 2026-07-13 (2nd pass — why the "fixed" trio kept regressing; three DEEPER shared-infrastructure root causes found and fixed): (1) BLUE SELECTION: `window.currentlyHighlightedProposalId` (set in selectAndHighlightProposal) was NEVER cleared anywhere — clearProposalHighlights nulled only the proposal object — so proposal-manager's "restore selection after apply/unapply" checks matched the stale id and re-selected mid-drawing whenever an absorb touched a road the user had EVER clicked. Fixed (cleared in clearProposalHighlights) + choke-point guard: selectAndHighlightProposal now REFUSES (console.error with stack) while roadDrawingMode/trackDrawingMode. (2) F DOUBLE-PROMPT: building identity was the POOL INDEX — Zagreb DKP_ZGRADE features carry ONLY `ZGRADA_ID` (verified via live WFS; no object_id/id, and convertGeoJSON drops feature.id), no code checked it, so every building fell to `building:<index>` which shifts between click time and F time (fetch merges, demolition filtering) → click-time demolish records unmatchable at finish. Fixed: buildingIdentifier checks ZGRADA_ID (+OBJECT_ID), falls back to geometry-derived key, NEVER index; exported as corridorBuildingKey and used by map-core's 2D-hide keyOf (which previously returned '' for Zagreb — id-based 2D hiding never worked) and fetch dedupe. Click-time detection also now uses the per-segment override width (roadDrawingWidthForSegmentIndex), same as the F check. (3) LEGACY GREY LOSS: both road child-feature builders derived remainder isRoad from the parent's `props.isRoad` ONLY — legacy/curated road parcels have NO such prop (status lives in roadParcelsSet) — so remainder slices were built with explicit isRoad:false; also `isCorridor` was wrongly set = parentIsRoad (now inherits parent's own isCorridor). getParcelBaseStyle/getParcelStyle additionally honor feature.properties.isRoad. ALSO: swept ALL remaining resurrection vectors — skipRestoreSource added to unapplyWholeFamily (threaded through recursion, both restore sites incl. the already-unapplied branch) and to: autoParkConflicts ("freshest drawing wins" parking), conflict-modal unapply-and-retry, one-structure-per-block, unique-building replacement, shared-plan conflict unapply, shared-import bulk unapply, drag-edit re-place (unapply→reapply same proposal), bulldoze-to-nothing, editor-shell + dialog-create absorbs. Only the user-facing "Remove from map" button still restores (intended one-jump undo). [f-check] console.warn added when the finish safety net fires (lists unmatched ids vs session records).
- 2026-07-13: 3D demolition/cut matching rewritten from centroid-containment to OVERLAP-AND-CARVE (three-mode.js buildNearbyProposalBuildings3D): every record contributes a demolished REGION (demolishedPart||geometry); for each building_3d mesh, regions overlapping its footprint hull (bbox precheck → turf.intersect, parts <2 m² ignored) are unioned and carved out; sliver remainder (<max(10 m², 15%)) → whole ghost, else remainder prism + ghost prism at measured height. Reason: DKP (2D) and building_3d subdivide buildings DIFFERENTLY — one 3D hall spans several DKP footprints, so centroid-in-record-footprint matching randomly missed ("cut through works sometimes"), leaving roads vanishing into uncut meshes.
- 2026-07-13 (3rd pass): F double-prompt residual root cause = THRESHOLD MISMATCH: detection floor was 0.25 m² but record-writing (upsertCutRecord/splitDemolitionFootprint) refuses clips under 2 m² — a building grazed by 0.25–2 m² prompted at click, wrote NO record, and re-prompted at F. findBuildingTunnelIntersections default minimumArea now 2 (matches record thresholds). "Some buildings not cut" root cause = COVERAGE: the footprint pool only ever contained viewport fetches (zoom-gated 17–19, maxFeatures 2000), so buildings never loaded were never detected/prompted/recorded. New machinery in map-core: fetchBuildings(boundsOverride) (skips zoom gate for targeted fetches), buildingFetchCoverage rects (a fetch that hit the 2000 cap does NOT claim coverage), window.ensureBuildingFootprintsForBounds(bounds) (50% padded fetch). Call sites: road click edge, track click edge, finish-check pre-pass (whole corridor), drag-edit introduced edges, demolishBuildingsUnderFootprint region. PLUS: 3D meshes with NO 2D counterpart can never earn a record — applied corridors now ALSO carve their SURFACE footprint (tunnels excluded via corridorSurfaceFootprintForDefinition) out of crossing meshes at a higher bar max(15 m², 5% of footprint) so facade-grazing dataset offsets don't prism-ify street fronts; retroactive (derives from definition.polygon, no records needed).
- 2026-07-13: structure footprints (park/square/lake, structureProposal.geometry) added to the 3D record-less clearance-carve alongside corridor surface footprints — buildings "surviving" on lakes/parks were meshes the 2D scan never recorded (coverage gap or no DKP counterpart); same max(15 m², 5%) bar, retroactive for already-applied structures.
- 2026-07-13: crossing nodes now minted at FIRST PLACEMENT: finishRoadDrawing runs insertCorridorCrossingNodes on the session segments (after the tunnel check) — previously only the drag path (updateLocalCorridorGeometry) and the finish-merge did, so a stroke crossing segments the session had snap-ABSORBED finished without junction nodes ("dragging heals it"). insertCorridorCrossingNodes gained protectedEdgeKeys (tunnel edges are never split — their records key by exact edge; a spliced vertex would silently revert the tunnel to surface); all three call sites pass their tunnel keys.
- 2026-07-13: 3D corridor rendering ported to per-segment entries (three-mode buildCorridorStrips3D now mirrors 2D refreshAppliedCorridorStrips): strips/decorations per corridorSegmentEntries entry with its own profile, junctions via buildCorridorJunctionTreatmentsForEntries — the old whole-centerline+default-profile call flattened networks to one width and dropped override-only lanes (per-segment tree groves never rendered in 3D).
- 2026-07-13: unclickable lake/ground under structures root cause: _buildAppliedDescendantIndex counted applied STRUCTURE proposals as parcel-replacing descendants, so a road re-apply (width/geometry edit) SKIPPED recreating the remainder slice under an applied lake/park ("hidden by applied descendant") → hole in the parcel fabric; structure visuals are interactive:false, so clicks fell through to nothing. Structure-only proposals are now excluded from the descendant-blocker index (they OVERLAY parents by rule). Likely also the cause of the older "unclickable space around proposals" report. Existing holes heal on the road's next re-apply (small drag or unapply/apply).
- 2026-07-13: mobile pass over the road-editing stack — convention: touch adaptations key off `(pointer: coarse)` (CSS media + `matchMedia` in JS), not screen width. ROAD_SNAP_PIXELS 12→26 on touch (finger-missed node snaps created near-miss disconnected junctions); node/junction/bulldoze handles 14/18/12→26/32/24; cross-section seam grab 14→32 px (visual line recentered via left:50% so it survives any width); corridor-editor inputs 16px font on touch (iOS zoom-on-focus threshold) + 38px steppers; 3D control panel spans the top edge ≤480px (4-segment Built row), segments get ellipsis + finger height, loader pill moves bottom-centre; Detect block button 34px on touch.
- 2026-07-13: mobile panel policy set: bottom-sheet info panels cap at 50vh (utilities.css generic rule + id-level caps for road/block panels which out-specify it); panels needing more go FULL height (proposal-details already does, with its minimize). Road drawing panel got a minimize button (−/+ next to close, is-minimized class): collapsed strip keeps title + Cross-section + Undo/Finish, hides metrics/ownership — pattern mirrors proposal-details' setProposalDetailsPanelMinimized.
- 2026-07-13: "parks/squares/blocks unclickable after a while" (misattributed to 3D exit) root cause: BOTH parcel ingest paths (parcels/ingest.js attachParcelEvents, parcels/selection.js onEachFeature) checked isParcelDrawingModeActive() at BIND time and skipped attaching the click handler — every parcel fetched or child slice applied WHILE a road/track drawing session was active came out permanently unclickable; structures/buildings visuals are all interactive:false so they read as dead. onParcelClick already ignores clicks during drawing at CLICK time, so the bind-time gate was redundant — removed; handlers always attach now. (3D was a red herring: users just notice while inspecting after a session.)
- 2026-07-13: reload-clickability root cause (2nd overlay-as-replacer instance): boot restore (_reapplyAppliedProposal) used _shouldSkipChildFeature which treated ANY persisted descendantProposal marker as "replaced by descendant" — structures stamp that marker on their ground parcels, so every road slice under a park/lake was skipped at boot AND its parent hidden → dead ground after every reload. _shouldSkipChildFeature now resolves the marker: skip only for APPLIED descendants that REPLACE parents (structure-only proposals never count). RULE OF THE DAY: any "has descendant ⇒ hidden/replaced" logic must exclude overlay proposals — three separate copies of this bug existed (_buildAppliedDescendantIndex, _shouldSkipChildFeature, and the ingest-time click-handler gate).
- 2026-07-13: structure geometry editor (upstream park/square furniture editor) is now modal over the map: global.isStructureGeometryEditorActive() gates onParcelClick + highlightFeature (no select/hover/click on parcels under the structure while editing); opening the editor closes the proposal card + parcel panel (captures ProposalSelection key, restores card on CANCEL; on save the shell re-selects the rebuilt proposal).
- 2026-07-13: pink-shaded parcels explained: appliedProposalStyleTemplate gave every PARENT parcel of an applied building/park/square a red 0.2 fill (partial overlap sufficed — the whole parcel shaded). Now border-only (fillOpacity 0); the proposal's own visuals carry the information. City dropdown: native macOS <select> popups anchor at the selected option (open upward near screen top, unfixable via CSS) — replaced with a custom .city-dropdown (button + listbox, always opens down); the hidden native #city-select remains the source of truth (value + 'change'), so all consumers unchanged.
- 2026-07-13: whole-block suggestion on the build palette: startParcelBuildTool with EXACTLY one selected parcel silently floodfills its block (new window.detectBlockParcelIdsForParcel in parcel-blocks.js — same visibility+floodfill as Detect, no animation/selection); if the block has 2..60 parcels (BLOCK_SUGGEST_MAX_PARCELS) a styled confirm offers "select whole block?" — Yes runs animateFloodfillFromSelected + lands on the Proposals tab WITHOUT auto-launching the tool, No proceeds single-parcel. Runs before the row-needs-two guard; 'offer' excluded (targets the picked parcel). Square edit-geometry verified: same buildStructureAdapter/openStructureGeometryEditor as park — all editor-exclusivity fixes are kind-agnostic.
- 2026-07-13: structure geometry editor round 2 — DECISION PIVOT: benches are FIRST-CLASS placeable objects in BOTH park and square editors (user changed his mind mid-request away from "benches derive from footpaths"); the bench-angle slider is REMOVED (bearing kept in data, always 0 for new benches; legacy bearings still render). Square editor gains stall/Table (🍽 restaurant tables) and statue (🗿) tools; every square gets ONE auto-placed statue (autoStatuePoint — centre-offset candidate corners; ensureSquareDecorations upgrades existing squares). Editor layers moved to a dedicated pane z=655 (default marker pane 600 sat UNDER the squares surface pane 630 — icons were buried). While the editor edits a structure, structures.js skips that structure's static decorations (window.structureGeometryEditorEditingProposalId) so no inert duplicates. 3D parity: parks render STORED tree coords (deterministic via treeRng; random sampling only for legacy parks without tree data), flowerbeds, benches; squares render fountains array + trees + benches + tables + statues via shared furniture builders (addTree3D/addBench3D/addTable3D/addStatue3D, module-scope materials).
- 2026-07-13: leftover sheds on parks/squares/lakes in 3D: the record-less clearance carve required overlap >= max(15 m², 5%) — a <15 m² shed FULLY INSIDE the region could never reach the bar and survived. Bar is now capped at half the building: max(2, min(max(15, 5%), 50% of footprint)) — big-building graze protection intact, small buildings clear.
- 2026-07-15: proposal-list Server tab showed goal ICONS not the server-rendered thumbnails though the DB had them (272/278 Zagreb) and GET /proposals/summary returns them — `normalizeServerProposalSummary` (proposals/server-sync.js) dropped `screenshotUrl` on the way into the view model; buildProposalThumbHtml (the only reader) then fell back to the badge. One-line map fix + test/server-proposal-summary.test.js. Lesson: missing picture ⇒ check the WHOLE chain (render → DB row → API field → client normalizer).
- 2026-07-15: urban-rule "Edit shape manually" froze/failed on large blocks. Root cause: the parametric outline is ~100k vertices (robustNegativeBuffer rounds every corner into GEOM_BUFFER_STEPS over a many-thousand-point superparcel — measured 97,751 on the Gredelj block), and manual mode dropped ONE draggable Leaflet marker per vertex → tab freeze then error, leaving the user stuck with all sliders greyed out. Fix: simplifyRingToVertexTarget caps the editable ring at MANUAL_MAX_VERTICES=60 (binary-search on DP tolerance; sub-budget rings untouched); enterManualMode simplifies the seed, build re-clips inside parcel; a first-build failure on entry falls back to parametric (manualBuildSucceeded flag) instead of trapping. test/urban-rule-manual-simplify.test.js. Verified by evaluating building-blocks.js in the SAME node realm as turf (NOT a vm — vm realms silently no-op turf.simplify because instanceof breaks across the boundary; a vm test "passes" on an unchanged 97k ring).
- 2026-07-15: pre-commit NUDGES added (new pure module frontend/js/proposal-warnings.js — DOM-free, turf-free, CommonJS-requireable, added to writeVersionedLocalScripts): (1) oversized block — assessBlockSize(perimeter) flags walk-around > 15 min (~84 m/min; user-chosen threshold), confirmBlockSizeIfOversized in building-blocks.js; (2) row houses on parcels varying >3× in area or wide Polsby-Popper compactness spread — assessRowHouseSimilarity, confirmRowHouseParcelSimilarity in row-house.js. Both gate the "Done" handler via showStyledConfirm (z 30000, above the editor), both async (click-handler-only callers), best-effort so a measurement hiccup never blocks a save. i18n ×4 (blockify/rowHouses .warnings). test/proposal-warnings.test.js.
- 2026-07-15: complex-parcel QUOTA error no longer breaks the block flow — the draft autosave's persist() (proposal-drafts.js) swallows storage-write failures (a giant geometry threw QuotaExceededError straight into the build flow, surfacing the raw "setItem exceeded the quota" in the status bar and aborting; drafts now stay in memory only on failure). AND showErrorPopup z-index 2000 → 30050 so the friendly "too complex" dialog renders ABOVE the modal (12050), up front, not behind it (why it only appeared after closing the editor). Quota-resilience case added to test/proposal-drafts.test.js.
- 2026-07-15: MOBILE block + row-house dialogs — the 2D map + 3D view both capture touch and together filled the viewport, so the only scroll grip was a 40px margin "strip" between them. Dropped the strip; sized map 36vh / 3D 28vh (blockify) and 36vh / 26vh (row-house) with min-heights so the slider section peeks below the fold and becomes the scroll grip (modals.css).
- 2026-07-15: EXISTING-ROAD CENTRELINES + impact-resolver Phase 1. New GET /osm-road?bbox= (backend/routes/osm-road.js) serves osm_road centrelines; frontend/js/legacy-road-centerlines.js draws a read-only, viewport-scoped, zoom≥14 line layer with a sidebar toggle (Zagreb/Croatia data only). osm_road is owned by cadastre-data-constraints (ingest fetch-osm-roads.js + db/osm_road.sql + migrations geom_3765/multicity); canonical schema = geom 4326 + generated geom_3765 (Croatian TM, indexed) + city (zagreb+split). consensus-builder's local `zagreb` DB held a STALE 3765-only Zagreb-only osm_road — realigned to prod by dump+restore (prod is canonical; do NOT convert prod to 3765). CAUTION: DROP osm_road CASCADE also drops road_parcel_classification MV (restored from prod). sync-tables now refuses+warns on local/prod column drift (agents repo). Endpoint filters geom_3765 && bbox(3765), outputs geom(4326)+city.
- 2026-07-15 DECISION (see impact-resolver.md): a proposal a road runs into is only SET ASIDE (unapply) / TUNNELLED / DESTROYED — never cut in place or split into two. Non-contiguous proposals are a firm no-go, and a bisected building has no natural two-piece meaning, so splitting (and cut-in-place) stays deferred. Mutability predicate isProposalImmutable(p) = isProposalMinted(p) || !!p.serverProposalId (server upload = publication, can't be rewritten). Phase 1 = predicate + setAsideObstacleProposal seam, no behaviour change.
- 2026-07-18: CORRIDOR TAB + MOVE-TO-FIT in the cross-section editor. New pure module frontend/js/corridor-clearance.js (IIFE, no bare globals; in writeVersionedLocalScripts after corridor-profile.js): perpendicular clearance sampling per station → corridor min width / pinch / rigid-fit shift interval / per-vertex variable offset with smoothstep taper. Editor grew a tab bar (Cross-section | Corridor, proposal mode only): headline corridor min width vs current, buildings-only vs +road-parcels constraint toggle (non-road parcels already crossed by the road are NOT walls), SVG width-profile chart (hover readout, click pans map), map overlays (dashed blue halo = available corridor, dark pinch tick + amber pinch buildings), what-if-demolished lines, "Move road X m <compass>" + "Widen toward: <side>" buttons. Shift bakes into definition.points via updateLocalCorridorGeometry (so cuts/tunnels/prompts replay); endpoints welded to other segments/roads are held and the shift tapers (taper = max(10, width), densify first). Directions shown as compass (corridorCompass8), never "left/right" — users can't know the drawing direction. tests: corridor-clearance.test.js (21), corridor-editor-fit.test.js (write-back shape handling). i18n ×4 under modal.corridor.
- 2026-07-20 (SUPERSEDED): PHOTOREAL ROAD FORMATION + SEALED CUTS. Roads in realistic 3D now use a separate temporary corridor group built from <=4 m longitudinal stations, terrain samples, bounded NoData interpolation, robust spike rejection and a smoothed engineered profile; each station has one shared elevation across every lane/kerb so roads follow long grades without crossfall, while leaving the normal abstract-3D corridor group untouched and restoring it exactly on exit. The photoreal cut uses a dedicated whole-column road mask at 0.5 m/texel, inset 0.65 m beneath the deck to hide raster scallops. Cut-shell closure combines the terrain-to-formation curtain with non-degenerate formation-height walls and inward cross-plane flanges, so vertical facade/tree triangles no longer produce zero-area caps or sky-visible holes. Rationale: raw per-vertex draping warped cross-sections, a single flat road plane floated on grades, and downward-only caps cannot seal vertical source triangles. Pure geometry lives in corridor-terrain-formation.js and photoreal-seam.js with node tests. CORRECTION: the first live diagnostic's 145 stations and 2.03 m value described the profile's min/max range, not an endpoint-to-endpoint drop.
- 2026-07-20 (SUPERSEDED): TERRAIN-PROFILE SOURCE DIAGNOSIS. Realistic roads currently sample a 22x22-capped grid raycast from the seated Google Photorealistic 3D Tiles surface, then run a 5x5 morphological low-envelope/3x3 median "bare ground" estimate before bilinear sampling; this is NOT DGU DTM and its z values are local scene offsets, not EVRF2000. A long road can therefore use ~25-30 m source cells despite 4 m render stations: one surviving low photogrammetry/LOD cell becomes a broad multi-station trough that the longitudinal smoother preserves. A fresh Zagreb reload reproduced a ~5.1 m min/max range while the visible surface did not share the dip. Treat this grid as cut-curtain support, not authoritative road elevation; a future road fix should use direct multi-ray station samples with symmetric outlier rejection plus grade/vertical-curve constraints, or a datum-aligned authoritative bare-earth DTM.
- 2026-07-20 (SUPERSEDED): ROAD PROFILES NOW SAMPLE THE GOOGLE MESH DIRECTLY; DGU IS OUT OF THE CURRENT PATH. The 22x22 cleaned grid remains only for parks/non-road ground bands and fallback curtains. Each <=4 m road station raycasts the seated Google Photorealistic mesh at the centreline plus two width-aware probes just outside the road edges; a valid centre hit wins unless both sides agree that it is a positive car/canopy/roof obstacle. Missing centres with disagreeing sides stay NoData, short bounded NoData gaps interpolate, and only >1.5 m locally unsupported mesh spikes are replaced — there is no broad low-pass that can erase a real terrain hollow. One shared longitudinal profile drives every lane/kerb/marking/deck edge and the road cut is a whole-column mask. Split surface runs reuse their parent profile, coincident graph-node endpoints are reconciled, unrelated roads have bounded support, and seam/curtain rings use the same 4 m sampling density. Sampling snapshots the visible tile meshes once per carve rebuild, caches probes at 5 cm, ignores the temporarily hidden tiles root during first-frame visibility classification, and performs one settled-LOD refit. Browser validation on Road 2007-0151: 101/101 true-metre-spaced stations, 749 visible-mesh probes, 0 misses/fallbacks, 399.14 m length, -2.27 m endpoint delta. Google Elevation API is a possible future independent/fallback source, but it returns local-mean-sea-level heights while 3D Tiles use an ellipsoid/ECEF frame and Google does not promise both products share an elevation dataset; compare relative profiles after fitting a constant offset before substituting it.
- 2026-07-20 (SUPERSEDED): FINAL TERRAIN SOURCE + PHOTOREAL-SIM CUT PORT. The direct Google station profile above remains the immediate/fallback profile and supplies a robust local-scene datum, but DGU 20 m EVRF2000 now supplies the relative longitudinal shape when `POST /api/terrain/profile` passes coverage/MAD gates. Only one median additive offset is fitted (never a vertical scale or absolute-datum comparison); a 2 m endpoint tolerance handles geodesic-vs-local-Mercator chainage drift without filling real DGU gaps. Road 2007-0151 browser check: 1/1 DGU reference accepted, 101 stations, Google calibration MAD 0.56 m initially and 0.10 m after finer-LOD refits, +0.17 m endpoint grade instead of the false Google-mesh -2.05 m drop, 781 visible probes and zero misses. The road sits 5 cm above formation plus its 5 cm strip offset. The road mask now ports photoreal-sim's height-aware rule: alpha stores each 4 m station quilt's local formation and Google is discarded only above formation-0.4 m, retaining ground below elevated spans. A mathematically owned 0.316 m true-metre raster collar (half texel diagonal + 10 cm guard), source-textured formation-height fascias, and a clamped inward flange eliminate pavement scallops and hollow facade/tree cuts; the same station quilt is widened through the collar, isolation filters cut patches, and valid split-run profiles are promoted instead of falling back to z=0. Up to three quiet-burst refits let finer visible Google LODs supersede the first coarse fit. Production must expose the DGU endpoint through `/api/terrain/profile` or set `window.__CORRIDOR_TERRAIN_PROFILE_URL__`; otherwise the tested Google-visible fallback remains active.
- 2026-07-20 (SUPERSEDED): PHOTOREAL ROAD EDGE OWNERSHIP CORRECTION. The 0.316 m theoretical raster collar plus asynchronous source-triangle flanges was not watertight: the visible sidewalk stopped before the cut, and the mask was the union of an exact ruled quilt with a Turf-buffered polygon whose rounded ends/bends had no matching surface. Top-down views therefore still exposed cyan 0.5-scene-metre mask teeth and grey per-triangle shards. Ported photoreal-sim's actual invariant: one exact padded station quilt now drives the mask (including real endpoint cap intervals), a solid sidewalk-coloured station foundation extends 1 m from the semantic edge with a 0.6 m downward skirt, and the cut is centered 0.5 m inside it. Per-tile flanges are clamped below the foundation and serve only cosmetic vertical facade/tree closure. Browser validation on Road 2007-0151 at close top-down LOD showed smooth continuous edges, one exact road mask + one matching foundation, no cyan/grey teeth, and no console warnings/errors.
- 2026-07-20 (SUPERSEDED): CURRENT PHOTOREAL ROAD INTERFACE — EXACT CUT + DEPTH-TESTED RULED COLLAR. The 1 m beige apron and the later inset-mask experiment both failed in close real views: one exposed Google/flat-slab competition; the other could leave blue and was not geometrically contained at bends. Roads now use the exact station quilt for a 4096² linearly filtered height mask. A uniquely paired station foundation extends only half a raster texel diagonal + 10 cm (0.193 true m in the Zagreb test), tops at formation+0.04 m and skirts 0.6 m down. The narrow sidewalk-coloured collar is explicit station-by-station ruled geometry, continues the actual outer lane/sidewalk height on each side, and remains depth-tested with only a small coplanar bias; it neither flattens a nonlinear drape nor paints through nearer Google geometry. Non-road mask classes use alpha-zero shaders so red/green coverage cannot corrupt linearly filtered road heights. Google stays underneath as the no-sky backstop, and the RGBA8 cutoff is quantisation-bounded below the solid cover. Generic Turf road cuts and all source-textured road seam caps are disabled; the latter were the grey asphalt shards. Google centre/outside/semantic-edge probes estimate plausible crossfall while rejecting roofs/cars, and DGU is accepted only when its calibrated value stays within 0.25 m of Google at every station. An eight-pass upper vertical-curve envelope never lowers accepted support and reduced Road 2007-0151's worst adjacent grade from ~14.7% to ~8.3%. That road currently uses Google (DGU rejected for local variance): one mask, one foundation, one vector collar, 101 stations, 747 hits and zero misses. Close top-down/oblique checks show no large cyan gaps, raster-scale scallops or grey shards; incomplete multi-road LOD refits retain the last complete road/mask/foundation set atomically. Focused formation/ground/seam tests: 58/58.
- 2026-07-20 (SUPERSEDED): CURRENT PHOTOREAL ROAD GROUND + CANOPY-CUT CONTRACT. Direct seated-Google station hits remain the local-scene anchor, but broad canopy/building surfaces can fool centre and side probes together. DGU 20 m EVRF2000 is now calibrated by one additive offset from the observed coherent lower Google/DGU residual band and contributes relative shape only inside runs where Google is >=1.5 m above DGU and a separate 20% maximum-grade envelope proves an abrupt obstacle. Exact Google support is retained elsewhere; smooth Google over a low DGU pit and all-canopy routes without trustworthy anchors fail closed to Google. The hybrid raw profile is re-filtered so canopy approach ramps do not survive. The blue height-aware road mask then removes Google trees/buildings above that accepted formation. Shell closure is generated only around the exterior union of padded foundation roofs: source-textured vertical fascias, across-flange only as a degenerate fallback, plus a depth-tested clearance curtain where stored Google top exceeds formation by >0.5 m. This closes wholly enclosed canopy holes without internal T-junction walls or generic Turf cuts. Browser validation on proposals 63..85 except 81: 6 complete profiles/replacements, 1,502 visible hits, 0 misses/failures, 4 DGU profiles, 440 exterior seam segments, 124 clearance segments; the former canopy road now follows ~0.57–1.70 m local formation. Focused formation/ground/seam tests: 63/63.
- 2026-07-20: CURRENT PHOTOREAL ROAD GROUND + NEUTRAL CLEARANCE CONTRACT. Keep the obstacle-filtered Google/DGU formation and exact blue canopy/building cut from the previous entry, but NEVER register road boundaries for source-textured per-triangle seam caps. The padded-foundation union is outside the unpadded shader quilt; intersecting it with every retained canopy/roof triangle duplicated Google textures, stretched their endpoint UVs down to formation, and produced the dark floating fins seen at grazing angles. Road closure is now one neutral, depth-tested curtain around only the exterior union of paired foundation roofs. It requires finite formation support and rises only when Google is >0.5 m higher; its top is max(stored longitudinal Google support, cached direct boundary top), capped at 30 m, so it covers both wholly enclosed canopy and local boundary trunks/facades without internal T-junction walls. Retained source geometry depth-occludes the neutral curtain. Browser validation on proposals 63..85 except 81: 6 complete profiles/replacements, 1,941 visible hits, 0 misses/failures, 4 DGU profiles, 0 road source-seam segments, 160 neutral-clearance segments; former canopy road remains ~0.57–1.70 m. Focused formation/ground/seam tests: 64/64.
- 2026-07-20: STRUCTURES (park/square/lake) ARE CLEARANCE REGIONS — clear ALL existing fabric under their footprint, then place the proposal. Fixes buildings surviving under structures in both 3D and photoreal. Root cause: commit 18cf211 ("the buildings we cut are the buildings we render") deleted three-mode.js's record-less clearance carve in favour of pure object_id record lookup (carveBuildingByObjectId), so any building the 2D footprint scan missed (coverage gap, or a /buildings/near mesh with no counterpart in the buildingFeaturePool it scanned) had NO demolition record and rendered as a survivor (visible even in "Surviving only"). Fix (3D): re-added a record-less fallback SCOPED TO STRUCTURES only — collectAppliedStructureClearanceRegions() + structureClearanceCarve(bld, regions) in three-mode.js; the per-building carve is now `carveBuildingByObjectId(...) || structureClearanceCarve(...)`. It returns the same {remainder,demolished} shape (hull-footprint ∩ structure geometry; full raze when remainder < max(10 m²,15%), else partial at the edge), so a mesh fully inside a park is hidden in Surviving and a straddling one keeps its outside part. Roads KEEP their precise record-based cut (tunnels/partial slices). Do NOT re-remove this fallback under an "only records, no geometry guessing" cleanup — 18cf211's premise that detection⊇rendering (1:1 object_id) does not hold when the scan runs against a partially-loaded pool. Fix (photoreal): parks now full-cut like squares/lakes — photoreal-mode.js collectCarveGeometries() drops the isPark keepveg branch (always mode:'full'), so the red whole-column mask discards buildings+trees+ground above the floor. This REVERTS the deliberate keepveg design (commits 006d3ef/0ecb8c8, "parks keep their vegetation") per explicit user choice "flatten all existing stuff". keepveg shader infra (maskMaterialPark/green branch/CARVE_KEEPVEG_BAND_M) is now dead but left inert for easy revert if trees-kept is ever wanted back.
- 2026-07-20: MODE TERMINOLOGY going forward = "model" (abstract 3D / three-mode) and "photo" (photorealistic Google view / photoreal-mode), matching the split in zagreb-isochrone. Light-scope adoption chosen (NOT a full internal rename): (1) URL params — proposals/core.js readers now accept canonical `?model` (is3DModeRequestedFromUrl) and `?photo` (isRealisticModeRequestedFromUrl) alongside the back-compat aliases `?3d`/`?mode3d` and `?rw`/`?rl`/`?real` (old shared links keep working; there is no auto-writer of the flag — users hand-append it to /proposals/<id> links). (2) User-facing labels — the realistic (globe) button + i18n threeMode.toggle.realistic → "Photo view (Google Photorealistic 3D)" (4 langs); the 3D button tooltip threeMode.toggle.switchTo3d → "Switch to model (3D)" (glyph stays "3D"). (3) New code/comments use model/photo. Internal identifiers (three-mode.js, photoreal-mode.js, window.enterThreeMode, setRealisticLayerActive, body classes three-mode/realistic-mode-active, i18n key names) were deliberately LEFT as-is. Also fixed: photoreal apron cap is coloured per structure kind (lake→water 0x3a8ad3, square→paving 0xb8b0a2, park→grass 0x6f8f4f) so a cleared lake reads as water instead of the bare-earth apron (0x6e7563); the recessed abstract lake basin sits below the cap and is hidden in photo mode.
- 2026-07-21: FREEFORM SURROUNDINGS (ground treatment). A freeform (single) building proposal now carries ONE choice for the whole merged parcel area around its buildings: unchanged (default), paved (square styling) or green (park styling). Decisions: three states with `none` as the default, so no existing proposal changes appearance; the surround clears existing fabric — which needed no new code, because building typologies ALREADY demolish buildings on their parent parcels (proposal-editor-shell.js scans the whole parcel region and parks records on buildingProposal.demolishedBuildings, honoured by collectCarveRecords in 2D/model/photo); freeform ONLY, since the blockify existing-buildings mode exists to preserve what stands there. Data: `proposal.geometry.groundSurface = { treatment, polygon }` — the polygon is computed ONCE at design time (block union minus every footprint) and persisted, never re-derived at render time, so it cannot drift when parcels move. New pure module frontend/js/building-ground.js (UMD, `window.BuildingGround`, loaded right after footprint-geometry.js) owns the geometry, the treatment vocabulary, the shared 2D fill styles and `appliedSurfaces(proposals, isApplied)`; three renderers consume it — structures.js (own Leaflet layer, reusing the squares/parks panes, the square paving texture and the corridor cut so a road slices the surround), three-mode.js buildProposalGrounds3D (plannedFlatGroup, refreshed via the new `buildProposalGrounds` hook in three-structure-refresh.js), photoreal-mode.js (a `kind:'covered', mode:'full'` carve entry whose structureKind maps paved→square / green→park, so the apron cap is paving or grass). Refresh is driven by a new `buildingGroundsUpdated` event dispatched from updateBuildingGroundLayer. Also fixed on the way: updateProposedBuildingsLayer only dispatched `proposedBuildingsUpdated` when the layer was NON-empty, so unapplying the last proposed building never reached 3D or the photoreal carve.
- 2026-07-21: A PROPOSAL THAT BECOMES A PARCEL NOW SHOWS THAT PARCEL. Established first: one road = ONE parcel. _buildChildFeaturesFromDefinition (proposal-manager.js) builds a single corridor feature (isCorridor:true, parentParcelIds = every crossed parcel) and starts childFeatures with it; everything else in that list is the REMAINDER of each crossed parcel with the road cut out. A T-junction into an existing road of the same kind does not cut it — road-drawing.js collects touchingRoads as merge candidates and absorbs it ("Connected to '{name}' — now one road."), carrying its segments, profiles, grade separations and demolition decisions, so the junction is one proposal with one corridor parcel. Structures (park/square/lake) create NO child parcels — they are non-interactive in 2D so clicks already fall through to the parcel underneath. Change: selectAndHighlightProposal (proposals/layer-render.js) now also opens the proposal's OWN parcel info, collapsed to its header, next to the collapsed proposal card. Which parcel that is lives in the new pure module frontend/js/proposal-own-parcel.js (window.ProposalOwnParcel, node-tested): exactly one child → that child; several children → the one flagged isCorridor; otherwise null (a reparcellization IS its slices, not one parcel). The parcel panel gained a real minimized state (setParcelInfoPanelMinimized + #parcel-info-minimize button + #parcel-info-panel.is-minimized), driven for this case by the one-shot window.__openParcelInfoCollapsed flag — same idiom as __openProposalDetailsCollapsed. It is presentational only: showParcelInfoPanel never touches selectedParcelId/currentParcel, so the road stays the selection and the parcel panel is genuinely secondary. corridor-render.js's hideParcelInfoPanel() on road click was LEFT in place — it ends the user's own parcel selection before the proposal selection re-opens the road's parcel.
- 2026-07-21: PHOTOREAL SEATING FAILED SILENTLY. tryLockGround returns early on `groundZ === null` (no probe hit the streamed tiles), and that return sits ABOVE the stability/LOCK_MAX_WAIT_S timeout fallback — so a session where no Google tile geometry ever arrives never seats, never sets tiles.group.visible, and never logs anything: pressing the globe looks like it does nothing at all. Now reports once per entry past the wait budget with loadProgress and a pointer to the network tab (failed Cesium Ion / Google requests), plus an on-screen status. The Ion token is hardcoded at photoreal-mode.js:14, so an expired token or exhausted quota presents exactly this way. Diagnostic: the line to look for in the console is `[photoreal] world seated: ground shifted …` — its ABSENCE after `[photoreal] streaming Google 3D Tiles anchored …` is the signature.
- 2026-07-21: APPLIED BUILDING PROPOSALS DID NOT COME BACK ON RELOAD (roads did). Root cause was NOT persistence — the proposal was in storage and hydrated into `proposedBuildings` every time. `#showProposedBuildings` has no `checked` attribute and its state is persisted NOWHERE (toggleLayer in sidebar-management.js only reads it), so it starts UNCHECKED on every load; loadExecutedBuildingsFromStorage gated its first render on that box being checked, so the hydrated buildings were never drawn. Every creation/apply path ticks the box, which is why it looked right until a refresh. Roads have no such gate — hence "roads survive, freeform doesn't". Fix: an applied building is part of the map, so the load path now ticks the box and renders whenever the pool is non-empty. Locked by backend/test/executed-buildings-hydration.test.js (evaluates the classic script in-realm behind DOM stubs, the urban-rule-manual-simplify.test.js pattern); test 1 (hydration) passes under BOTH behaviours, test 2 (render) fails under the old gate — verified by reverting the gate.
- 2026-07-21: `status` WAS AN UNDECLARED GLOBAL IN hydrateProposedBuildingsFromProposals (building-blocks.js). `props.proposalState = status` had no `status` in scope: in a browser that silently resolves to window.status (the legacy status-bar string, ''), so every reloaded building was stamped '' and then relabelled 'applied' by upsertProposedBuildingFeature — an EXECUTED proposal's buildings quietly lost their 'executed' state on reload. Outside a browser it is a hard ReferenceError, which is how the headless characterization test found it. Now derived like the apply path: getLifecycleStatus(p) === 'Executed' ? 'executed' : 'applied'. Worth grepping for other bare identifiers that happen to be real window properties (status, name, length, origin, top, self, closed, event) — in classic scripts they never throw, they just read the wrong thing.
- 2026-07-23: PROPOSAL MODEL DECISIONS (rethink-proposals.md §9-§12 is canonical). (1) Every proposal = FORMATION (merge∪cut with an ownership flow) + CONTENT; reparcellization is pure formation, rules/votes pure content; roads already implement cut-then-merge (one corridor parcel, per-parent remainders). (2) D2 DECIDED: unit of consent = the owner's SLICE (plan restricted to proposals whose base ancestry touches their parcel), per-proposal acceptance bound to the slice content-hash; triage per (owner,proposal) = acceptance (formation taking their land) / offer (content on retained descendants) / vote (rules) / disclosure (content on ceded land). (3) THREE FRAMES: official cadastre (read-only import) → effective/legal frame (official + executed formations; proposals are ON-CHAIN and auto-execute when all accept — funds disburse, legally binding) → per-plan hypotheses. Executed formations become force-applied shared ground truth with a "legal, awaiting registry" badge; after any frame advance every pending proposal gets a computed status unaffected/re-based/impacted via effect-hash (footprint + per-owner cession) — consent survives iff the effect is unchanged. Ownership machinery is ASSUMED WORKING for now (his call) — design fields, don't build redistribution. (4) Rework step 1 SHIPPED: handleSharedPlanRoute orders the plan by resolveApplyOrder (intersection + createdAt) and, on dependency failures whose missing ids are all ghost-derived (#p-…), re-parents from geometry against live fabric (resolveParentsByGeometry, ≥95% coverage guard, once per proposal) — this was the 2/8 "Missing prerequisite parcels" prod bug. Payload-share route + single-proposal upload gates still use old logic (next step 6).
- 2026-07-23: CLAIMS MODEL + CONFLICT TOUR (branch proposals-rework, worktree consensus-builder-proposals-rework; rethink-proposals.md §13 canonical). Everything clickable is a CLAIM: proposal content > fabric piece > bare parcel (the null claim); derived parcels stop being an interactive class. New pure modules js/proposals/claims.js (__claims: baseParcelIdsOf/dossierFor/ranks) + conflict-tour.js (__conflictTour: stopKey/blanket decisions) + browser layer claims-ui.js (__claimsUi). Structures flip: park/square/lake base fills clickable → open proposal (bindStructureClaimClick; reverses the old non-interactive choice). Breadcrumb "On parcels:" on the details panel opens BASE parcels even when consumed/hidden (claims-ui falls back to parcelLayerById — selectParcel only searches the ON-MAP group). Parcel panel Proposals tab gains a claims rescue (base-ancestry membership). Cadastre view toggle (#cadastre-view-toggle, fas fa-border-all, bottom 240px): clones of all base parcels in cadastreViewPane (Leaflet strips 'Pane' → .leaflet-cadastreView-pane), all other panes dimmed to .25/inert via body.cadastre-view. CONFLICT TOUR in handleSharedPlanRoute: cross-plan parcel-conflicts pause the ordered apply → modal Replace/Keep + "do this for all"; scenario-2 silent auto-replace REMOVED; replace sweeps the applied bucket for members dragged down by unapplyWholeFamily and requeues them. showSimpleShareModal GOTCHAS: onClose fires BEFORE action onClick (defer dismiss-defaults a tick) and modal DOM is removed before onClick (cache checkbox state on change). All verified in a clean sandbox (localhost origin + prod API via fetch monkeypatch): clean 8/8, keep path 7+kept-section, replace path 8/8 + foreign unapplied-but-stored.
- 2026-08-02: ŠIBENIK (Šibenik–Vodice) ADDED AS A CITY, and the generic-city 3D sources CONSOLIDATED onto cadastre-data's tables. (1) Parcels needed NO ingest — `public.parcel` already holds all of Croatia (14.4M current rows, 3,445 KOs), so Šibenik/Vodice/Bilice/Srima/Tribunj/Zaton-Raslina were already there and the existing bbox `/parcels` route serves them unchanged, exactly as for Split. Only OWNERSHIP was missing: `parcel_info` was Zagreb-only (411,467 rows, 58 KOs). Fetched via cadastre-data's DGU GraphQL script, which grew a `--maticni-broj-ko` filter because a grad/općina is far bigger than a city (grad ŠIBENIK = 48 KOs / 439,616 parcels vs the 11-KO corridor / 92,253). Corridor KOs: 329886 Bilice, 329924 Crnica, 329975 Donje Polje, 330019 Gorica, 330108 Mandalina, 330116 Martinska, 330256 Srima, 330264 Šibenik, 330299 Tribunj, 330337 Vodice, 330353 Zaton-Raslina. (2) DECISION (user): one ingestion, one set of tables, shared with zagreb-isochrone. This repo's private `overture_feature` table + `backend/scripts/ingest-overture.js` are DELETED; buildings now read `public.overture_building_footprint` and trees `public.osm_decor` (kind='trees'), both owned by cadastre-data. The duplication was real and measurable: Split existed in both with identical stats (53,537 buildings; trees 3,878 vs 3,885 — Overture base/land trees are themselves derived from OSM natural=tree), so every city had to be ingested twice. Belgrade was the only city living solely in the old table: re-ingested into the shared one via cadastre-data (187,126 rows, now WITH osm_id which the old copy lacked) and its 3,185 trees migrated into osm_decor. (3) Cities read a REGION, not their own id — `overture-cities.js` entries now carry `region`, and `sibenik` → `sjeverna-dalmacija`, the Zadar–Šibenik–Knin box already pulled for the M606/M607/L211 rail reconstructions (187,545 buildings). Querying by city id would hit an empty row set and render no buildings with no error; a unit test pins the indirection. (4) `copy-overture-to-prod.sh` is now REGION-SCOPED — the old whole-table `pg_dump --clean` was safe on a private table but would have deleted zagreb/rijeka/corridor rows from the shared one. (5) Fixed on the way: `CITY_CODE_TO_ID` (backend/routes/proposals.js) never mapped `st`, so Split's server proposal list had been filtering on city='st' and returning nothing since Split was added; the guard test compared a hand-copied fixture to itself, so it now parses CITY_QUERY_MAP out of frontend/js/city-config.js and cannot drift again.
- 2026-08-02: DEEP-LINKED PARCELS NOW RESOLVE THEIR CITY FROM THE DATA, NOT A PREFIX. `parcelIdToCityId` (frontend/js/parcels/route.js) mapped every `HR-` id to 'zagreb', so a shared link to any Split or Šibenik parcel opened Zagreb — the parcel still loaded (one countrywide dataset behind one route) but the city selector, currency and, worse, the city a proposal made from there is filed under were all wrong. Rejected the obvious fix, a hardcoded KO→city table: ~90 literals that go stale whenever a KO is added or a city's extent changes. Instead `resolveCroatianCityId` asks `/parcels?parcel_id=`, which serves the WHOLE country regardless of the current city, and picks the nearest city among those whose `parcels.source === 'oss-wfs'`. A fourth Croatian city needs NO change here. The source filter is load-bearing, not decoration: Vukovar is ~140 km from Belgrade and ~230 km from Zagreb, so unfiltered nearest-city sends a Croatian parcel to Belgrade, where it would be fetched from /parcel-bg and never found (locked by a test). CityConfigManager gained `findNearestCity(lat, lon, {filter})` and `getCitiesByParcelSource(source)`. The lookup carries a 6 s AbortController deadline (overridable via `__CB_CITY_LOOKUP_TIMEOUT_MS__`, which keeps the test at 0.6 s instead of idling for the real timer) and falls back to 'zagreb' on any failure, so a wedged backend cannot stall the deep-link boot. test/parcel-deep-link-city.test.js evaluates the REAL city-config.js in-realm behind a window stub, so it exercises the actual city coordinates rather than a restatement of them.
- 2026-08-02: TREES ARE NOW PLANTED FROM GREENERY, AND BOTH RADIUS QUERIES WERE SCANNING THE TABLE. (1) Šibenik looked bare and the suspicion was a stale/clipped decor load. It was neither: OSM has 139 mapped `natural=tree` nodes across the whole Šibenik–Vodice strip, and an Overpass count over central Šibenik returns 43 — EXACTLY what osm_decor holds. Overture cannot help (its base/land trees are derived from the same OSM tag, which is why the two Split copies agreed to within 7 points). NO RELOAD OF ANY SOURCE PRODUCES MORE MAPPED TREES THERE. What OSM does have is green AREAS: 1,812 greenery polygons over 9,830 ha on the same strip. So decor/overture-trees.js now unions mapped points with points scattered through `osm_decor` kind='greenery' (what zagreb-isochrone already does with the same rows): Šibenik 17→139 trees at 300 m, Split 215→397, Belgrade unchanged at 107 (no greenery rows → the scatter is a no-op, not an error). Derived presentation, computed per request and deliberately NOT written back to the shared table, because the isochrone plants its own and stored copies would double up there. THE LATTICE IS ANCHORED TO ABSOLUTE EPSG:3857 METRES with jitter hashed from the integer cell indices — never from the query point, never from a polygon clipped to the current radius — so clipping only ever removes candidates and the woodland cannot crawl as you pan. Verified live: 136/139 trees identical to ~1 cm across a 100 m pan, radius 300 a strict subset of radius 450, repeat queries byte-identical. The 3857 step is divided by cos(lat) from each polygon's OWN centroid (3857 units are ×1.38 off metres at Šibenik) so spacing is stable per polygon; `greeneryTreeSpacingM` overrides the 12 m default per city. (2) Found while measuring: `ST_DWithin(geom::geography, …)` READS LIKE an index lookup and is not one — the cast puts geom behind a function call, so the GIST index cannot serve it and Postgres scans every row computing geodesic distances. Both providers did it. An `&& ST_Envelope(ST_Buffer(…))` prefilter (exact superset of the circle, results unchanged) measured back-to-back on the same box at 300 m over Šibenik: buildings 3,185 ms → 75 ms (42x), trees 2,654 ms → 309 ms (8.6x). The whole /decor/near request is now 175 ms INCLUDING the greenery scatter, under a tenth of what the old tree query alone cost. Grep for other `::geography` radius predicates without a bbox prefilter.
- 2026-08-02: `?backend=` OVERRIDE WAS IGNORED BY THE CROATIAN PARCEL PATH — a ./dev.sh worktree loaded ZERO parcels for Zagreb/Split/Šibenik. buildParcelRequestParams' `dataSource === 'localhost'` branch built its URL from the hardcoded LOCAL_BASE (port 3000) instead of getBackendBase(), so every parcel request went to a port nothing was listening on and the only symptom was a wall of ERR_CONNECTION_REFUSED on :3000 while the page itself served fine from its real port. Every OTHER city source (parcel-ba/-bg/-lj/-co/-nyc) already called getBackendBase(), so this was the single branch that ignored the override — and it is the branch the three most-used cities take. This is the SECOND instance of the same bug (buildBuildingRequestParams was fixed earlier with the identical reasoning, comment still in place), so the guard is now structural: test/data-source-backend-override.test.js asserts LOCAL_BASE/UGT_BASE appear ONLY inside getBackendBase() — anywhere else is another silent port-3000 bug. Both new tests verified to go red with the fix reverted. End-to-end after the fix: the page's own buildParcelRequestParams yields http://localhost:4582/parcels?... and the 9 grid cells around the Šibenik default centre return 4,325 parcels (the two near-empty cells are the harbour).
- 2026-08-02: REWORK STEPS 2-4 BUILT + FOUR DECISIONS RECORDED (rethink-proposals.md §7/§12/§14 canonical; branch proposals-rework, main merged in). Decisions (Simun): parks/squares/lakes FORM their parcel and transfer to public (a stay-yours "park" belongs to urban rules later); a formation owes the owner their remainders as DISCLOSURE, never an automatic reshape (proposer wants the scraps → another proposal); cross-plan conflicts at apply = the conflict tour (already built); land assumed FIXED — §11 frame machinery stays designed-not-built. Built: ownership-flow.js (per-parcel cededM2 + destination stamped at publish → proposal.ownership_flow; cadastreFrame capturedAt → cadastre_frame; effectFingerprintOf = footprint quantised ~0.1m + cessions + goal, from STORED fields only); dossier.js (§10 triage acceptance/offer/vote/disclosure with the chain rule via creation time — a take wholly inside an EARLIER formation's ground defers to it — + remainderReport) surfaced as chips + remainder note in the parcel panel; acceptance records stamp effectHash and refreshAcceptanceValidity runs before every execute (lapse on edit, revalidate on edit-back, legacy records stay valid; on-chain path still raw parentParcelIds — deferred contract change); A6 prerequisite graph replaced findAncestorTree in ensureAncestorProposalsUploaded (overlays gate on NOTHING; deadlock unrepresentable); payload route got resolveApplyOrder + ghost re-parenting; failure messages name the blocking PROPOSAL (describeMissingPrereqs, i18n ×4); counts/list/summary queries now also match cadastre_parcel_ids (badge truth for base parcels). Migrations dry-run default: add-ownership-flow.js, backfill-ownership-flow.js. 1752 unit tests green.
- 2026-08-02: UNAPPLY TOUR + RECUT FINDINGS (rethink-proposals.md §13 canonical). Unapply/delete dependents modal replaced by an impact-tour-style dock (js/proposals/unapply-tour.js, __unapplyTour): clickable list → map focuses+blinks the item (proposal red / slice orange, fabric first via __claims.claimKindForGoal); _showDescendantsConfirmModal delegates and keeps the old modal as fallback; removal semantics unchanged. ROAD DECISIONS (Simun): a road can only demand what happens ON ITS CORRIDOR — cut|tunnel, never destroy (demolition of a clipped building = separate purchase/demolition proposal against the remainder; structural-nonviability = dossier disclosure). RECUT: already automatic — runLocalCorridorGeometryUpdate (road-drawing.js:842) unapplies→re-derives parents→re-applies on every placed-road edit, but with skipConfirm:true (silent slice re-mint = the §3.1 ghost factory) and NO width-diff gate on placed-road profile saves (drawing mode has footprintChanged); building impact tour fires only for newly-hit buildings on widening. Cut/tunnel choices live in definition.demolishedBuildings (remainder⇒cut)/definition.tunnels, read by apply (tunnel spans acquire nothing) but NOT in the effect hash yet — must join it when choices become consent-bearing.
- 2026-08-02: RECUT ASYMMETRY FIXED + NARROWING SEMANTICS CONFIRMED. runLocalCorridorGeometryUpdate now compares derived footprints (buildRoadUnionPolygonForDefinition of snapshot vs mutated + tunnel edgeKeys + edgeFill): unchanged → short path (persist/mirror/re-render, identity detached because content changed); fully-unchanged definition → no-op keeping published identity; only real footprint changes unapply→re-apply. NO headless harness for road-drawing.js — gate verified by syntax+suite+browser only. Narrowing a road correctly RESTORES building remainders in the freed band (re-carve re-cuts stored full geometries at the new corridor) — slivers standing there are honest, they belong to the owners again; what's missing is the LOCAL cascade: overlapping applied proposals (parks) are not re-applied after a road recut, so structure clearance doesn't raze slivers under park footprints until manual unapply/reapply (replay already orders this correctly via A6). A park only clears ITS footprint — a park drawn to the old 7.5m edge never covers the freed band; growing it is authoring, not cascade.
- 2026-08-02: EVERY APPLY REFUSAL NOW RECORDS ITS REASON (invariant #6 in rethink-proposals.md §6). All ~24 formerly-silent `return false` sites across apply/road.js, buildings.js, structures.js, parcels.js now call _setLastApplyFailure({code, message, missingIds/conflict ids}) first — decision.defer sites skip (recorded upstream by _resolveParentAvailabilityOrDefer). NOTE: _setLastApplyFailure whitelists ONLY message/code/missingIds/conflictTitles/conflictProposalIds — bake counts into the message text. Trigger: user's 7/8 plan replay with "Proposal did not apply" and empty failure info; clean-slate AND all-unapplied-slate sandbox replays both give 8/8 (agent-browser headless, city-mismatch dialog appears on fresh profiles because geo-default resolves to New York), so his exact dirty state was unreproducible — next occurrence will name its cause. The dependent-cascade after road recut: DECLINED by user, not needed.
- 2026-08-02: STALE-APPLIED ROADS SELF-HEAL ON SHARED REPLAY. User's twice-failing Road 2107-2045 was diagnosed BY the new refusal reasons: his storage had applied=true with the child slices gone (half-unapplied session state), so apply entered RESTORE mode and demanded a dead generation → 'restore-missing-children'. Fix in importAndApplySharedProposal: on exactly that failure code, drop the lying applied flags (top + typology sub-objects), re-apply FRESH once (definition re-derives the cuts, §A2); a fresh apply that then misses parents flows into the existing requeue/ghost-healing. Verified in a sandbox with the planted stale flag: 8/8, new-generation children minted. Diagnosis time from named reason: minutes — the invariant-#6 instrumentation paid for itself on its first day.
- 2026-08-02: ORPHAN SLICES — unapplied-subdivision parcel remaining on the map is NOT expected; diagnosis: debris from pre-fix sessions (earlier unapplies removed wrong-generation child ids, leaving live slices unclaimed). Current-code repro of the exact sequence (unapply square → unapply subdivide) is CLEAN: slices off-map, parents restored; unapplied slices legitimately stay in the in-memory INDEX (hidden — descendants must resolve) but never visible. Fix: ingest-funnel guard (parcels/ingest.js) — a derived id whose MINTING proposal (new-style `#c-<proposalId>-N` literal; old `#p-` tokens kept conservatively) is present and NOT applied is dropped from map+cache and its persisted record cleared; verified live (ghost dropped, legit slice kept). Apply-time adds bypass ingest so mid-apply slices are safe. A reload self-cleans an affected profile.
- 2026-08-02: UNAPPLY MUST NOT RESURRECT PARENTS ANOTHER APPLIED PROPOSAL STILL CONSUMES. User selected base 823/2 under an applied road: the subdivision's unapply "restore ancestors" step re-added every declared parent with no cross-proposal check, stacking the whole parcel under the road's slices. Fix in apply/unapply.js: _parentStillConsumedElsewhere (structural `<parent>#` prefix scan; new-style slice ids attribute to their minting proposal whose applied state decides; unattributable old tokens count only when on-map) + _filterRestorableParents applied at all three restore sites (road, decide-later, reparcellization). Also fixed the '#p-' literal in the road-unapply missing-ancestor check — new '#c-' slices were misread as missing BASE parcels and could block unapply. Verified in sandbox with the doctored record: 823/2 stays hidden, 823/5 restores, road untouched. Suite 1756 green.
- 2026-08-02: "UNAPPLY DID NOTHING" = ghost parent aborting the family transaction. Road 2043's declared parents still carried the sender's dead-generation slice id (823/1#p-1mkonr8j4t2-1); the road-unapply RESTORE resolution was the only one of the three restore sites called WITHOUT allowMissing:true, so _resolveParcelFeaturesByIds THREW on the ghost, executeRoot aborted the family unapply mid-way, and the unapply-tour caught the error but closed silently. Fixes: allowMissing:true at the road restore site (+ comment); the tour's onConfirm failure now shows a styled alert with the error (modal.unapplyTour.failed ×4) — invariant #6 applied to the UI. Verified: planted the exact ghost on an applied road → family unapply completes, slices removed. ALSO observed in his console: the full healing chain worked on the way in (stale flag → downgrade → fresh apply → missing parents → geometry re-parent → applied).
- 2026-08-02: ROAD DRAWING NOW SHOWS ITS COLLISION TARGETS. "Segment looks clear but click says it hits a building": detection is honest (exact intersect of the segment corridor at real width vs the GDI buildingFeaturePool, 2 m² floor, no inflation) — the GDI footprints were simply NOT DRAWN on the default map, and the raster basemap draws smaller/different outlines (his #338230 museum footprint far exceeds the raster drawing). Fix (his call): entering road drawing auto-enables the GDI reference layer silently (autoShowBuildingsForRoadDrawing — no B dialog, other surveys untouched), and leaving restores the previous state unless the user flipped it themselves. Verified: before=false → during=true → after=false. B dialog unchanged for manual survey picks.
- 2026-08-02: FINGERPRINT V2 + REPLAY FIDELITY (rethink-proposals.md §7 items 9-10 canonical). Upload identity `c2-…` is blind to parentParcelIds EVERYWHERE (top level AND nested typology payloads — the nested lists were the easy miss); legacy `c-…` bytes kept as proposalContentFingerprintLegacy for (a) upload adoption — uploadProposalToServer HEADs the v1 id and reuses it when the server has that row (verified: same serial, no new row; note adoption only fires for the AUTHOR's unchanged copy — an imported+healed record has genuinely different content and correctly mints fresh) and (b) content-only CONSENT binding (parcel targets are part of an offer's terms; also keeps pre-v2 acceptance stamps valid). Replay fidelity: collectRebasedSharedProposals in both shared routes compares stamped ownershipFlow vs live-derived (compareOwnershipFlows, tolerance max(5 m², 5%), missing parcel counts only if loaded) → one summary line + console detail; verified no false positives on a clean 8/8 replay and correct detection of a forced 500 m² divergence. Local DB got 3 harmless test rows (a healed-road duplicate + 2 adoption fixtures). Suite 1766 green (one run flaked 3 files, vanished on rerun — same parallel-scheduling family as geo.test.js).
- 2026-08-03: STREAM COMPLETE UP TO THE ON-CHAIN BOUNDARY. (1) Geometry-first parent resolution: both shared routes run the ghost re-parent BEFORE the first apply attempt (plan route: proactive tryReparentGhostPrereqs at the attempt site; payload route: canApplyProposal precheck + reparentSharedProposalByGeometry for records already in storage) — verified 8/8 with 7 proactive re-parents, failure round-trips gone. (2) Cut/demolish/tunnel verdicts JOINED the effect hash (collectImpactModes reads definition.demolishedBuildings — remainder⇒cut — and definition.tunnels buildingIds; null when no records so record-less hashes are untouched): a cut→tunnel mind-change now lapses acceptances. (3) Recut disclosure: footprint-changing edit of an applied road WITH dependents opens the unapply tour 'recut' variant (i18n ×4); cancel rolls the definition back BEFORE the identity detach (footprint signature + disclosure moved above the detach in runLocalCorridorGeometryUpdate); drags exempt via options.preEditSnapshot; dependent-free roads recut silently. Verified live both paths. WHAT REMAINS (all on-chain-gated or deliberately deferred): contract minting base ids + effect hashes; executed-formations log (trigger = on-chain auto-execution); §11 frames machinery (land assumed fixed, decision 4); formation-engine unification (§12.5, when it hurts); cross-plan conflicts at execution. Suite 1767 green. UNCOMMITTED along with yesterday's fingerprint-v2/fidelity work.
- 2026-08-03: THE DRILL PANEL — click resolves the whole vertical stack. `drill-stack.js` (pure, 12 tests) computes the chain at a point: content proposals → formed slices → the minting formation → base parcels; formations seat BETWEEN the ground they consume (maxParentDepth+0.5) and slices they mint (minChildDepth−0.5); dead derived slices dropped, consumed BASE parcels kept. `drill-ui.js` wires it: all three click funnels (parcel tail, structure fill, corridor hit) route through the drill — topmost claim selected (roads now included; the old funnel excluded them on purpose) — and a button column docks under the proposal card (rows top→ground, ↑ arrows between, goalLabels chips, selected marked; row click hops levels, parcel rows via openBaseParcel so consumed bases open too). Map hover highlights the topmost claim via highlightFeaturesForHover; row hover previews that level. Footprint hit-tests cached per proposal keyed by updatedAt|appliedAt|childCount. Killed two `#p-` literals: getProposalsForParcel ancestor projection and hasLiveReplacementSlice now strip on any '#' (new `#c-…` slices inherited nothing before). Verified live on Borovje: 5-level chain incl. street network road one hop under the recreation park. NOTE seen while verifying: re-visiting an already-applied plan link can leave DIFFERENT fabric (base 1791/30 live where a slice covered it before, slice ordinals shift) — pre-existing replay/persistence behaviour, not the drill; drill shows whatever is actually there.
- 2026-08-03: DRILL PANEL POLISH (same day). Drill joins the right dock properly: `right-dock-panel` width (400px shared var), shared `close-circle-btn` X, and drill-ui's positionPanel now computes the MIDDLE slot — top = card bottom + 10, max-height capped at parcel-panel top − 10, `.no-room` hides under an expanded card (ResizeObserver+MutationObserver on both neighbours). Row click focuses the object only when NOT fully in view (map.getBounds().contains(bounds) gate; openBaseParcel's unconditional fitBounds got the same gate). ROAD INTERIOR HOVER root cause: corridor hit targets set bubblingMouseEvents:false, so the map got no mousemove INSIDE a corridor and drill hover fired only on the border — fixed by forwarding mousemove from attachHitBehaviour to __drillUi.notifyHover, with showAppliedCorridorHover deferring entirely to the drill when present (drill draws the topmost claim, which may be an overlay on the road). Proposal-card title: too-long names marquee like text on a cylinder (applyProposalTitleMarquee + .title-marquee CSS: two copies, slide one copy-width+48px, seamless loop, hover pauses, prefers-reduced-motion keeps ellipsis, clientWidth 0 keeps ellipsis). All verified live: gaps [10,10] noOverlap, cameBack/stayedPut, 1 hover outline mid-corridor with road topmost, marquee 2 spans 16s.
- 2026-08-03: DRILL HOVER/SELECTION COHERENCE (3 user-reported issues). (1) Building selection was invisible: renderAppliedProposalHighlight passed includeBuildingGeometry:false (set in 48fee506, no recorded reason) so selecting a building highlighted only its parcels — flipped to true; the building footprint now outlines on selection app-wide. (2) Road click read as "segment selected": every in-road click lands on a topmost per-segment hit polygon whose rememberSegment painted the amber segment highlight — now renderSelectedCorridorSegmentHighlight paints ONLY while the corridor editor is open in segment scope for that exact proposal+segment (corridorEditorState read via try/catch — top-level `let` in a later script, TDZ), with refresh hooks at editor open/close/scope-toggle; plain click = road-only red outline; corridorLastClickedSegment stash (editor scoping) unchanged. (3) Hover stolen by a parcel under a building: parcel mouseover (highlightFeature) ran showProposalInfoHoverOverlay into the SAME hover group as the drill, last writer wins — highlightFeature/resetHighlight now defer to the drill when __drillUi.ownsHover() (mapWired && !interactionBlocked), and the drill is the single hover authority drawing the topmost claim whatever its kind (parcels included, labels only for base — slice labels would leak synthetic tokens); ProposalSelection.subscribe resets the memoised hover ref so selection wipes redraw on next move; corridor hover defers via the same ownsHover gate. All verified in sandbox: building outline on select=1, amber without editor=0/with editor=1/after close=0, topmost-at-building=the building with 1 outline.
- 2026-08-03: THE FLAT-RECORD INVARIANT + BOROVJE DIAGNOSIS (rethink §15 canonical). Clean replay measurements: reparcellization 29 parents→38 slices; 11 buildings and 6 parks each 1 parent → 0 children (content, not formations); road 3 declared parents → 32 children (13,991 m² corridor + 31 remainders) — so my earlier "Borovje roads consume 1:1, no cutting" was WRONG, corrected. ONE geometric offender in the whole plan: the road's footprint covers 3,733 m² (22%) OUTSIDE its declared ground — every "X sits on top of/below the road" oddity traces to it (parks fill their own plots exactly 100%/0 m² outside; buildings sit inside theirs). Roads are also blind to proposed buildings: pool = 123 GDI survey buildings, 0 proposed, so the street runs through M1-9 (25% of it) with demolishedBuildings:0. NEW RULE (Simun): max 3 levels base→one formation→content; temporal stacking stays legal, flatten at draw/mint, publish is the GATE. Flat≠single-parent (comasation = 1 level, N base parents). Id parsing CANNOT compute base ancestry — every derived id is minted against ONE root (rootParcelId), so Borovje's 38 slices are all named `1791/25#…` though 29 base parcels were consumed; use computeBaseAncestry/cadastreParcelIds. Roles must be BEHAVIOURAL (does it mint?) not goal-based: my first strict rule flagged 18/19; keying on minting leaves exactly 1 (the road). Formation is ADOPTIVE: if the ground already exists as a parcel matching the footprint, take it, don't re-cut. Parks split in two: structure park (forms/takes a whole parcel, ownership→public) SUPPORTED; urban-rule park (keep ownership, use restriction) deferred to urban rules. Built: frontend/js/proposals/formation-depth.js (pure, 15 tests) + scanner. Suite 1793.
- 2026-08-03: STEP 2 — PROPOSED FABRIC IS NEVER CUT (rethink §15 decision 3). New pure `content-occupation.js` (11 tests): geometric detection of applied proposals standing on ground a formation would take; MATERIAL only — (≥25 m² AND ≥5% of the victim) OR ≥50% swallowed — because blocking someone's proposal is heavy and sub-threshold slivers are a geometry-precision problem, not consent. Ground-authoring goals (reparcellization, decide-later) never count: a road crossing drawn plots is normal composition. Wired into `_applyRoadProposal` via `_detectContentOccupations` (uses corridorSurfaceFootprintForDefinition — the surface the cut consumes, NOT footprintOf, which reads larger: M1-9 is 25% of footprintOf but 6% of the real cut surface); refuses with named failure code 'content-occupied' + conflictTitles/conflictProposalIds, so the shared-plan conflict tour picks up Replace/Keep for free. Restores and the recut re-apply pass allowOccupation:true (the recut tour already disclosed). CRITICAL intra/cross split: the first cut blocked Borovje's own road on its siblings (18/19) — occupation protects OTHER people's proposals, so sharing-routes passes occupationExemptKeys = planMemberLocalIds() and intra-plan overlaps no longer block (19/19 restored); cross-plan still blocks (verified by re-applying without the exemption → refused naming Z1-5 6% + M1-9 6%). Local one-click: unapply-tour gained an 'occupied' variant (i18n ×4) + `resolveContentOccupation(key)`, hooked into applyProposalToMap's false branch — confirm un-applies the blockers and re-applies (verified: 2 items, road applied, both blockers unapplied), cancel touches nothing (verified). Suite 1805.
- 2026-08-03: PARCEL CONTIGUITY + DRILL BODY-VS-GROUND ORDERING (both user-reported on Borovje). (1) A parcel is ONE connected piece: new pure `parcel-contiguity.js` (10 tests) + enforcement inside `_assignSyntheticChildIdentitiesImpl` (the single choke point every mint path funnels through — splices the array in place so all 4 call sites are covered). Any MultiPolygon child is exploded into one parcel per component, parent identity stripped from the pieces (two parcels sharing an id was the bug being prevented), sub-1 m² slivers dropped but never all of them (largest survives). Borovje had exactly ONE offender: the road's corridor parcel `#upu-borovje-ulice-1`, a 2-part 13,991 m² MultiPolygon → now 33 road children instead of 32, 0 non-contiguous parcels on the map. (2) Hover flip-flopped along one road (road here, a parcel there): my drill seated EVERY formation below the parcels it minted, which is right only for ground-authoring goals (reparcellization, decide-later — their slices are ground for others) and wrong for a road/park/building, whose minted parcel IS its body. drill-stack now seats body formations at minMintedDepth+0.5 and ground-authoring at −0.5; verified 8/8 sample points along the corridor now report the road. FRAMING (Simun): Borovje was imported haphazardly and may contain illegal elements — do NOT bend the system to make it valid; reject or fix the invalid parts. Still-invalid in Borovje: the road's 3,733 m² (22%) of undeclared ground (step 4).
- 2026-08-03: LOCAL road_parcel_classification MV REFRESH IS BLOCKED by invalid source geometry — NOT fixed, needs a decision. `REFRESH MATERIALIZED VIEW road_parcel_classification` fails with `GEOSIntersects: TopologyException: side location conflict at 430866.48 4850764.27` (also serially, so it is not a parallel-plan artifact). Cause: `parcel` holds 4,455 invalid (self-intersecting-ring) geometries across 1,274 KOs countrywide, and the MV joins ST_Intersects(p.geom, …) directly; the parcel at the failure point is in KO 330043/330272 — NOT Zagreb, so a bad parcel in another city kills the whole countrywide refresh. dgu_road_usage and osm_road are 100% valid. THREE options, all needing Simun: (a) UPDATE parcel SET geom=ST_MakeValid(geom) on the 4,455 — fastest, but mutates canonical countrywide SOURCE geometry, diverges local from prod, and changes geom_hash on a VERSIONED table (next ingest may see phantom changes); (b) wrap the parcel geom in ST_MakeValid inside cadastre-data's db/road_parcel_classification.sql — cross-repo canonical DDL change, and their own fix-invalid-gdi-building-geom.sql measured a runtime guard at ~6× (that doc's principle: repair data once, never guard the query, and never alter source data — they repaired a DERIVED column); (c) copy the MV from prod via sync-tables. Cursor unification shipped separately in the same turn.
- 2026-08-03: ONE CURSOR ON THE MAP. Measured, not guessed: all 204 interactive paths already computed `pointer`; every `grab` came from draggable `.road-node-handle` markers plus Leaflet's `.leaflet-grab` container default showing through wherever no path sat under the cursor. Since the drill resolves a claim at EVERY point (background clicks included), map.css now sets `#map.leaflet-grab` and every `.leaflet-interactive` (excluding `.road-node-handle`/`.leaflet-marker-draggable`) to `pointer`, with `.leaflet-dragging` → `grabbing`. Verified by grid-sampling the viewport: 311 sample points pointer, the only 5 `grab` are the node handles — a genuinely different affordance.
- 2026-08-03: REPARCELLIZATION EDITOR — CLOSING NO LONGER SAVES. Root cause of "I clicked X and it still applied what I did": closeModal() called persistResult() unless skipPersist, i.e. every dismissal was a SAVE ("closing is non-destructive" was the old intent). Now only Done persists; X, the new explicit Cancel button and Esc all route through cancelModal() → discard, with a confirm when dirty (isDirty compares a layoutSignature — slice geometry + ownerKey + per-owner shares — against a baseline snapshotted AFTER the opening layout renders, so open+close is never "dirty"). TWO bugs found by verifying rather than assuming: (a) the signature read `s.feature.geometry` but hydrated slices carry `.geometry` directly, so nothing ever looked dirty; (b) showStyledConfirm takes (message, {okText, cancelText}) with NO title option — I passed `confirmText`/`title`, so the labels silently vanished and the dialog resolved false. Verified live: clean close → no prompt, no write; All-public then Cancel → prompt with [Keep editing][Discard changes], Keep keeps it open, Discard closes and writes nothing. Action row restyled: All public shrunk to 68px on the left (was a 240px slab), spacer, Cancel+Done paired at 120px on the right; phones wrap. i18n ×4.
- 2026-08-03: EDIT GEOMETRY ON AN IMPORTED REPARCELLIZATION. Borovje's plan carries `algorithm: "upu-plan"` — a key the editor has no option for, so the chooser opened with NOTHING selected and no editable mode ("it opens no choice"). openReparcellizationModal now normalises an UNKNOWN saved algorithm to 'manual' when seeded polygons exist (known keys keep their mode so a sweep-line plan can still be re-cut from its handle). NOT verified live: the editor requires a live parcel SELECTION and Borovje's 29 source parcels are consumed+hidden under the applied plan, so getCurrentParcelSelectionContext returns 29 ids but 0 layers and validateSelection refuses — meaning "Edit geometry" on an APPLIED reparcellization cannot build its pool at all. That is a second, separate defect still open.
- 2026-08-03: REPARCELLIZATION IS A GEOMETRICAL FACT (Simun). openReparcellizationModal no longer needs a live parcel selection: when reopened on a saved plan it builds the pool by unioning the plan's OWN polygons (buildSuperParcelFromPlan) and takes ownership from the parent layers resolved out of parcelLayerById — hidden-but-indexed, since consumed parents are never deleted. Verified live on Borovje's APPLIED plan with NO selection at all: opens, algorithm chooser shows 'manual' (the unknown 'upu-plan' key normalises), 38 plots drawn, owners resolve, no error status. Before this it was impossible — getCurrentParcelSelectionContext returned 29 ids / 0 layers and validateSelection refused.
- 2026-08-03: THE EDITOR WAS A LIVE WRITER, NOT A WORKSPACE — the deeper half of "X still applied my changes". updateCommitState() called persistResult() on EVERY state change (every drawn plot, every owner assignment), so by the time the user hit X the edits were long since written and Cancel had nothing to discard. Removed; only Done persists now. Verified: All-public then Cancel leaves the draft byte-identical (noWriteWhileEditing + cancelLeftDraftUntouched both true). CAVEAT: the Done→saves path is NOT verified in the sandbox — driving the modal directly gives no active design draft, so persistResult writes nothing there and the probe cannot tell saved from not-saved; persistResult itself is unchanged. Consequence of the change: a plan no longer reaches the draft (or the card preview) until Done is pressed.
- 2026-08-03: EDITING A LAND READJUSTMENT DISPLACES WHAT STANDS ON THE PLOTS IT CHANGES. New pure `reparcel-edit-impact.js` (13 tests): changedPlotIndices compares saved plan vs current slices by shape (area within 1%/1 m² AND near-total overlap, so reordering and vertex noise are not changes), childIdsForPlots maps changed plot indices to minted child ids, proposalsOnPlots finds applied proposals declaring those children (top-level or typology parents, self excluded). Wired into the Done handler: affected proposals open the unapply tour in a new 'reform' variant (i18n ×4) — "Saving this layout changes the shape of plots that other proposals already stand on… plots you did not change keep theirs" — confirm un-applies them then saves, cancel refuses the save. Scoped to CHANGED plots only, per Simun. NOT verified live end-to-end: the gate no-ops without an active design draft (editedSourceProposal returns null), which is also why it cannot misfire. Suite 1829.
- 2026-08-03: INVALID PARCELS WERE HOLES ENCODED AS FOLDED RINGS — and the MV had a SECOND bad operand. Simun looked at the "broken" parcels and saw enclosed inner parcels (legal), not folded shapes; he was right. The shape is a legal donut; the ENCODING is invalid: DGU walks the outer ring INTO the hole, around it and back out along the same line (zero-width channel), so the ring touches itself. Proven across all 4,455 current invalid rows: area preserved (<0.5 m²) 4455/4455, gained a ring 4455/4455, valid after 4455/4455, zero GeometryCollections — the repair is lossless, it just promotes the hole to a proper interior ring. geom_hash is a PLAIN stored column written at ingest from the SOURCE, so repairing geom does not disturb versioning (next ingest compares source-hash to stored-hash, sees no change). Repair committed as cadastre-data/db/fix-invalid-parcel-geom.sql (idempotent, backs up to parcel_invalid_geom_backup, refuses to commit on any area drift or leftover invalid): UPDATE 4469 (incl. 14 superseded versions), invalid_now=0, max_area_drift=0.0000. THREE bugs while writing it, all caught by the guards: PK collided because now() is the TRANSACTION timestamp and 14 cestica_ids have several versions (→ surrogate key); parcel has NO updated_at column; osm_road.geom_buffered is typed Polygon not MultiPolygon (→ no ST_Multi, plus a guard refusing any buffer that repairs into several polygons rather than truncating). THE REAL MV KILLER was not the parcels: after repairing all of them the refresh still died at the identical coordinate, because the MV intersects `osm_road.geom_buffered` — a DERIVED corridor polygon — and exactly ONE of 371,161 was invalid (osm_id -13816872001, sjeverna-dalmacija, pedestrian, self-intersection 0.0 m from the reported failure point, area 974.07 unchanged). Checking osm_road.geom and calling the table clean was my miss; geom_buffered_3765 is GENERATED so repairing geom_buffered fixes both.
- 2026-08-03: THE REPARCELLIZATION EDITOR DOES SHOW ITS 38 PLOTS — via the real adapter path. Verified with getProposalEditorAdapter('reparcellization').draftFromProposal(p) → openDesignEditor(draft): subtitle "Manual · 29 parcels · 29 owners", manual selected, 39 new-plot rows carrying the real UPU names (Prometna površina IS-1, Rekreacija R2-1…). Simun's screenshot showed ONE unassigned plot of 87,677 m² instead — the blank-slate manual fallback, i.e. initialPolygons arrived empty. Most likely his LOCAL copy of the proposal was damaged by the auto-persist bug before it was removed (updateCommitState persisted on every state change, so opening the editor once and letting it fall back to a single plot would overwrite the stored 38-polygon plan). Remedy for him: wipe local data and reload the plan link, then retry Edit geometry.
- 2026-08-03: PLOT HOVER/SELECT + NODE-AND-EDGE EDITING IN THE REPARCELLIZATION EDITOR. (1) The "bbox that flashes around the clicked part" was the BROWSER'S FOCUS RING: on an SVG <path> it is drawn around the bounding box, and the editor had no hover/selected styling of its own at all. Added real boundary feedback in drawPreview (plotStyle idle/hover/selected — blue, thicker, brighter fill; hovered plot brought to front; selected plot keeps its style through redraws) and killed the ring with `#reparcel-map path:focus { outline: none }`. (2) Node/edge editing: new pure `plot-topology.js` (13 tests) reads the plot list as a planar subdivision — buildTopology snaps near-identical vertices (1e-7) into shared nodes and records which plots each node/edge belongs to; moveNode rewrites EVERY referencing ring (so a shared corner moves both neighbours and no gap opens, ring closure maintained); insertNodeOnEdge adds a vertex to every plot sharing that edge; removeNode refuses when any ring would fall below a triangle; edgeMidpoints gives the insertion targets. UI: a ✳ tool button toggles handles — blue dots for private corners, larger amber for SHARED ones, faint dashed dots at edge midpoints to insert; drag previews live (re-cut from the original topology each frame so intermediates never accumulate), dragend commits, Alt-click removes; every commit recomputes plot areas/percentages, redraws, and rebuilds the handles from the new topology. Verified live on Borovje's 38-plot plan: 885 node handles (324 shared) + 931 edge handles; clicking a shared edge handle inserted exactly one node (885→886, edges 931→932); simulating a real drag on a shared node changed exactly 2 plot outlines — the two that share it. Suite 1842.
- 2026-08-03: roads.md UPDATED for proposals-rework + the repair. It is the EXISTING-roads vocabulary, so proposals-rework does not rewrite it — but three things had drifted. (1) Vocabulary collision, the real one: "road parcel" now means two different objects across the repos. §1 gains a table separating the CLASSIFIED road parcel (read from cadastre, `HR-<ko>-<broj>`, any shape incl. mixed-use) from the CORRIDOR parcel a road proposal MINTS (`<base>#<proposalId>-N`, local bookkeeping, exactly one connected piece), plus the two proposal-side rules that constrain what may happen to this land: cuts only declared ground (enforcement pending), and proposed objects are never cut in half (unapply) while SURVEYED buildings keep cut/tunnel/demolish. (2) Stale counts: road_parcel_classification 28,641 → 112,634 (30,894 road, countrywide); osm_road 239,504 → 371,161 current, in both §1 and the §2 table. (3) New known problem #4 recording that one invalid geometry anywhere aborts the whole MV refresh, that the parcels are legal holes written as folded outer rings (lossless repair), and that the MV intersects `osm_road.geom_buffered` NOT `geom` — the trap that cost a session — pointing at db/fix-invalid-parcel-geom.sql and saying to re-run it after any ingest.
- 2026-08-03: EDITOR — UNDO, NODE POPUP, DEEPER ZOOM, ONE MANUAL MODE. (1) Cmd/Ctrl+Z undo: a 50-deep history of {slices, ownerShares, selectedSliceIndex} snapshots plus a ↶ toolbar button. applyGeometries OWNS the snapshot — it decides first whether anything actually moved and only then pushes, because the first cut pushed unconditionally and produced undo steps that did nothing (caught by verifying, not by reading). Ownership actions (All public, shuffle) push their own; failed draws call discardLastHistory() so a rejected shape leaves no phantom step. Shortcut ignores INPUT/TEXTAREA/contentEditable; Shift+Cmd+Z is a deliberate no-op (no redo). Verified live: edit → button enables → Cmd+Z restores the exact path set → button disables, status "Undid the last change." (2) Node click now opens a POPUP ("Corner of one plot" / "Shared corner · N plots" + what dragging does + Remove corner); Alt-click stays as the shortcut. Verified: popup content correct, Remove took 885→884 handles, Cmd+Z restored 885. (3) Editor map zoom to 23 (maxNativeZoom 19, so tiles upscale while geometry stays crisp), zoomSnap 0.5. (4) Manual IS the node/edge system: switching to manual used to `startDraw('polygon')` immediately — that is why boundary editing looked unreachable and drawing looked like the only manual way. Now manual opens with handles on and nothing armed (verified: 885 handles, draw aria-pressed=false, nodes aria-pressed=true); draw-plot and split-with-line are tools that hide the handles only while a shape is in progress and restore them in cancelDraw, sharing the same undo stack. NOT verified: the node DRAG path under synthetic events — an earlier run moved exactly 2 plot outlines for a shared node, a later run left Leaflet mid-drag (the `dragging` class stuck, dragend never fired), so the harness is unreliable here; the pure moveNode is covered by 13 unit tests but the drag glue wants a human check. Buildings editor undo NOT done (separate editor, not touched). Suite 1842.
- 2026-08-03: GEOMETRY EDITING UNIFIED — one history, one handle layer, one topology, one look. Inventory first (there were more than the three suspected): reparcellization plots, blockify manual, freeform single building, row house, road/tram centreline nodes, park/square furniture, blockify gap/wing handles, sweep-orientation handle, reparcellization's own draw-tool vertices. Two shared systems already existed and each had solved a different third: `polygon-geometry-editor.js` (3 building hosts) had insert/delete + boundary SNAPPING but no undo and no shared vertices; the new plot-topology had shared vertices + undo but no snapping; `road-node-edit.js` had its own coincident-vertex bucketing (toFixed(7)) + snap-on-drop. NEW: `js/geometry-edit/history.js` (13 tests) — one undo stack with capture/restore injection, depth limit, discardLast for actions that fail after recording, isEqual guard, bindKeyboard (Cmd/Ctrl+Z, ignores INPUT/TEXTAREA/contentEditable, swallows Shift+Cmd+Z since nothing has redo, optional enabled() gate) and bindButton (drives disabled state). `js/geometry-edit/handles.js` — the render/drag/preview/commit loop, driven by a TOPOLOGY so a shared vertex is one handle; hosts supply onMove/onInsert/onRemove/onPreview/onNodeClick/snap. plot-topology generalised to OPEN shapes (LineString/MultiLineString + shapesToPlots/plotsToShapes) so road centrelines use the same node identity — 6 new tests incl. a junction shared by two legs. MIGRATED: reparcellization → shared handles + shared history (verified live: 885 vertices/324 shared/931 midpoints all `.geom-handle*`, insert +1, Cmd+Z restores exact paths, button re-disables); polygon-geometry-editor → shared history, so blockify + single building + row house gain undo (verified: ring 4→3 on remove, 4 after undo) PLUS an automatic `.geom-undo-button` in the map corner when the host supplies none (verified: renders, enables, undoes, disappears on destroy); road-node-edit → shared history hooked at `mutateGeometry`, the one funnel for drag/delete/bulldoze, keyed per proposal, keyboard gated to when handles are up, and its node identity now uses the shared tolerance instead of its own toFixed(7); structure furniture editor → shared history. CSS: one `.geom-handle` / `--vertex` / `--shared` / `--mid` / `.is-dragging` set, old per-editor classes kept as aliases. TWO BUGS caught by verifying: `pane: undefined` passed to L.marker OVERRIDES Leaflet's default pane (setOptions copies the key) so every marker failed to attach — only set pane when asked for; and a python block-replace silently ate nodeEditingAvailable(). NOT migrated (justified standalone): polygon-geometry-editor keeps its own handle rendering (delete-marker + boundary constraint + selection are real behaviour, 3 consumers, high risk to rewrite); road-node-edit keeps its junction/track/bulldoze rendering. Suite 1893.
- 2026-08-04: THREE EDITOR FIXES + THE MAP EDIT LOCK. (1) "corner" was the wrong word and mine — the road editor had already settled on node/čvor/nodo, so the 5 reparcellization strings were retranslated in en/hr/sr/es (hr *vrh* and sr *teme* → *čvor*). (2) DONE STAYED DISABLED ON AN IMPORTED PLAN, and not because of node removal — it was disabled from the moment the plan opened. A plot carries its owner TWICE: the singular ownerKey/displayName and an owners[] array for split shares. The editor keeps both in step, but the Borovje UPU was written by the import script, which set only the singular pair (verified: all 38 polygons have ownerKey, none have owners[]). hydrateSlicesFromPolygons read only the array, so every plot came back ownerless → "38 plot(s) unassigned · 0.0% covered" → commit gate never satisfiable. Fixed by normalising both shapes in ONE place: normalizePlotOwners/plotIsAssigned in the pure reparcellization-ui-state module, used by both the hydrator and evaluatePlanCompleteness (6 tests, one built from the verbatim stored polygon shape). Verified live: "All pooled land assigned (100.0%)", Done enabled, still enabled after removing a node (885→884 handles). (3) "Edit geometry does nothing" on a building — NOT REPRODUCED from a clean load (drove it minimised, from a map click with the drill panel up, synthetic and real hit-tested clicks; it opened every time). What was real: editProposalGeometry had five exits with no visible result, and the button is an inline onclick, so the promise is discarded and anything thrown inside (createDraftFromProposal throws on a proposal with no stable ID) became an unhandled rejection — a dead click with nothing anywhere. Every path now reports its reason via showStyledAlert (+2 i18n keys); verified a bad key raises "Proposal source not found." while the real edit still opens. If it recurs it will now name the gate.
- 2026-08-04: MAP EDIT LOCK — an editing mode owns the map (`js/map-edit-lock.js`, 14 tests). The park editor's placement clicks did nothing because the park's OWN fill layer binds a click that calls L.DomEvent.stop() and selects its proposal, so the click never reached the editor's map handler (removal kept working — decoration markers carry their own handlers). So the placement bug and "editing should freeze other interactions" were one bug. claim(owner)/release(owner) is exclusive (a second owner is REFUSED, not swapped), sets body.map-edit-locked, and blocksSelection() also folds in the guard list that had been copy-pasted into four files (measure/road-drawing/cadastre-view/browse/parcel-draw/area-paint). Consumers: structure fill lets clicks pass UNTOUCHED while held (the actual fix), corridors re-fire theirs to the map (their hit targets keep bubblingMouseEvents off), parcels/transit/drill-ui/proposal selection decline; CSS dims and de-activates the selection panels; the editor now also hides the drill panel on open. Structure editor claims on open and releases in closeEditor BEFORE its own re-select. Proven by A/B in the browser: with isHeld() stubbed false a click on the park fill places nothing, with the fix it places (6→7→8 icons). NOT locked: road node editing (ambient, not modal — selecting something else is how you leave); building editors are already exclusive in their own modal maps; square shares the park code path.
- 2026-08-04: DECIDED MODEL FOR REPARCELLIZATION EDITING (not yet built). The pool boundary and the cuts are different kinds of thing and the editor currently treats them as one — every vertex of every output plot gets a free handle, so dragging a vertex on the OUTER boundary of the union takes or gives land outside the plan (a boundary agreement with a neighbour, not a readjustment). Invariants: area is conserved (outputs tile the pool exactly) and the pool boundary is changed by re-selecting INPUTS, never by dragging. So the only degrees of freedom are the internal cut graph, and outputs are the FACES of that arrangement — derived, never edited directly (today a shared border is stored twice, once per ring, and any drift is a sliver). Refinement: only the OUTER boundary of the union is off-limits; an output border coinciding with a former INTERNAL parcel line is free to move, since that line dissolved when the parcels were pooled. Three node classes: interior = free; where a cut LANDS on the pool boundary = slides along it (that is the split ratio, a real design choice); inherited pool-outline corner = locked. Agreed next step is the classification (contained, no data-model change, and polygon-geometry-editor already has the machinery in snapCoordinateToBoundary/constrainRingToBoundary); the full arrangement model is the destination, and its real cost is IDENTITY — once faces are derived, which face is "M1-2", its owner and its cash offer must be re-matched after every edit instead of travelling with the polygon object.
- 2026-08-05: FLAT FORMATION MODEL + PARTITION-EDIT ENGINE (rethink-proposals.md §15a). Terminology decided: land readjustment ≡ reparcellation = a bundle of FORMATIONS (the atom: one output parcel formed against current fabric); plots of a readjustment are independently formable at apply time (joint only for consent/execution). Fabric invariant: `cadastral parcel(s) → one formation → content`, flattened along the way. Built for the road edit path: `proposals/formation-edit.js` (pure — matchPieces two-tier identity carry-over, footprintDelta, retainedUnloadedParents) wired into `_assignSyntheticChildIdentitiesImpl` (consumes `__carryIdentity`, stamps `baseParcelIds`), `_buildChildFeaturesFromDefinition` (matching + allocator seeding past prior indices + `cadastreParcelIds` at build time + corridor multi-root anchor), `apply/road.js` (`options.priorChildren`), `road-drawing.js` (prior-children capture pre-unapply, delta-scoped recut disclosure, self-ghost keptUnloaded fix). Existing stored ghosts (plan 97-104) predate this — new edits stop minting more; structures/reparcel still to funnel through the same engine.
- 2026-08-05 (second pass): PARTITION-EDIT ENGINE THROUGHOUT. Reparcellization in-place edits carry plot identities (editor-shell captures priors → _applyReparcellizationProposal matches/seeds; per-plot baseParcelIds via overlappingBaseIds — multi-parent plots anchor to every base they cover); road merge-on-connect captures absorbed roads' children pre-unapply (swallowed ground keeps names); split-on-disconnect passes footprint-filtered priors per component with sequential consumption (no id on two proposals); buildings/structures stamp cadastreParcelIds from parents' base ids at apply. Suite 2007. Still deferred: structures forming their own parcel (invariant #4 rewrite), legacy no-polygon road path, read-side baseParcelIds consumers, re-anchoring old stored records.
- 2026-08-05 (third pass): ERRORS OVER HEALING — records migrated flat, healers removed. migrate-flat-records.js (dry-run default) flattened 146/342 local rows' parent declarations to base ids (consent fields untouched, idempotent, verified zero derived ids left); backfill-cadastre-parcel-ids --force restamped 300 rows. PROD NOT RUN (command: ssh, then from /opt backend: node scripts/migrate-flat-records.js --apply + backfill --apply --force). resolveParentsByGeometry promoted to FIRST-PASS in _applyRoadProposal/_applyReparcellizationProposal (≥95% coverage, keeps unloaded declared parents) — formations consume live fabric, declarations are consent anchors. Removed: both share routes' ghost re-parent retry ladders, payload 8-pass requeue (now one A6 pass + intra-payload occupancy exemption), plan-route intra-plan conflict retry + mixed/derived waiting ladder + catch-path requeue, decide-later PersistentStorage scan. Publish gate: formation-depth.preparePublishRecord wired into buildUploadReadyProposal — non-flat records refuse with named violations. Old ?proposalShare= ghost payloads now fail loudly by design. Suite 2016.
- 2026-08-05 (fourth pass): STRUCTURES FORM — invariant #4 rewritten for park/square/lake. wholeParcelTakePlan (formation-edit.js) + _formStructureParcel (apply/structures.js): ADOPT the one matching parcel (ownership snapshot → City, nothing cut/minted) or MERGE-TAKE a union of whole parcels into one minted parcel (baseParcelIds = all bases, no cuts possible → §2.1 hole guarantee intact); PARTIAL coverage refuses loudly naming offenders ("cut the ground first with a road or a land readjustment", code structure-partial-parcels). Station forms nothing. Unapply restores ownership snapshots / consumed parents + city agent index re-derives. Candidates from live fabric geometrically. Suite 2024.
- 2026-08-05 (fifth pass): BUILDINGS FORM + PALETTE. Freeform building (goal single, one footprint) forms its parcel at apply (_formBuildingParcel): default FOOTPRINT parcel (mint from footprint, ownership → proposer agent, host remainders cut back to their owners, funnel splits multipart pieces, refuse off-fabric footprints); option buildingProposal.takeWholeParcels (adopt/merge whole hosts — merge unions HOST ground) chosen via "Building parcel" select in the freeform modal, carried through drafts/edit-reopen (adapter context.takeWholeParcels). Shared unapply reversal _reverseFormationRecord (structures+buildings; agent lists re-derived). Build palette regrouped per taxonomy: Land readjustment ungrouped first, Urban rules fieldset (Block/Row/Detached), Structures fieldset (Freeform Building/Park/Square/Lake) with legends like Transport; "Freeform"→"Freeform Building" (hr/sr Slobodna zgrada, es Edificio libre). Suite 2029.
- 2026-08-05 (sixth pass): EXEMPTION AT THE GATE + DRILL MULTI-PARCEL ROW. First Cibona replay exposed that occupationExemptKeys never reached _resolveParentAvailabilityOrDefer (only the road-content check) — the removed intra-plan retry had been load-bearing (3 members parked overlapped, park refusal cascaded from Road 2043's failure). Fixed: the gate drops conflicts whose occupier ∈ occupationExemptKeys first-pass (§3.3 as a rule, all apply types). Honest residual: parks/squares whose plots were shaped by server-dead ghost generations may still refuse if in-plan formations don't recreate matching ground. Drill "At this spot": base row for multi-base formed parcels = "Cadastral parcel (N)" chip + horizontally scrollable clickable parcel-number strip (baseGroupIdsFor/renderBaseGroupRow off baseParcelIds; clicked parcel first; i18n ×4). Suite 2029.
- 2026-08-05 (seventh pass): NAMED-ID DIAGNOSIS → WRITE-BACK BUG. Road 2043's newly-named failure id (823/1#c-w3dzvsn5qx0w-1 — Road 2045's slice) exposed that the apply-layer derivation PERSISTED its per-attempt working set into the stored record (proposalData/roadProposal.parentParcelIds), so a later attempt failed on its own stale generational reference. Fixed: working set stays attempt-local (road.js already writes record parents mid-flow from RESOLVED live features; parcels.js tail write stands); consumed declared parents (isParcelReplacedByChildren) filtered out of the cut set — never cut a hidden feature under the live generation. dependency-missing messages now NAME the parcels (invariant #6). Shared Plan Result got a Copy button (showSimpleShareModal actions gained per-action keepOpen; plan.copyResult/copiedResult i18n ×4 under modal.roadWidth.share.plan). Suite 2029.
- 2026-08-05 (eighth pass): GEOMETRY MADE AUTHORITATIVE FOR FORMATION GROUND — answering "why is this road not just geometrically applied?". Coverage-pass (≥95%): working set = resolution.ids EXACTLY, nothing declared re-added (the old keep-unloaded merge let stale ids ride along). Coverage-decline: declared DERIVED ids are hints, never prerequisites — with no live layer they can't be fetched into existence, so they're dropped with a warn (base ids stay, they fetch). Combined with consumed-filter + success-only write-back, a declared id can no longer block a formation as a phantom "prerequisite parcel". Note: lived-in browsers carry multi-generation leftover fabric from the buggy runs — one clean-state replay is the true verdict; normal browsers self-correct as records re-apply.
- 2026-08-05 (ninth pass): DEAD-GENERATION CHILD SNAPSHOTS MIGRATED OUT (per Simun: "one-off migration, do not patch and branch infinitely in the apply code"). Road 2043's record carried its July capture's childParcelIds minted under the dead predecessor token p-2g0teu3onpu (in descendant_parcel_ids + road_proposal + proposal_data); the v1→v2 fingerprint rename (p-→c-) had likewise orphaned 40 rows' own children into foreign tokens. migrate-flat-records.js gained dropForeignChildIds (child ids whose #token ≠ own proposal_id token are a dead generation's snapshot — children are derived data, apply regenerates): 40 rows cleaned locally, idempotent, verified zero residue. syncCanonicalSharedProposalState already refreshes local records' parents/children from server payloads, so lived-in browsers heal on next link-open (empty canonical lists are skipped by sync — a wipe/incognito run is the clean verdict). PROD still not migrated.
- 2026-08-05 (tenth pass): THE ACTUAL ROAD-2043 BUG, from incognito console. A6 orders Road 2045 BEFORE 2043 (constraint graph, not creation order); at 2043's slot my derivation swapped declaredParentIds to the live set (incl. 2045's minutes-old slice) but parentFeatures still held step-1's resolution of the DECLARED 8 bases — the gate reads declared ids against resolved features, so the live slice counted as "absent" → phantom "prerequisite parcels unavailable". Fix: after derivation/filters, parentFeatures re-resolved for the working ids (features follow the ids). Park 2047 / Square 2049 were cascades of 2043's failure. Suite 2032 (one flaky thumbnail blip, passes on re-run).
- 2026-08-05 (final pass): CIBONA 8/8, BOROVJE 19/19 — verified by driving the browser (agent-browser, fresh profile per run, ?reduceMotion=1; city-switch gate needs the "Open in Zagreb" click). The browser loop found three real defects: (1) features-follow-ids — derivation swapped declared ids but the gate compared them against step-1's stale features, so a sibling's minutes-old slice read as "absent"; (2) derive-or-refuse — when derivation declined, the consumed-filter left formations consuming NOTHING and minting on top of live pieces (double fabric; Cibona square's 57/45/84% offenders); now coverage<95% with a resolver present = loud refusal (formation-ground-unresolved); (3) resolver liveness — loadedLiveParcels' #-prefix inference can't see cross-token consumption (2048 consuming 2045/2043 slices), so hidden pieces resurfaced as candidates; LIVE now additionally requires parcelLayer.hasLayer, same test as the gate. One-off Cibona data migrations: 2048's plots clipped by both road footprints (they double-claimed the corridor strip), square 2049 rebuilt deterministically (clipped plot2 ∪ whole bases 823/5,6,7 — PostGIS; also had to overwrite the raw legacy proposal_data.geometry which import prefers), park 2047 snapped to its take-set (+4%). INCIDENT: careless `git checkout` destroyed road.js's uncommitted day-work mid-session; reconstructed from session context (priorChildren threading + derive-or-refuse block) and re-verified by the 8/8+19/19 runs. Suite 2032. Prod migration still pending; Simun's main profile needs local-data wipe.
- 2026-08-05 (sliver endgame): THE CIBONA SLIVER WAS AN ORPHANED-POOL HOLE. Browser probe (transect point-in-polygon over live layers) found a genuine GAP strip: Subdivide 2048's geometric pool consumed more ground than its authored plots tile, and _applyReparcellizationProposal minted ONLY plan.polygons — consumed, unminted, hole; hover fell to the same-owner highlight of the hidden ancestor. §14.2 completed on the reparcel path: after minting plots, pool−plots leftover is minted back per-parent as remainder parcels (cloned from parent → owner keeps; ≥0.5m²; funnel explodes parts). Side effect: Square 2049 stopped silently adopting one plot and now MERGES (plot + remainder). Verified fresh-profile: Cibona 8/8 clean, transect 51 pts 0 gaps; Borovje 19/19 clean. Note: hovering a sliver now highlights every SAME-OWNER parcel (by-design ownership-group flood) — not the hidden-parent bug.
- 2026-08-05 (6804/5 case): CONSERVATION GUARD ON ROAD PARENT-HIDING. The road clipped ~1 m² off 38 m² base 6804/5, minted no remainder (whichever micro-branch dropped it), yet hid+consumption-marked the parent → orphaned ground; in older fabric it stayed visible-but-marked (hover fine, click re-selected the road = looked dead). Fix in apply/road.js: (1) builder reports skipped-as-uncut parents via buildOptions.uncutParentIds → excluded from parentsToRemove + descendant marks; (2) conservation guard — a parent in the removal set with NO remainder child may be hidden only if ≥98% under the corridor polygon; unknown/unresolvable geometry = conservative DON'T hide (the first guard version silently returned on missing feature and still hid). Verified fresh-profile: Cibona 8/8 clean + 6804/5 live + openBaseParcel click opens the panel; Borovje 19/19 clean. Also learned: hover parcel-number labels = the proposal-hover overlay (showLabels, #FFB300) — they appear only on parcels belonging to a non-executed proposal while Proposals UI is on; plain parcels get style-only hover; permanent labels via the "Show parcel numbers" checkbox. Latent display wart: minted children's BROJ_CESTICE uses number-root with subdivision stripped ("6804#c-…" for a 6804/1 child) so drill labels collide across siblings ("6804 #1" ambiguity) — ids are correct, labels only.
- 2026-08-05 (reload + road-edit, §15a items 16-17): RELOAD REPLAYED 4/8 EVERY TIME because boot restore is snapshot-faithful (consumed children deliberately not re-added) while the presence check demanded every stored child id in the registry; each needless re-apply ran against STANDING later members with the intra-plan exemption on (road consumed the park's parcel; write-back recorded the inversion). Fixed: presence = registry OR saved-feature descendant-marker witness OR adopt formation.parcelIds (isProposalAppliedAndMaterialized, single predicate both routes); restore barrier unconditional when anything is applied (unbarriered, the same reload skipped 8 or re-applied 6 by race); canonical sync never rewrites an APPLIED record's parent/child lists; loadedLiveParcels' consumed-shadow from ON-MAP ids only (dead slices vetoed their restored bases — 21% coverage refusals); exemption scoped to under-construction members (standing-member overlap → conflict tour); queue-excluded members count in "Skipped". ROAD EDIT past a formed park died (formation-ground-unresolved 55%): unapply can't restore parents the park lives on → hole. Fixed in the edit path: re-show priors whose parent didn't return (wasVisible/parentParcelId captured; consumed priors stay hidden); a carried id is never hidden/marked/recorded as parent (one apply hid its own corridor strip); record parents = truly-consumed only (spared parents recorded as consumed got boot-hidden — the park vanished); consumed priors are not carry candidates (frozen names — carrying one onto new ground made it vanish every reload) but still seed the allocator. Verified fresh-profile end-to-end ×2: apply 8/8 → reloads skip 8 (no modal or Skipped 8) → extend-through-8-buildings cut-all → applied with extension → reload: all skipped, corridor live. Suite 2040.
- 2026-08-05 (drill cap, §15a item 18): "CLICKING THE ROAD SELECTS A SUBDIVISION" — a pre-item-16-degraded profile's Subdivide 2048 claims road ground (corridor slice in parents + footprint over it), and drill-stack ranked it above the road at a corridor point (parent-depth tie → newer-first). Claims-model rule added in drill-stack.js: a ground-authoring formation (reparcellization/decide-later) that did NOT mint the live parcel at the point never places above live fabric minted by a different proposal (foreignLiveDepth cap in the else-branch); base ground unaffected (readjustment still above its pool parcels). Clean records unchanged (verified in-page); degraded shape locked by 2 new unit tests. KNOWN RESIDUAL (measured, clean fabric, post-extend edit): re-shown priors carry pre-readjustment geometry → a minted remainder can overlap a later formation's small plot (48 m² under 2048's 49 m² plot); fix = §15a-19 (below). Suite 2042.
- 2026-08-05 (residual restore, §15a item 19): "THE SLIVER IS GONE" — extending the road buried 2048's 213 m² plot under a 3,610 m² road remainder. Traced on clean fabric: the edit's unapply RESTORED road 2045's long-dead slice (823/1#c-w3dzvsn5qx0w-1) at full stale geometry — its direct children were dead so the structural `<parent>#` restorable test passed, but its GRANDCHILDREN (2048's plots, across a dead intermediate generation) were live on that ground; the recut baked the stale geometry into a 3,984 m² remainder over five live plots. Structural prefix tests can't see cross-token consumption → geometric fix: formation-edit.residualGround (pure, 4 tests) = parcel ground minus live fabric; _filterRestorableParents restores parents CLIPPED to it (refuse at ~full coverage; keep geometry verbatim under 1% so micro-slivers don't churn); road-drawing's prior re-show applies the same rule; new _liveGroundEntriesNear helper (registry + hasLayer + bounds prefilter) feeds both. Verified fresh-profile: same extend-edit → ZERO >2 m² road-child×plot overlaps (was ~3,980 m² across 5), both sites log clips, reload = Skipped 8 + corridor live + plots intact (2 dead plots = square 2049's merge sources, pre-existing). Suite 2046.
- 2026-08-05 (apply/unapply audit, §15a item 20): full matrix audit (every type × apply/unapply/shared/conflict) on request. FIXED+VERIFIED: merge/footprint formation reversal removed children from the RECORD only, never the MAP — un-applying the merged park left its City-owned parcel ORPHANED live (residual test then blocked all 5 source restores; re-apply silently ADOPTED the orphan → original owners never got ground back); buildings leaked all 4 minted parcels identically. _reverseFormationRecord now removes minted layers+persistence BEFORE restoring parents; park+building round-trips verified (minted gone, sources restored, re-forms under same ids). SOUND: adopt reversal, reparcel unapply order + round-trip under standing square (bases the square owns correctly refused), delete-applied, undo (same partition engine), rules/votes, payload sanitize. OPEN (recorded in §15a-20, not fixed): (1) upload keeps `formation` → receiver's apply skips forming (guards structures:371/buildings:253) + unapply would restore uploader's ownership — strip at upload/import; (2) imported nested childParcelIds + _addChildParcels MERGE → future imports re-enter the reload re-apply loop — strip children on import; (3) re-apply/edit of a reparcellization under a standing dependent re-mints the consumed plot (double fabric under the square) — needs residual exclusion vs standing content formations; (4) residual restores persist CLIPPED base-parcel geometry (Refresh Parcel Data resurrects full shape over plots); (5) fast-reload IDB write race (~100ms). Suite 2046.
- 2026-08-05 (records travel light + dependents follow rules, §15a item 21): per Simun's directives on the audit's open items. (1)+(2) stripDerivedRecordData (formation-depth, pure, 3 tests): children/childFeatures/formation/demolition-scans/parentsToRemove stripped at PUBLISH (gate) and again at IMPORT (old server rows) — children re-derive on apply; gov-plan roads keep childFeatures (they ARE the plan); gate verifies BEFORE stripping and now flattens structure/building sub-parents when the record minted ground (publishing an applied merged park threw formation-on-formed-ground). (3) dependent rules made real: formed bodies are OCCUPIED fabric at the gate (named conflict; adopt excluded), descendant index treats merge/footprint formations as consuming (blocked-plots filter added to reparcel apply, mirroring roads), _filterRestorableParents vetoes reviving another formation's minted body (stale claims made every unapply resurrect the square's dead parcel as ownerless fabric), and the reparcel write-back records only parents LIVE at cut time (registry keeps dead layers — feature resolution let a parked parcel's id ride into the record) with a post-gate re-derive after autoPark. Verified fresh-profile: plain reapply-under-square REFUSES naming it; autoPark parks square + plots 7/7 + record clean; square re-applies with live body. (4) clipped-restore persistence explained: normal loads skip existing records so clipped shapes are stable; only explicit force-refresh repaints cadastral truth (that button was always fabric-destructive — reapply heals). (5) IDB race accepted. Suite 2049.
- 2026-08-05 (reload witness fix + building items, §15a item 22): Simun's clean wipe still re-applied 2/8 per reload (Road 2043 + Subdivide 2048) — MY descendant-index change stopped re-adding structure-consumed children at boot (correct) but _shouldSkipChildFeature still carried the pre-formation "structures only overlay" rule and refused to witness them → presence failed. Fixed: a merge/footprint structure's marker now vouches for consumption (adopt/overlay still don't). Verified: wipe → 8/8 → two reloads NO-MODAL nothing re-applied. OPEN (found while verifying, own investigation needed): (1) freeform building record DEGRADES across reload (ends unapplied/childless/formationless post-boot; route silent; suspect overlay↔record reconciliation un-applying it — look for the blue building missing after reload); (2) buildings lack geometric derive-or-refuse — declared flat base parents hit the gate and refuse outside route exemptions (roads/reparcels resolve footprint-first; buildings should too). Suite 2049.
- 2026-08-05 (editor lock + auto-buildings, §15a item 23): profile-edit clicks selected parcels + blank blown panel — corridorEditorOpenOverlay could THROW mid-render after RoadEditingZoom.enter but before its click-lock, stranding an unlocked deep-zoom map under a half-mounted panel. Open is now atomic: claim FIRST (own lock + shared __mapEditLock owner 'corridor-editor' so drill/parcel selection refuse uniformly), render in a rollback (zoom/locks/DOM/listeners undone + loud toast on failure); close releases both. ROAD OPS AUTO-SHOW BUILDINGS: ensureRoadOperationBuildings (road-drawing.js) — no survey on → GDI on, NO dialog, remembered; existing choice untouched; wired into drawing-mode enter, node-handle mount, and the cross-section editor (its survey dialog + restore-on-close REMOVED — buildings stay on). Browser-verified: open → lockHeldBy=corridor-editor/blocksSelection/gdi-on/no-dialog; close → released, gdi stays; drawing mode → gdi-on. Suite 2049.
- 2026-08-05 (base-anchor flatten, §15a item 24): derived id (`823/1#c-3g5b…-1`) surfaced under the drill's "Cadastral parcel" row — the corridor stamp copied consumed parents' rootParcelId RAW into baseParcelIds (a piece cut from another formation's slice inherits a derived "root"). Fixed: _buildChildFeaturesFromDefinition flattens through baseIdOf at the stamp; drill-ui baseGroupIdsFor flattens defensively for pre-fix fabric. Also ANSWERED Simun's model question: the drill stack Road→Subdivide→cadastre is the TIME view (road's edit consumed ground the readjustment authored — legal local stacking); the RECORD/consent view is the cadastral row only (publish flattens; records never stack readjustments). Suite 2049 (2 ENS-route timeout flakes under host load; pass isolated).
- 2026-08-06 (§15b worklist item 2 closed): reparcel apply persists the RESOLVED POOL (union of consumed parents) as record.geometry — the current claim the amend pass clips and footprintOf reads; stripped at publish (derived — receivers re-derive). Plus: a merge-take's taking = the WHOLE PARCELS taken (union of takenFeatures), not the drawn footprint — a 1 m² graze took a whole remainder while the amend clipped only the graze (found by manual amend probe: overlap 1 m² vs whole-parcel consumption). VERIFIED: the 2048-under-square roundtrip that fired 'blocked a child' every run now completes with ZERO blocked-child warns, both applied+intact; apply 8/8. Remaining §15b: item 3 only (corridor trim for roads-as-victims; loud detection in place — structure-consumed-corridor warn + residual-restore clip warns on road pieces are the signal). Suite 2054.
- 2026-08-06 (§15b worklist item 1 closed): taking geometry now = CUT geometry — _takingFootprintOf prefers full definition.polygon (tunnels included; what resolver+cut consume), surface as fallback; live road call site routed through the helper. Tunnel-takes-surface? = recorded model question for Simun; until the cut changes, taking/amending stay consistent. Verified fresh cycle: apply 8/8 → 2 reloads NO-MODAL, the 2042 once-per-reload symptom gone. Remaining §15b worklist: (2) persist resolved pool as record geometry so §14.2 remainder ground is amendable; (3) corridor trim for roads-as-victims. Suite 2054.
- 2026-08-06 (§15b steps 3-5 partial: telemetry+pruning+migration): the 4 reconciliation mechanisms now WARN '[§15b]' on every firing (behaviour kept; quiet telemetry = retirement criterion); amend pass prunes amended victims' dead childParcelIds (self-consistent records); versioned boot-once migration cb_flat_partition_v1 replays applied takings historically (silent amends). Battery made the system enumerate its remaining old world: (1) Road 2045 consumed 2042's plots though its SURFACE footprint overlaps them 0 m² — the cut takes through a different geometry (full polygon/junction) → 2042 re-applies 1×/reload until that geometry feeds the amend pass; (2) square-taken §14.2 REMAINDER ground has no amendable plan (no plan polygon, record.geometry absent) → persist resolved pool as record geometry at apply; (3) roads-as-victims (corridor trim + remainders) still fire residual-restore clips. Mechanisms stay in place, loud, until those close. Suite 2054.
- 2026-08-06 (§15b steps 1-2 BUILT): pure engine clipPiecesByTaking/amendReparcellizationPlanByTaking (formation-edit, 5 tests) + _amendAppliedPlansByTaking wired into road apply (surface footprint, !isRestoring), reparcel apply (authored polygons) and structure merge (geometry; adopt exempt). Amends victim plan polygons AND record-level pool geometry, bumps editSeq (also bumped at the updateLocalCorridorGeometry funnel + editor-shell commit), persists, toasts. VERIFIED live: strip through 2048 → "took 331 m² — 1 plot(s) split" (1508→1177 exact), victim claim over taken ground = 0, drill shows road only, idempotent second pass silent, reload = Skipped 8 with amended plan persisted. Steps 3-5 (retire reconciliation machinery→assertions, current-plan restore/presence, migration+battery) NEXT — that machinery is load-bearing until then. Suite 2054. UNCOMMITTED on top of 4b81399.
- 2026-08-06 (DECISION §15b: one partition, latest wins — no time view): Simun decided the model simplification: applied formations' plans TESSELLATE at all times; when an action takes ground another applied formation holds, the taker AMENDS the taken record's local plan immediately (clip plots/pool; trim corridor segments; content keeps the disclosure flow, never silently clipped); latest wins, permanently (unapplying the taker frees ground to base remainders — no auto-expand back). Legal because local records are COPIES (publish mints new proposals; consent attaches to the published snapshot). Adds editSeq per record for deterministic rebuilds. RETIRES most of 2026-08-05's reconciliation machinery (presence witness, blocked-children filter, consuming-index semantics, residual-restore clip→assertion, formed-body veto, drill cap, most exemption choreography). Implementation = its own session, 5-step plan in §15b (pure amend engine first, tests-first). NOT built yet.
- 2026-08-05 (UI closers): map-surface hover no longer shows the inconsistent parcel-number label (details-panel + layer-render highlightParcelHover showLabels:false; panel-driven list/drill highlights keep labels; "Show parcel numbers" = the all-labels path). Placed freeform buildings recolored blue #1d4ed8 (were selected-parcel red). Committed alongside (not this session's work): level-aware transit clipping — an applied road at grade no longer cuts gaps into an elevated alignment's deck; only a TRACK corridor replaces the line it's laid along (TransitAlignments.corridorClipsSource + per-source clip mask in three-mode, transit-alignment-clip.test.js); and the lower-left mode-stack buttons moved to a --map-mode-slot-N CSS-variable ladder in map.css with a slot-uniqueness guard test (map-mode-stack.test.js — two buttons sharing a bottom offset had twice made an icon unreachable).
- 2026-08-06 (§15b worklist item 3 closed — roads as victims): pure trimCenterlineByTaking (formation-edit, 4 tests) + _trimRoadDefinitionByTaking in the ONE victim loop (_amendAppliedPlansByTaking) so every taker trims road victims: points/segmentIds rebuilt (split pieces SHARE the source segment id → id-keyed profiles survive), edge-keyed tunnels/grade-separations retained per live edge, polygon/latLngPairs/surfaceFootprint re-derived like the edit funnel, mirrored to all 3 record locations, strips redrawn once; whole-centerline swallow = loud warn, record stays applied empty; full recut deferred to the road's own next edit; gov-plan roads (no centerline) skipped with overlap-gated warn. TWO REGRESSIONS the pristine battery caught: (a) boundary-graze churn — lineSplit fires where a centerline TOUCHES taken ground (junctions) and sliver-dropping reported "0 m trimmed" changes; rule: a segment changes only when a piece is midpoint-INSIDE the taking, else return by reference; (b) the amend tail's liveness-only prune emptied a fully-consumed victim's childParcelIds to [] (road 2045's cut ate both of 2042's plots) and presence reads [] as never-materialized → re-apply every reload; prune now keeps dead-but-WITNESSED ids (_isChildConsumedInSavedFabric) — witness noise = honest "identity doesn't carry across proposals yet" telemetry. VERIFIED fresh-profile: pristine 8/8 → NO-MODAL ×2 (only real amends fire: 2045→2042 128 m², 2049→2048 2903 m²); square over 2043's corridor piece → 454 m swallow, loud, NO-MODAL, PERMANENT after square unapply (ground freed, road stays empty); synthetic strip over 2045 → partial split 36→30 m, segIds [s3,s3], polygon 143→121 m², mirrors sync. Suite 2058. §15b items 1–3 all closed; mechanisms stay loud until telemetry quiets in real use. UNCOMMITTED on top of 4b81399.
- 2026-08-06 (identity flows with the ground — cross-proposal carry-over, per Simun's ruling): a cut's remainder of a FOREIGN formed plot stays the victim's child (largest piece keeps the plot's id + all clone props incl. proposalId/ownership; splits continue the VICTIM's numbering via derivedIdParts + a registry+records allocator). Wired in road cut (builder remainder loop, foreign stamp wins over own-prior match) and reparcel §14.2 remainder mint; structure merges = whole-parcel, nothing to carry. _adoptForeignPlotPieces appends carried ids to the victim's record; the taker records BASE anchors (baseIdOf(plot)) instead of the live plot id, so its unapply frees the strip as a BASE remainder. Three enabling fixes: _parentStillConsumedElsewhere no longer hard-blocks when the residual is computable (geometry decides; multiple records legitimately share a base anchor); residual-clipped restores stamp residualOfConsumedBase:true and the boot predicate (core.js isParcelReplacedByChildren) shows them (else the freed strip died at reload and re-apply refused at 0% coverage); presence: editSeq>0 + plan-claims-nothing = materialized (amended-empty records stop re-applying). Gov-plan roads (authored polygons, no centerline) amend via clipPiecesByTaking on polygon+childFeatures. Tunnel ruling recorded: tunnels take the surface (level tunnels only) — current taking=full-polygon stays. VERIFIED fresh profiles: pristine 8/8 with 2042's three children ALL LIVE and ZERO witness warns at boot; unapply 2045 → 97/97 transect covered, survives reload, re-apply succeeds, final NO-MODAL. Suite 2060 (area-monitors parallel flake passes isolated). PROD MIGRATION: agreed to run once Simun confirms feature-complete on local. UNCOMMITTED on top of 4b81399.
- 2026-08-06 (road-edit over §15b residuals — Simun's live extend broke, three exemption fixes): the edit funnel's re-apply refused at 21-27% coverage though the corridor ground was 100% covered by flagged base residuals. A `residualOfConsumedBase` feature/record is the base MINUS all live claims by construction, and three mechanisms still judged it by ID as the full parcel: (1) _filterChildFeaturesBlockedByDescendants dropped restored residual clones (dependents live on that base) — exempt flagged; (2) cadastre-ancestry loadedLiveParcels' structural consumed-shadow (any live `#`-child shadows its base prefix) excluded them from derive candidates — exempt flagged; (3) parcels/ingest.js rebuilt bases from the SERVER row at boot (full geometry, no flag) so residual claims died at reload — a flagged persisted record now replaces the server row (force-refresh keeps server = documented destructive path). VERIFIED: fresh 8/8 → 40m extend applies (corridor carries -1/-4/-5, extension mints on 339164 bases, presence true all RP) → reload ×2 Skipped 8 byte-identical record, 2042 carried children intact. Suite 2061 zero failures. Repro harness note: headless edits need stubs — __unapplyTour.showUnapplyDependentsPanel:=async()=>true and resolveBuildingObstacles:=auto-cut — else the funnel silently returns false on the unanswered dialogs. UNCOMMITTED.
- 2026-08-06 (stale residual flags — the sliver merged across the road): residualOfConsumedBase is a WRITE-TIME assertion; after a later apply consumes flagged ground, a leftover flag made ingest+visibility resurrect the pre-cut base shape every boot (parcel spanning both sides of the road that consumed it — Simun's screenshot). Fix 1: consumption clears the flag at the single marking choke point _setDescendantProposalOnParcels — set explicit false, NOT delete: _upsertParcelProperties spreads {...rec.properties, ...working}, so a deleted key silently survives the merge (general gotcha for that writer). Fix 2: ingest swap heals stale records (flag + descendantProposal naming an APPLIED proposal → drop flag, keep server row) — profiles from the broken window self-heal at next boot; wipe always heals. VERIFIED clean battery: extend → zero flag+consumer records, zero both-sides-of-corridor parcels, reload ×2 Skipped 8, 2042 carried children intact; hand-staled record healed at boot. Suite 2060 (docs date-test parallel flake, passes isolated). UNCOMMITTED.
- 2026-08-06 (Simun's monster parcel — verdict + open items): his `6804/1#c-3g5b9q1ch9176-1` is a road CHILD minted with blob geometry DURING the broken window (coverage-refusal forced unapplies over half-restored fabric; the child's geometry is baked data). Current code cannot retroactively identify it (it is a recorded child; clean batteries mint `-1` as corridor-only, verified by direct inspection). Self-heal attempts that DON'T work: ingest heal misses it (child, not flagged base); two-click Unapply→Apply rebuild TRIPS GATES on heavily polluted states (forged-blob test: first formation-ground-unresolved 19%, then after flagging verbatim restores parcel-conflict from cascade churn) — and flagging verbatim restores REGRESSED the clean extend flow (reverted; only the >1% clip branch flags). Reliable recovery = WIPE + reopen plan link. OPEN DEFECT (separate): boot race — an edited road's presence check sometimes runs before its children rehydrate → share route re-applies it ("Applied 1/Skipped 7", nondeterministic vs "Skipped 8"), converges visually but re-mints a child generation and merges old+new ids into the record (the ratchet); root = _reapplyAppliedProposal/_addChildParcels merge semantics + route presence timing. Band-probe note: a remainder WRAPPING a road's stub end is ONE connected polygon on both sides — parts-based monster probes miss it; the park parcel legitimately wraps road 2043's stub per authored plan (model question, not a bug). Suite 2061. UNCOMMITTED.
- 2026-08-06 (§15c BUILT — tessellation, never restoration; drawing-board decision implemented same-day): rebuildAppliedFabric = capture priors → reset derived fabric to pristine cadastre → replay applied list via ordinary applies in constraint-graph (createdAt) order with per-member ground fetch (base anchors + footprint bounds — replay was VIEWPORT-DEPENDENT without it: 0%/65%/95% for the same op). parcelDataLoaded reapply listener stands down during rebuild. severanceVerdict (pure, 7 tests): destroy only on rule-2 signature = pool fragments AND a plot fragments ("3 become 6"); whole-plot takes disconnecting the pool and single-plot splits are rule 3 (amend) — first two strictnesses destroyed Cibona members wrongly. Severance outside a rebuild → ONE deferred rebuild waiting for apply-queue idle. Edit funnel core and unapply restores REPLACED by rebuild; unapply rebuilds AFTER its transaction commits (inside = deadlock on the mutation queue). DELETED: _filterRestorableParents/_liveGroundEntriesNear/_parentStillConsumedElsewhere, residual clipping, residualOfConsumedBase + all exemptions, r- pieces, re-show priors. Fixed en route: hasLiveReplacementSlice requires slice ON MAP (hidden dead registry keys blinded the resolver after reset); structure/building formation guards re-persist standing bodies + fall through only on VERIFIED absence (bodies died at boot). VERIFIED: 8/8 → drag-sweep → all 8 stay ON, fabric clean (no multipart/under-road/overlap) → Skipped 8 ×2 → unapply 2045 → cascade clean, Skipped 6. Suite 2068. migrate-tessellation.js (explode non-contiguous slices) — local DB already conforming (342 rows, 0 changes). UNCOMMITTED.
- 2026-08-06 (hover-under-the-square = double cover, §15c follow-up): the map hover outlines exactly ONE parcel — the highlight showing ground under the square meant a VISIBLE parcel really extended there. Cause: after a rebuild the structure/building formation guard early-returned when its body layer stood (the apply re-adds it from stored data), skipping the re-take — the replay's re-minted road remainders stayed visible under the body (5 parcels ≈ the square's whole area). Fixes: rebuilds ALWAYS re-take formations; and the rebuild take CUTS partial parcels at the body edge instead of refusing (whole-parcels is the authoring gate only) — inside consumed, outside re-minted with identity flowing to the owner (derived parents via §15b carry incl. allocator; base leftovers as the structure's §14.2 remainders). VERIFIED: 8/8 → drag-sweep → all 8 ON, zero body overlaps, Skipped 8 ×2 (settled state clean; one transient mid-hydration overlap 8s post-boot is timing, not state). Suite 2068. UNCOMMITTED.
- 2026-08-07 (road bulldoze corruption fixed): a road formation is allowed to contain disconnected authored centerline stretches, but the edit path ran `corridorConnectedComponents` and minted every disconnected remainder as a separate generic `p-*` road. One bulldoze therefore created several records/replays, lost the original plan identity, and stale handle indexes could delete a different stretch; filtering geometry before ids also shifted segment profiles. New contract: pure `removeCorridorEdge`/`removeCorridorNodes` keep segment+id+profile paired; a split edge keeps both remainders in ONE road snapshot; every geometry edit parks the source, inserts exactly one immutable replacement, and performs one queued cadastre replay inside one storage batch; failed replacement is deleted and source replayed atomically. While busy, stale handles disappear and extra clicks are ignored. Removed the live near-miss heal/weld/component-split code. Unclickable terrain had a second legacy cause: entering road drawing detached every parcel click listener even though the handler already gates at click time; listener mutation is gone, so exceptional exits/replays cannot leave inert parcels. VERIFIED headless full suite: 164 files passed, 1916 tests passed, 1 integration skipped. Existing fragment records produced by the old `p-*` path are corrupt local data and cannot be attributed safely because the old splitter deliberately removed lineage; recover that browser profile by removing those fragments or wiping/reopening the shared plan, not with live fallback inference. UNCOMMITTED.
- 2026-08-07 (contiguity everywhere — four rulings from the §15c enumeration review, §15d): (1) EVERY proposal footprint stays ONE connected piece; severanceVerdict is now purely geometric (severed = footprint consumed || fragmented; the "pool AND plots" conjunct dropped — a whole-plot take disconnecting the pool severs). (2) CROSSROADS: road×road is a junction, never a taking — the later road drops applied-road-held parcels from its parents (_appliedRoadHeldParcelIds) and mints around the boxes; amend pass skips road victims for road takers; box ownership is a derivation (unapply the holder → rebuild lets the other road mint it); full overlap refuses road-over-road. (3) Severing a road is IMPOSSIBLE: structure/building takes that would disconnect an applied road refuse before mutation (_appliedRoadSeveredByTaking; end-trims still legal); a disconnected trim result logs a loud [contiguity] error. (4) Readjustments stand on the CADASTRE (derived ground refuses + interactive ask-to-unapply flow _offerToFreeReadjustmentGround, outside the fabric queue; editor blocks derived selections), and the plan must TESSELLATE every input parcel (strict, >1 m² uncovered refuses) — 10 stored plans incl. Cibona's Subdivide 2107-2048 now refuse until redesigned, ACCEPTED by Simun. Amended records exempt via amendedByTaking stamp (their §14.2 remainders are the taker's ground on replay; remainder minting stays for exactly that). Editor: an authored disconnect SPLITS at save into one proposal per corridorComponents component (endpoint↔vertex matching for T-branches; largest keeps the title) — deliberately REVERSES the 2026-08-07 bulldoze-fix stance ("disconnected stretches allowed in one record"), this time carrying profiles/tunnels/grade-seps per stretch. migrate-tessellation extended: split stored disconnected roads (Golden Street→13, Silver Way→12, UPU Borovje ulice→2; applied locally, idempotent), report partial/discontiguous readjustments. Open: road-severs-STRUCTURE not handled; gov-plan road over applied road double-covers. Suites: formation-edit 34, migrate 15. UNCOMMITTED.
- 2026-08-07 (contiguity battery findings — three fixes the live run forced): (1) PHANTOM ADJACENCY TRIMS, a pre-existing §15b bug: roadCenterlineTaking buffers the taking by the victim's half-width, so a park merely SHARING a corridor's edge "trimmed 105 m, 2 stretches split" from a road it overlapped by 0 m² (measured: park 2047 ∩ road 2043 corridor = 0). Fix: a genuine-overlap guard (taking ∩ corridor ≥ 0.5 m²) before any centerline trim, in the amend trim, the severance pre-check, and the migration detector. (2) The road-severance pre-check must test the CENTERLINE (run the same trim + corridorComponents), not the corridor polygon — a wide corridor stays polygon-connected through an edge sliver while the road itself splits. (3) Crossroads needs the CUT GEOMETRY to exclude road-held ground, not just the parent set — road children are corridor RUNS spanning bases (a 2,247 m² child on a 531 m² base), so parent-exclusion alone left runs double-covering the junction box by 234+448 m²; buildOptions.cutExclusionFeatures now subtracts holders from cutGeometry in _buildChildFeaturesFromDefinition. Also: whole-parcel gate calibrated to covered<97% AND uncovered>5 m² (turf/4326 vs PostGIS/3765 read a conforming parcel-aligned plan ~1% apart — flat 1 m² falsely refused 2042 at "99%"); severed records get _clearDerivedRecordState (stale childParcelIds grew 2→4 per replay). FINAL Cibona under the new canon: 6/8 standing — 2042 severed by 2049's whole-plot take (ruled), 2048 refuses readjustment-derived-ground (accepted; also partial-parcels per migration); junction log fires; multipart 0; overlaps only 1–2 m² edge kerf; reload ×2 byte-stable; drag-edit of 2045 mints replacement cleanly. Local DB migrated (--apply): 3 disconnected roads split into 27 rows total; 10 partial-input readjustments + 246 structure-severs-road pairs (mostly Dec-2025 fantasy-city lakes/parks over street grids) reported for author work. Suite 1922/1923. UNCOMMITTED.
- 2026-08-07 (crossroads over-exclusion — Simun's "parcel with a road on top, undivided"): _appliedRoadHeldParcelIds collects a road's childParcelIds, which include its §14.2 REMAINDERS (owner ground beside the corridor: 1,852 m² and 4,647 m² pieces with ~0 m² under the road's own corridor), so a later road excluded them from its cut and ran over an owner's whole parcel undivided. Fix: the crossroads exclusion (parents + cutExclusionFeatures) keeps only CORRIDOR pieces (isCorridor/isTrack props — remainders carry neither); remainders are ordinary ground, cut normally with carried identity. Invariant formalized in the battery probe: no live parcel may lie >5 m² under any applied road corridor unless it IS road surface — passes fresh + reload; suite 1922. Note: his clicked id (base `HR-339270-823`, a parcel that does not exist in the cadastre — only 823/1,2,6 do) was older-generation naming; the record's token is in the replay set, so a plain reload re-derives it correctly. UNCOMMITTED.
- 2026-08-07 (edited roads lost their replay slot — "can't extend to the right"): makeFreshRoadSnapshot stamped replacements createdAt=now(), so an edited road replayed LAST in the §15c order — structures authored after it replayed first, whole-took the bases along its route, and the re-derive refused at 56% coverage ("The replacement could not be derived from the cadastre", every geometry edit reverting). Fix: the replacement inherits the SOURCE's createdAt — an edit changes the record, not when the formation entered the partition (also applies to editor split stretches). Follow-up the fix exposed: reloading the share link re-applied the SOURCE next to its own replacement (URL names the server id, the fabric holds the local replacement) — handleSharedPlanRoute's applied-state analysis now walks replacementOfProposalId/sourceProposalId chains (idsAnsweredBy), counts allIncomingApplied over covered incoming ids, and the queue filter includes chain-covered ids. Verified: extend-east succeeds (p-* replacement applied), reload keeps source OFF + replacement ON, ground-under-roads invariant holds, suite 1922. ALSO: his tab was READ-ONLY ("open in another tab" = a second tab of his own browser holds the web lock — writes parked); the crossroads GRAPHIC overhang he flagged is cosmetic (he called it moot; noted: junction art shouldn't paint for sidewalk-only crossings — unbuilt). UNCOMMITTED.
- 2026-08-07 (dead ground around the edited building — cut failures swallowed): buildings.js footprint mode treated a turf.difference THROW (sweep-line errors on degenerate rings — the Parking Cibona parcels reproduce them) as "host fully consumed": the host was hidden, no remainder minted, and a whole parcel went dead — unclickable/unhoverable, exactly Simun's giant edited Freeform-building 2107-2053 leaving only two computable remainder slivers alive. Fixes: safeDifference (retry on truncate precision 7) + an honest fully-consumed check (threw AND host not ≥99% inside → refuse `building-cut-failed` BEFORE any mutation, naming the parcel); same treatment in structures.js rebuild partial cuts (a PARTIAL with null difference is never "fully consumed" → cutsOk=false). Unit test: broken-turf Proxy → refuses, hidden=[] (form-building-parcel.test.js). Chasing the repro also surfaced: the live-fabric-overlap gate refused at GEOMETRY_AREA_EPSILON (0.01 m²) on kerf debris — a corridor run grazing 39 m² base 6804/5 by 0.817 m² made all ground touching both permanently unappliable; the refusal threshold is now OVERLAP_REFUSAL_M2 = 5 (kerf measured 0.8–2 m², real double-cover is tens+). Verified: giant-footprint building now refuses honestly (building-severs-road for a mid-span crossing) with ZERO dead ground (grid probe 0/625 after refusal; fresh canonical boot back to 1% baseline = genuine cadastre gaps); apply/unapply churn ×10 leaves fabric intact; suite 1923. Simun's profile heals by reloading in a SINGLE tab (his other tab holds the web lock → writes parked). UNCOMMITTED.
- 2026-08-07 (dead ground ROOT CAUSE — carried remainder hidden by its own host id): the earlier cut-failure fix was real but NOT Simun's mechanism; reproducing his giant building placed clear of the roads (applies fine) still left 9% dead ground. Diagnosis at a dead point: the building's DERIVED host (road 2043's remainder, 3,326 m²) was hidden and its carried remainder absent — buildings.js ADDS children first then _hideFeaturesFromMap(hostFeatures), and a derived host's largest remainder CARRIES THE HOST'S OWN ID (§15b identity carry), so _addFeaturesToMap replaced the registry entry and the hide then killed the just-minted piece. Roads avoid this by design (they hide BASE anchors, never live carried ids); structures hide BEFORE adding (safe). Fix: hide only hosts whose id no child re-uses (reusedHostIds filter). Verified: giant building over the parking block applies with 0/961 dead grid points (was 87/961), carried road-remainder live, reload stable, suite 1923. Note: _adoptForeignPlotPieces no longer exists in the codebase (summary-era name) — replay re-derives records, no adoption needed. "Kerf" vocabulary explained to Simun (saw-cut waste ↦ float debris along turf cut lines). UNCOMMITTED.
- 2026-08-07 (red ground in 3D = set-aside plan preview, z-fight fixed): the red under the buildings east of the junction is buildPlannedReparcellization3D — every UNAPPLIED readjustment's slices render as owner-colored slabs (red = the plan palette's first owner color). New under §15d: 2048 (derived-ground refusal) and severed 2042 are PERMANENTLY set aside, so their previews render persistently — incl. over ground roads now hold (which is exactly why they refuse). The shimmer-through-the-road was the slice slab at z=0.08 with polygonOffset −2 beating the depth of the road surface ABOVE it (corridor strips ~0.21, OSM roads 0.30, both offset-less); preview now floats at z=0.35/borders 0.45 with renderOrder 30 — an overlay, not ground. Product question left open for Simun: whether set-aside plans should preview persistently at all or only when selected. UNCOMMITTED.
- 2026-08-07 (unapplied readjustments no longer render persistently — Simun's ruling): "visible on the map when applied or when previewed, only — like any other proposal." getPlannedReparcellizationProposals now returns ONLY the currently-selected unapplied plan (ProposalSelection.getKey), matching the interaction layer's existing parked-proposal semantics ("only render while selected"); rebuildPlannedReparcellization3D clears+redraws on every selection change (subscription alongside rebuildProposalInteraction3D) and the three-structure-refresh hook points at the rebuild (append-only re-add would have duplicated slabs). The set-aside 2048/2042 red slabs therefore vanish unless that plan is selected. Suite 1922+ (index.test.js rate-limiter fail = parallel flake, 26/26 isolated). UNCOMMITTED.
- 2026-08-07 (bike-lane edit "made the road disappear" — pre-check/amend trim mismatch): _appliedRoadSeveredByTaking's ctx lacked `buffer`, so roadCenterlineTaking returned the UNBUFFERED taking inside the pre-check while the amend pass trims with the half-width-buffered one — Simun's new squares (0708-1850/1932, authored over the road) passed the pre-check, then their amends trimmed 174+196 m off the bike-lane replacement road and DISCONNECTED it (the paired [contiguity] errors). Fix: buffer added to the pre-check ctx; those squares now refuse (structure-severs-road) instead of consuming the road. Also diagnosed: the 3D "tunnel rendered as cut" — building-tunnel liners render walls+roof+portals (addBuildingTunnelLiners3D), but the mass above shows an open slot, likely a CUT carve record coexisting with the tunnel (suspect: the bike-lane widening extends the corridor beyond the tunnel liner's clearWidth = old width+0.8, so the silent rebuild demolition scan records a cut for the overhang) — NOT yet fixed, needs his record's carve data. ID landscape documented: c-… proposalIds ARE consent-content fingerprints (djb2 ×2, sharing.js), c2-… upload fingerprints, p-… random local ids, numeric server ids — four id spaces; recommendation given to keep fingerprints as FIELDS and stop minting content-hashes as record ids (new records only; existing c- ids stay, they're baked into derived tokens). UNCOMMITTED.
- 2026-08-07 (Share Plan rebuilt as a right-docked panel — Simun's spec): showSharePlanModal → showSharePlanPanel (dialog-share.js): full-height right panel (mobile: lower-half dock) mirroring the proposals-list panel, NOT a modal. While open the app is in share-plan mode (window.sharePlanMode + body.share-plan-mode): the map is pan/zoom ONLY — guards in map-edit-lock blocksSelection, parcel hover (selection.js), parcel click (parcel-selection.js), drill (interactionBlocked + handleSurfaceClick, which structures call directly), and the selectAndHighlightProposal funnel; CSS disables sidebar/toggles/mode switches/leaflet controls (the app has NO zoom buttons — scroll zoom only, so the zoom-control CSS exception is moot-but-kept). Each CHECKED proposal is painted on the map in its row's palette colour (chip in the row matches; uncheck = unpaint + row dims via .is-excluded); row hover outlines it, row click also frames it (getProposalPanelFitPadding now measures .share-plan-panel-content; fitMapToAppliedProposals dedupes through it). KEY FIX the smoke test forced: collectProposalHighlightFeatures resolves only ROAD children and otherwise falls back to parentParcelIds — hidden once applied, so structures/buildings painted NOTHING (2/6); the panel's proposalFeaturesFor prefers live children across all containers (childParcelIds + roadProposal/buildingProposal/structureProposal/reparcellization/decideLaterProposal) with the shared helper as fallback (6/6). Opening exits model/photo view first (overlay is 2D-only), closes list/browse, collapses sidebar; Escape/×/failed-open all restore via closeSharePlanPanel. Upload/share-URL/ENS logic unchanged. Contract test share-plan-panel-contract.test.js locks the lockdown wiring; suite 1930. Browser-verified end-to-end (paint/uncheck/hover/click-inert/link gen). UNCOMMITTED.
- 2026-08-08 (square refused as "would cut into a road" — ROOT CAUSE was an unregistered take, not the severance rule): Simun's hunch was right, HR-339270-6804/5 was the culprit, and the mechanism is a threshold that discarded real ground. `MIN_INTERSECTION_M2 = 2` in plan-order.js `computeBaseAncestry` dropped any footprint/parcel intersection under 2 m². Road 2107-2043's corridor genuinely covers **0.755 m²** of 6804/5 (38.67 m² parcel), so the parcel never entered the road's ancestry, was never cut, and the corridor simply LAY ON TOP of ground its owner still held. Taking that parcel whole for a square therefore took road surface with it; `_appliedRoadSeveredByTaking` buffers the taking by the road's HALF-WIDTH (roadCenterlineTaking, ~7 m), so the 39 m² parcel became a 185 m² blob that removed **14 m of centerline from the middle** of a 454 m road → 2 components → severed → refusal. Measured directly: taking 6804/5 whole ⇒ "Road 2107-2043"; taking a properly-cut neighbour ⇒ null. FIX per Simun's ruling ("if a parcel is cut by a road even a little it has to be properly registered — otherwise it's tons of special cases"): put the floor at the MEASURED noise instead of a size judgement. Measured on real fabric: across 20,001 bbox-adjacent cadastral pairs only 8 overlap at all — three at 0.001 m², three at 0.100 m², then a jump to 1577/1859 m² (genuine data defects); and Road 2043's 8 cadastral touches are 0.755/17/25/117/190/219/680/991 m² with NO noise cluster. So noise tops out ~0.1 m² → `MIN_INTERSECTION_M2 = 0.25` (2.5x headroom over noise, 3x under the smallest real take). Verified after reload: 6804/5 now registers as parent+cadastre anchor and is cut — owner keeps a 37.82 m² remainder, 0.85 m² absorbed by the existing corridor run (NO sliver parcel is minted: corridor children are built from the whole cutGeometry, not per-parent), the remainder has ZERO overlap with any corridor, severance verdict is null, and a probe square applied on it end-to-end (ok:true, 1 child, no failure) then cleaned up. No regression risk to structures: `wholeParcelTakePlan` has its own independent 1 m² boundary tolerance, and the cut path filters at GEOMETRY_AREA_EPSILON_M2=0.01. Regression test in plan-order.test.js proven to go RED at the old value of 2. Suite 1932/1933. STILL OPEN (different class, do NOT conflate): 0.3–1.8 m² float debris where a corridor child overlaps ITS OWN cut remainders along the boolean cut line — those parcels WERE registered and cut, so this is turf imprecision, not an unregistered take; OVERLAP_REFUSAL_M2=5 and the 0.5 m² severance guard are still load-bearing for it and were NOT removed. Real fix there would be snapping remainder edges to the corridor boundary. UNCOMMITTED.
- 2026-08-08 (kerf ELIMINATED — the cut was always exact, we were rounding the result): Simun's instinct ("it's software, we should be able to cut two pieces that sit exactly next to each other") was correct, and my earlier "float imprecision along cut lines" answer was WRONG. Decomposed on the real Cibona fabric, same cut four ways: cut at 6dp result NOT rounded → **0 m²**; cut at full precision NOT rounded → **0 m²**; cut at 6dp then rounded → 2.478 m²; cut at full precision then rounded → 1.376 m²; both sides rounded → 3.170 m². The boolean op is exact either way — turf REUSES the shared vertices — so 100% of the sliver came from rounding the RESULT. Cause: **Leaflet's `layer.toGeoJSON()` rounds coordinates to 6 decimals by default** (`L.Util.formatNum`; ~7.8 cm of longitude and ~11.1 cm of latitude at Zagreb), and every read of map geometry went through it. Proof: the road's stored definition vertex is `15.96336584382847` (15 dp) but reads back as `15.963366` (6 dp). It bites because `difference()` INTERPOLATES new vertices where the cutter crosses a parcel edge; those points lie exactly on the shared line but have no twin vertex on the other polygon to round with, so rounding drifts them off it — measured sliver mean widths 0.72/1.11/3.19/4.92 cm, i.e. sub-grid-cell, exactly as predicted. FIX: `layer.toGeoJSON(false)` (Leaflet 1.9.4 supports `precision === false` = no rounding) at all **21 call sites across 10 files** — chosen over a helper module because test fakes implementing `toGeoJSON()` ignore the arg, so zero test churn and identical return shape. RESULT measured on a cold-start replay: sliver overlaps under corridors **0** (was 10 totalling ~9 m², worst 1.78 m²); full pairwise sweep of the live fabric (4,656 parcels, ~21k adjacent pairs) leaves exactly **2** overlaps, both pre-existing defects in the cadastral SOURCE across different KOs (HR-335622-4550/1 and HR-335649-808/1, 1855/1577 m²) — **zero involve any parcel we cut**. Because the kerf justification is now factually dead, the compensating tolerances were tightened with it: `OVERLAP_REFUSAL_M2` 5 → **0.25** (proposal-manager.js, comment rewritten — it had claimed kerf "no replay can remove"), and road.js's two bare `0.5` genuine-overlap guards → one named `MIN_REAL_OVERLAP_M2 = 0.25` matching plan-order's ancestry floor, so there is ONE noise floor with ONE meaning instead of three magic numbers. Verified after tightening: cold-start battery still 6/6 applied with zero failures, 6804/5 still properly cut, probe square on its remainder still applies. Contract test `geometry-precision-contract.test.js` fails on any bare `.toGeoJSON()` in the 10 geometry-reading files (plus a guard that ≥20 full-precision reads still exist, so deleting call sites can't fake a pass). Suite 1942 passed / 1 skipped; 2 failures per full run are PRE-EXISTING parallel flakes that MOVE between runs (ens-gateway+ens-route one run, parcels+proposals the next) and pass isolated. REMAINING rounding site, narrow and known: `turf.truncate({precision: 7})` (~1.1 cm) in the buildings.js/structures.js throw-retry fallback — fires only when turf's sweep-line throws on degenerate rings. UNCOMMITTED.
- 2026-08-08 (truncate-retry REMOVED + suite flakiness diagnosed as host contention): (1) TRUNCATE — Simun: "if there is an error I prefer a clear error than opaque fixing". Both `turf.truncate({precision: 7})` (~1.1 cm) retries are gone (buildings.js safeDifference, structures.js partial-cut). They were self-defeating: quantizing coordinates is what MANUFACTURES the degenerate rings they existed to survive (a coarse grid collapses near-coincident points into identical ones), and they silently returned a cut computed at centimetre precision — the same rounding class we just eliminated. Evidence they carry no real traffic: instrumented `turf.difference` across a COMPLETE fabric rebuild = **283 calls, 0 throws**; plus a 33-cut sweep of every applied footprint vs every touching parcel threw 0 at BOTH full precision and 6dp. Both sites already had loud paths (`building-cut-failed` refusal naming the parcel; `cutsOk=false` authoring refusal) and now also `console.error` the real exception instead of `catch (_) {}` swallowing it. Cold-start battery after removal: 6/6 applied, 0 slivers, 0 failures, 6804/5 still cut. (2) FLAKINESS — root cause is **host CPU contention, NOT test order or shared state**, proven four ways: `--no-file-parallelism` ran 1944/1944 green; every flaky test passes isolated; the failing tests MOVE between runs; and `createTestApp`/`createMockPool`/`validProposalBody` were each checked and hold zero module-level state (test app has no rate limiter at all; the recurring "all basemap tiles failed" line is an INTENTIONAL mock rejection in a passing test, not a network call). The machine was at **load average 74 on 8 cores** — largely 13 leftover agent-browser Chrome instances from my own earlier sessions (closing them dropped it to ~27). CONTROL EXPERIMENT: at the lower load the DEFAULT 8 workers is also 3/3 green, so the worker count was never the true cause — total machine load was. Kept anyway as cheap insurance: `backend/vitest.config.js` now caps `maxWorkers` at half the cores (override `VITEST_MAX_WORKERS`), since vitest defaults to one worker per core and each boots a full express app. Measured, same commit: default-8-at-load-74 = 70–188 s and flaky; maxWorkers-4-at-load-73 = 31–42 s, 3/3 green; no-parallelism = 260 s green; config-cap-at-load-27 = ~25 s, 3/3 green; default-8-at-load-27 = ~28 s, 3/3 green. LESSON FOR FUTURE SESSIONS: close agent-browser sessions when done (`agent-browser session list` then close each) — leftovers accumulate across sessions and silently poison every timing and every parallel test run. Suite 1944 passed / 1 skipped, 7 consecutive clean full runs. UNCOMMITTED.
- 2026-08-07 (epoch buckets — branch epoch-buckets, worktree ../consensus-builder-epoch-buckets, branched off 4b81399, main merged in 2026-08-09 at 2779f75): proposals carry `epoch_year` (2026–2966, NULL = unbucketed) — PRESENTATION metadata for the plan timeline; apply order stays created_at via plan-order.js and the timeline never applies/unapplies anything (deliberate non-dependency: that machinery was mid-rewrite when this was built). Additive DDL applied to LOCAL public.proposal (geodata; prod NOT touched — additive ALTER in proposals-ddl.sql migration note). API: epochYear in create validator+INSERT (INSERT now 37 params after merging main's flat-declaration column drop — proposals.test.js arity updated), all SELECTs incl. summary, PATCH /proposals/:id/epoch (screenshot-patch pattern; null clears), GET /proposals/epoch-plan (buckets ordered by year, then created_at) registered BEFORE /proposals/:id (route-order trap). Frontend: js/proposals/epoch.js (UMD, pure part node-tested 9/9; window.__proposalEpoch), one-line hooks in list-ui (cumulative filter + card badge), dialog-share (timeline strip "Vremenska crta / Kumulativno do X · treba primijeniti n · treba maknuti m" — a DIFF READOUT vs local applied state, user still presses apply), details-panel (epoch select, opens via showProposalDetailsModal — a list-card click only selects), server-sync summary passthrough, data.js setProposalEpochYear; i18n ×4. Suite 2058 green. Verified live (backend :3020 PGHOST=localhost, frontend :8082, city=zg): PATCH→DB roundtrip via the select, badges, cumulative filter 250→249, hr strings. Test epochs left on local rows 674 (2045), 673 (2055), 665 (2035). Epoch-plan export is the artifact hr-reljef/šibenik-report consume.
- 2026-08-09 (epoch inputs — authoring + bulk; branch epoch-buckets merged with main at 2779f75): the epoch could only be set post-hoc one at a time in the details panel, so two real inputs were added: (1) "Plan epoch" select in the CREATE dialog, sticky via localStorage cb.epoch.lastUsed (a run of proposals for one decade needs one pick, not five) — read at submit by readCreateDialogEpoch() into proposal.epochYear; the API already accepted epochYear on create so no server change; (2) per-card checkbox + purple bulk bar in the proposal list (appears only when something is ticked) that reassigns the whole selection through the existing PATCH, collecting failures into ONE report instead of silently skipping. Merge notes: main dropped parent_features/child_features/parent_proposal_ids/child_proposal_ids/descendant_parcel_ids, so the INSERT went 41→36 and with epoch_year is now 37 (proposals.test.js arity 37). TRAP WORTH REMEMBERING: resolving the modals.css conflict ate the closing brace of .proposal-epoch-select — a missing } makes the browser STOP PARSING that file, so every rule after it vanished (my bulk bar AND main's .reparcel-node-popup__consequence) while the page loaded fine and all 2100+ tests stayed green; found only by checking computed style in the browser. Guard added: backend/test/css-structure.test.js (brace balance ignoring comments/strings + leftover conflict markers, every frontend/css/*.css), proven to go red on exactly that deletion. Verifying the create dialog headlessly needs two stubs — getCurrentUserAgent (guest is gated out) and getCurrentParcelSelectionContext (it returns early with no parcels). Suite 2112.
