# DAML project for the Canton chain option (M1 minimal purchase slice).
# SDK 3.x = the Canton DevNet / Seaport target (dpm, @c7/ledger, JSON Ledger API v2).
sdk-version: 3.4.11
name: consensus-builder-daml
version: 0.4.0
source: daml
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
