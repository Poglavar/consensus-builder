------------
TODO Legend:
- to do
+ done
------------

- when a completely new parcel is created, it should get correctly the click handler
- when "finish road" is triggered 1) the mouse should be released (no more road preview to the mouse cursor) and 2) the parcel highlighting for the affected parcels and the road itself should be removed

- I also wish the road parcel to retain/inherit no other styling apart from that which all other road parcels have. At the moment it seems that it ends up with a hue of green that is darker than roads generally. Maybe it is inheriting some style, or maybe it is because it is drawn over an existing parcel which is red. After forming the road parcel I wish to re-render it, so that it has the same color as other roads on the map, and is not darker.

- Change the boundary check to on the fly conver to htrs, remove htrs from object
- create a Clear Local Parcel Data button in the sidebar. Clicking this button will clear local storage of all parcel data.
- Add Park / Square / Single Building options
- Add money / cost estimates for operations. To the sidebar. hardcode m2 price
- Add saving or sharing or screenshot taking
- add labels to map (random positions or attach to things?)
- add instructions
- add about and contact telegram / email
- on clicking isRoad name the road
- progress bar for counting blocks
- croatian block names
+ fix problematic counting where blocks have extra members outside borders
- clear highlighting when finishing roads
+ I need to add Contact, About and Help/instructions texts. Where would be the best location in the accordion? These are the texts to add.

-----------
Information
-----------
- Contact: Telegram link. Email.
- About: This application helps communities reach consensus on future development of their settlement. This is an early alpha version. Do not put in too much work -- all data is saved locally in your browser and will be lost. See this link for the roadmap.
- Instructions: A key concept is a Parcel Block. It consists of one or more parcels separated from the rest by roads (of any width or type). Each parcel block can contain a Building Block, or be earmarked for some other purpose. Parcel Blocks already exist naturally. But the problem is typically that they are too large and badly formed. This application allows you to add new roads to the map and thus rework Parcel Blocks. You can then propose different structures on them.
- Add a Roadmap page
- Let's add a global variable TOTAL_SPENT and initialize it to 0
- Let's add a label to the sidebar saying Total Spent and being updated with value of TOTAL_SPENT
- Each time the box "is road" is checked in parcel info panel the estimated market price of the road parcel should be added to the TOTAL_SPENT. If the checkbox is unchecked, the estimated market price should be subtracted from TOTAL_SPENT. 
- When a new road parcel created, its estimated market price should also be added to the TOTAL_SPENT.

+ When drawing the road, if the mouse pointer exits the map the road segment preview that was attached to it should be removed, as well as parcel highlighting

+ scraper parcela za slucaj da nam ukinu pristup podacima

- njuskalo scraper
- Let's move these buttons into separate subsection (Buildings,Single Building,Park,Square)

- When a road is finished by pressing F the same has to happen as when mouse leaves the viewport. The current road segment preview has to be removed.

- When road drawing is cancelled (Esc is pressed or button is clicked) the circles marking centreline inflections need to be removed as well

- Add a road list to the sidebar, like we have for block list.
- AI can do image analysis now. Think which parts of this app can be outsourced to AI and focus on those that cannot

--------
Username
--------
- We will display the user's username in the top right of the page, small box in the top right corner of the map with the username. When the user loads the page let's check if the user is new. In the localstorage we shall store userName. If it doesn't exist on page load it means the user is new and we have to prompr for their name. We will display a modal saying Welcome! Enter a username to start and the user can't proceed until they enter a username. We then store it in local storage and use it later to fill the Author in proposals, and for other purposes in the future.


---------
Proposals
---------

- We need to add functionality to create proposals. The create proposal button should be gold with black letters and a black border.

- The first type of proposal is "buy parcels for a purpose".

- We need to add the "create a proposal" button to the parcel info popup.

- We need a checkbox in the parcels section that says "select multiple parcels". If clicked it allows the user to select multiple parcels by clicking on each (the second click on the same parcel deselects it). If two or more parcels are selected the Parcel Info panel changes to show, instead of the parcel info, a list of all selected parcels, their total area.

- When a user has selected at least one parcel they can click "create a proposal" button to create a proposal. The proposal popup pops up.

- The proposal popup has an author, name/title, a description field, list of parcels and a box to enter EUR offered. Optionally, it could have an image of the proposal (which would be a screenshot of the bounding box on the map encompassing all the parcels in the proposal, with the parcels highlighted). The proposal will be stored in local storage. Each proposal that consists of same parcels is unique, only one such can be created. We ensure this by having a proposalHash field in the proposal object, which is arrived at by hashing the (sorted) list of parcel numbers in the proposal. The proposal popup has a button to "create proposal" and a button to "cancel".

- Creating a proposal leaves it marked on the map (hatched). There is a "show proposals" checkbox in the parcels section, initially unchecked, which controls the "parcel proposals" layer where parcel proposals live. Creating a proposal auto-checks the checkbox. It can also be checked manually which triggers a re-draw of the layer. While the layer is on, clicking a parcel that is part of exactly one proposal highlights the parcel and pops up a Proposal Info panel. If the clicked parcel belongs to more than one proposal, a Proposal List is populated in the Parcel Info panel, it is scrollable and has a clickable list of proposals it belongs to.

- We need in the parcels section a button/roggle called "Show parcel proposal list" which will pop up a "Parcel proposal list" popup on the right side of the screen. It will have a clickable list of proposal titles (and the number of affected parcels in brackets) of all parcel proposals. Clicking a proposal in the list centers the map on the proposal bounding box and higlights the parcels in the proposal.

- We will also later have road proposals and building block proposals, so make the code reusable and keep the generalized stuff in proposals.js

-------------------
Proposal acceptance
-------------------

- Move the  Accept Proposal button below the acceptance status. Also, have ownership check always return true for now (for testing purposes). So the button should always be enabled. When the button is clicked, the proposal should be updated store the information which parcel accepted. When we click Accept only the parcel we are currently on (the last parcel we clicked/selected) should accept the proposal. It should also change the button to Reject (clicking on which un-accepts the proposal, for that parcel). Only when we click on a parcel that has not accepted the proposal yet we can click Accept. So for a proposal that affects three parcels, we need to accept for each parcel separately by clicking on it first.

Introduce Proposal Execution. If the parcel to accept is the last parcel, the proposal will be Executed. Execution will do several things:
  - Show a small celebratory popup/animation saying "Wow, all the parcel owners have accepted the proposal! It will now be executed and go live. Congratulations for reaching Consensus on this matter, may you enjoy your just rewards!"
  - Change the proposal status to "Executed" in the data, this means the proposal will not be drawn on the map any more when "show proposals" is checked. Change that functionality to only draw "Active" proposals
  - Distribute the budget of the proposal equally to all the parcel owners that accepted the proposal
  - Change the ownership of the affected parcels from current owners to the proposal author
  - The proposed changes become permanent on the map. 1) In the case the proposal was a road, the same function is called that is called by the "Create Road" button and the road is implemented on the map (parcels are split, new road parcel is created, etc). 2) In the case of a building rule proposal the proposed buildings are added to the existing buildings layer and removed from the proposed buildings layer. 3) In the case of other proposals the parcels involved get their property isSquare, isPark, etc, set to true according to the type of proposal.

- When we render a parcel that is part of a Square we should color it light gray. (When we render a parcel that is part of a park it should have small trees scattered on it, maybe tree emojis?)

------
Agents
------

- We need a backend system that will use AI agents to create proposals, accept them, vote on them, etc.
- We will use ElizaOS https://www.elizaos.ai/

- We will need to build a backend and a polling mechanism on the frontend that will update the map based on changes at the back end.

- We need to fix now how the building proposals work. Currently when executed the proposal is cleared from the map, but the building is not applied/drawn on map. It needs to be added to a new leaflet group/layer called building rules, and this should be drawn on the map. To clarify the terminology, these are not buildings, but rules how new buildings will look when built. So they are not proposals any more, but not buildings either, they are a property of the terrain, like buildings, roads, etc.

+ There is quite a bit of javascript in the index.html. Think hard and propose new .js file(s) to sequester it into

Agent general functionality:
  - Agents (that can be real people, bots, AI, whatever)
  - Agents will have personalities, state and actions. 
  - Agent avatars will be taken from the 5x5 grid in heads.png, which has a grid of 100x100 pixel heads
  - Agent names will be randomly generated to be 3-6 syllables of consonant-vowel combinations, first letter capitalized.
  - Agents will be able to 1) create proposals, 2) cancel own proposals, 3) act on proposals of others (accept, reject, donate). An agent can only accept or reject a proposal if it owns a parcel that is part of the proposal. Agent functionality for now should be mostly kept in agents.js, new file. Agents persist in local storage. Their names, lists of wallet addresses (if known), their ETH budgets.

Initializing the game state (in game.js):
    - 1) create 10 agents with initial 100 ETH budgets and
    - 2) assign each parcel an owner (a random agent) and store that in local storage (parcel_${id}_owner). Also part of the game state is current game datetime and current game turn, as well as the game log (which is an array of 500 strings where new log items are added by pushing out the oldest), they also need to be persisted in local storage. We also need to persist the mapping parcel -> owner in local storage. We need to modify the Parcel Info panel to show the parcel owner (name and avatar in a rounded circle, clickable and opening the Agent Dialog/Modal which shows stats/properties of that agent: wallet address (if known), ETH budget, number of parcels owned with a clickable list of them, number of proposals created with a clickable list of them)
    - 3) have a game loop (representing a week in real time, and going off every 5 minutes in real time). We need to add "Game Datetime" to the UI, as well as "Turns played". These will be in a collapsible sidebar accordion section called "Game" which we will insert before the Parcels section (collapsed by default). It will have a Play/Pause toggle button which is Paused initially. When player clicks it it starts the game loop. The first call will initialize the game state, that is, create the Agents. There is "Reset Game State" button which deletes all the Agent data from memory and local storage. There is "Show Game Log button" which pops up a large dialog with the logged events each in one line. The game loop will go through Agent objects and call act() for each agent so they can decide what to do and do it. Agents will eventually be run by AI, for now they will on each game loop pass decide randomly between options:
      - 1) do nothing
      - 2) accept one proposal (if proposals for their parcels exist), random for now
      - 3) create a new proposal by selecting 1-10 adjacent parcels (chose first one randomly) and randomly selecting the type, title, description and amount (amount never more than 5% of the agent's current ETH holdings)
      - 4) donate to another person's proposal, random for now, (we need to add a budget variable the proposal objects, and also persist that in local storage. Budget of a proposal is the amount that will paid out equally to owners of the proposal's parcels once the proposal is accepted by all of them)
      - 5) reject one proposal you already accepted
      - 6) cancel an existing own proposal. For now we only implement the first 4 and they have equal odds of happening (25%)
- when an agent does something a line is written into the game log in the format Agent X did Y, where Agent is clickable and opens Agent Dialog, and parts of Y may also be clickable and may open Proposal Info, Parcel Info or another dialog/modal with more information.
- The map view needs to be updated if various changes that happen to the world state in the game loop are in the visible part of the map (maybe this already happens? When is map refresh triggered?). Meaning, if agents create proposals, user should see them appear (with a small short animation on top of them to draw attention). Same if a proposal is executed by agents (not just user). Same if there is an acceptance of a proposal.
Agent Statistics. We need to add a screen showing various statistics about agents, popping up when the (new) "Show Agents" button in the Game accordion section is clicked. For now a table with:
  - 1) the head/avatar
  - 2) the name
  - 3) current wealth in ETH
  - 4) number of parcels owned
  - 5) number of proposals created
  - 6) number of proposals accepted
  - 7) number of their proposals that have been executed

- We need a new file called financials.js. The first function to put in it should be estimateParcelMarketValue(parcel id). For now it will return a hardcoded value of 0.1 ETH. This parcel 
- It will also have getLastTransactedPrice() function
- Is proposal always to buy? Or is listing to sell also a proposal?

- Let's modify the "Welcome to Consensus Builder!" initial behaviour. We want the user to be one of the agents. The difference is user would not be controlled by the game loop. But he would get an allocation of parcels when game state is initialized, as well as the same amount of ETH as everybody else. The avatar for the user will be reserved: avatar0.png. User will be an agent in all respects except his actions are not controlled by the game loop. But he appears in the statistics, his avatar and name follow his proposals, his parcels, etc. When the user chooses the username on the initial welcome screen, they should get the Agent Details dialog with their avatar and name populated. Everything else will be empty because the game state has not been initialized. We should therefore add a note to the Agent Details dialog saying "To get some funds and parcels to play with, click Init Game State in the Game dialog. Have fun!"

- User is being created, but not with the avatar0.png as their avatar, as it should be. Fix that. Also, when the user is logged in let's have a log-out button available when their username is clicked in the upper right corner. When user logs out their agent and their data should not be deleted, but they should be dissociated with them so the next time they visit they get the popup to enter username again. If they enter an existing username they "take over" that agent. Whichever agent the user controls is marked to be user controlled in the data and the game loop should skip it. Logging out removes the userControlled boolean.

- In the game loop, if an agent has done something regarding a parcel or a proposal on a map, let's draw a clickable bubble with their avatar at the edge of the map view the user is currently viewing. The bubble should be positioned approximately on the intersection of the imaginary line between the centre of the map view and the object (parcel, proposal) the agent interacted with and map view edge. The bubble should have a pointed tip pointing in the direction of the object. When the bubble is clicked, it should gently zoom the user on the object and put the bubble on it. Whenever the map view moves the position of all the bubbles is recalculated and adjusted.

- When we arrive to the proposal, it should be selected/higlighted (show proposal checkbox needs to be auto clicked on when we click on an agent bubble). Also, agent bubbles should only disappear if clicked and arrived at destination, or if the game loop updates them when a new turn comes. Otherwise they should remain until user clicks them.

- accepted proposals are missing highlighting

- Change Parcel Proposals list be tabbed with the two tabs being (Active) (Executed)
- And every item in the list should get the author avatar. The list should be sorted by datetime of proposal creation descending (if this datetime information is not part of proposal data, we need to add it)

- Make game status bar a scrollable list

When creating proposals, Agents need to look at all available parcels, not their own parcels. The steps should be:
  - choose a random parcel on the map
  - randomly pick number of parcels between 1 and 10
  - loop/floodfill to select the number of continuous/adjacent parcels chosen in the previous step, making sure the parcel being added is not a road
  - when done, check to make sure the proposal has at least one parcel not owned by the agent themselves (if not, go back to step 3)
  - when done, pick randomly the other properties of the proposal, as now (type, etc)

----
Game
----

- Let's make the game run by default, on start.
- On browser refresh, once the user has entered username (or if already logged in), the game should start, same as if user pressed Play.
- If the game is not initialized, it needs to be initialized first, same as if user pressed Init Game State.

- We need to change the proposal - parcel clicking for the user, currently it is not clear which parcel is selected
- We need to change proposals to consist of multiple parcels, not own
- budgets are going negative
-  Fix how we purge parcels
- if a turn finishes mid flight it will disappear/reset the bubble... we need a duplicate/detached bubble for the flight

- Let's change the Parcel Info panel to have a Proposals List section. This is a scrollable list of Proposals that have been made. At the right part of each proposal in the list there should be [Accept] button but it is only enabled once the proposal is clicked and highlighted (so it cannot be done blindly)

Explain to me the current architecture for storing parcels in the system:
  - Are they stored only as a Leaflet layer group where each parcel is one leaflet layer? Or do we also have another data structure? What happens when we flush/clear parcels from memory? Is it possible to mark some parcels for preservation so that flush/clear doesn't clear them?
  - Is the performance improvement noticeable/significant?
  - Propose a solution (don't implement) for preserving all parcels that have ever been loaded in memory. Or maybe for defining the Game City, which then has city limits, and we slowly load all of the parcels
  - When a parcel becomes a part of a proposal, its geometry should become part of the proposal too? This is so that getParcelFromId doesn't have to be called, and the relevant calculations can be made...

----------------
User as an agent
----------------
- Let's implement a log out feature. When user logs out they can log in with a different or same name.
- They can "take over" an agent if they type their name.
- Let's make it so that the user is just an agent in the system, just not being controlled by the game loop.

- Why can't it get bounds? also what do we use getBounds for and can we rework it to use data that does exist in the grid cache, like the geometry itself? as far as I know we just need to find the direction where to point the agent bubble and fly it to? Can we point and fly based on grid cache data and then highlight based on real parcelLayer data (when the flying is complete and we arrive at destinatin the parcelLayer will become populated again)

- To the bottom of the Agent Details panel we should add Agent Log, which is same as Game Log only filtered for that particular agent.

User as an agent:
  - Let's now work into integrating the user more as an agent in the system. The main difference is that the user's actions will not be controlled by the game loop, but in other things he'll resemble agents
  - Agents should have an aiControlled flag which is true/false. Those with aiControlled = true should be controlled by the game loop, others should be skipped by it. The agent currently controlled by user should have userControlled = 1, only one agent at a time can have this flag set to true. It is also possible to have both aiControlled = 0 and userControlled = 0, in which case the agent is simply idle. 
  - The user needs to have an avatar, they can pick an unused avatar from our set of images (avatar0.png - avatar16.png). This avatar choice is done right on the log in modal by clicking the placeholder image. Users can't change avatar later.
  - The username in the upper right part of the screen needs to be changed to show miniature version of the avatar, not generic icon as now, and the width should not be hardcoded but fit the name chosen.
  - It also needs to be clickable and on click it should pop up Agent Details for the user's agent. When the user is looking at the Agent Details for his own agent, there should be (You) in brackets next to agent name and a prominent "Log Out" button to the right of that (not in the footer). If they click log out they are presented with a choice ("Let AI run the agent" (default), "Agent should be inactive". This choice sets the aiControlled for the agent to true or false) and be offered the "Log In" screen again. When the user is logging in, if they type in an existing agent name they will get modal message saying "Taking over agent X (Yes/No)" where agent X is a link that pops up the Agent Details dialog, and they will take over that agent if they choose so (aiControlled = false). User can take over any agent. If they choose No the name is cleared and they can type something else.
  - We need to add the user to the function that distributes parcels in the Init Game State. The user should get a distribution of parcels like other agents, as well as funds, etc.
  - The user's actions (only those of consequence, such as creating proposals, accepting them, donating...) should also be added to the Game Log.
  - User's agent data is preserved accross browser reloads, same as other agent data.
  - Reset Game state keeps working as now, and deletes all agents' data including user's agent. 

+ In the Agent Details modal we need to rework the parcel list. I think we already do this but we need to check and make sure that we track proposals on parcel level -- each time a proposal is created its ID needs to be stored with the parcels affected (and this should also be in local storage to preserve it between reloads). When we show the list of parcels in Agent Details we also need to show (in brackets) the number of proposals the parcel has. The list should be sorted by number of proposals descending. Clicking the parcel number should close the Agent Dialog, center the map on the parcel and pop up the Parcel Info modal (which will then allow the user to accept the proposal).

Agent personality:
    - This is a sentence that will be displayed in the Agent Details under a label "Personality". It is choosen randomly when agent is created from these choices:
    * "My goal is to get as rich as I can from this"
    * "I love my city and I want it to be well balanced, I will support all good proposals"
    * "We need more parks, I will support such proposals, roads, not my thing"
    * "Never sell. I wish to accumulate land -- God ain't making more of it"
    * "Personally I am quite NIMBY. But we can talk if the price is right..."

- For park proposals let's add a few tree icons to be drawn on the parcels that are part of the proposal. They should remain after the proposal is accepted. Maybe they should be part of a new Structures layer.
-----------
More cities
-----------
- Dodati BG: https://portal.rgz.gov.rs/rgz-portal/map
- Ljubljana?

- Let's change how agents create proposals. Let's have them 1) consider any parcel in the parcelLayer, instead of just their own parcels, 2) create proposals from between 1-8 contiguous parcels, 3) insure at least one parcel in the proposal is one they don't own

- Beautiful :-). Now I would like to go back and remove all the extra debugging logs we added for this. Also, we did some changes in failed attempts to fix this that I would like to reexamine -- maybe they were not necessary and could even be problematic. I am especially concerned about the  updateProposalLayer() function. Can you analyze it and think whether it is even necessary, and if so whether we are calling it only where and when needed. We removed it from fetchparceldata() -- that seems a good thing, but will it break something? Also, during this process we noticed we have selectedParcel variables (two) both as globals and as window variables. Let's unify this so we don't have the duplicate store, and fix dependencies if any

+ I am uneasy about reapplyProposalHighlights() and updateProposalLayer() and other things in fetchParcelData(). I do not like the deferred call (what if it triggers and fetchParcelData has not yet finished?). But even more I do not like the function that should only fetch parcel data be responsible for anything else. Can you analyze the fetchParcelData() function to see how we can make it do just one thing, what its name says. For example in the case of updating the proposal layer, whatever is triggering the fetching of map data should be aware that proposal layer will (maybe) need to be updated once data fetching finishes. Am I making sense? Would it be hard to separate these concerns? Think hard

- Solve the problem of unowned parcels, which we now get as initial distro is only for the visible ones.

+ Introduce a "size check" on parcels agents are making proposals on.

+ Let's add some transparency to the Proposal Details modal so that a bit of map can be visible through it.

+ Fix avatar choice, which currently shows but never picks avatar0. There is an issue with +/- 1 array offsets. We have avatar0.png to avatar16.png. Let's make sure our indexing in avatar choice matches this fact so avatars are correctly chosen.

- User logout should start new game? Or get him some new parcels in a different way?
- Implement user inbox, fix finances, make leaderboard sortable

-------------------
Mobile improvements
-------------------
- Let's remove the + / - zoom buttons from the map overlay, nobody uses them. Let's also do a few changes for the mobile view. In the mobile view the sidebar should initially be collapsed, not expanded. Also, check all our info panels and modals (Parcel Info, Proposal Info, Agent Detail, show block list, etc) to see whether they are compatible with small (mobile screens)

- Let's enhance the mobile view. Perform an analysis of the codebase and suggest improvements so that everything would look better on mobile screens.

- Let's work on Proposal Details. First, it should have a title bar (separated style which will be shared by other elements) consisting of the title and the X button. Title bar should be at the very top of the detail container and should not be part of the scrollable content, which should be in a separate div.

- Second, on mobile both have different heights, which seem arbitrary (not based on content length). Why are they not either full screen height or, say, half but scrollable? Width-wise on mobile both seem full screen, in fact, more... their right sides seem to overflow the screen and not fit into it. The left margins for content are also pretty large, they should not be large and should be same as right margins.

Username bubble:
  - Let's enhance the mobile view. One thing that can be better is the username bubble in the upper right part of the screen. It should snugly fit around the avatar and the username. At the moment it seems it's wider.
  - The username bubble is not even visible on mobile Brave. On mobile Chrome it is visible, but is overlapped by the expand sidebar button when sidebar is expanded. Let's maybe have it positioned vertically a bit below the expansion button, so when expanded it will not be covered.
Ephemeral message:
  - On mobile Ephemeral messages now conform to screen width but seem to be too thick, almost as if they have an extra row at the bottom. Text needs to be styled better. 
Consensus builder title:
  - let's make Consensus Builder title colapse into "Consensus..." or some such on mobile so that all three elements of sidebar's title bar (logo, title, head icons) can fit on one row. Reduce spaces and margins between the three too as needed
Agent Statistics dialog (Show Agents button):
  - Needs some mobile love (note that this is not the same as Agent details dialog). The title (Agent Statistics) is covered by the browser address bar (implement same solution as we did for Agent Detail here). Also it has a partially visible Close button. Given the title has an x for closing, we can remove the Close button section. We should also expand the length of the scrollable content box so that it uses the whole length of the container.
  - We still need to tweak the height of the dialog. It should take up the full height of the mobile device. It seems to be taking up more. The title bar is cropped on the top on mobile Chrome and completely covered by the browser address bar on mobile Brave. The bottom is similarly cropped, on both. Think hard, we seem to have the height issue with all our modals on mobile. We should not be hardcoding pixel offsets as a workaround, but use some provided "visible viewport" CSS setting, or some such, to position the modals.
Show game log:
  - Has same issues as Agent Statistics dialog mentioned before.
Parcel proposals:
  - Parcel Proposals on mobile is still looking the same. Change it so it takes up the whole available screen area, like Agent Details, etc. Also, on mobile when a proposal in the list is clicked we should close the dialog and collapse the sidebar if open, so that we properly focus on the proposal.
Agent Details:
  - Let's simplify the styling of the lists in Agent Details dialog so they all have same styling and there is no color and no colored sidebars in the owned parcels list. There can be coloring in the lists pertaining to proposals, where the color of the proposal can be used (if we know it).
  - I also don't like the "...and xyz more..." lines in the lists. Let's change the lists so they only load the first 20 items, then lazy load 20 more if the user keeps scrolling down, etc.

--------------------------------
User notifications - new feature
--------------------------------
- Let's now add user's inbox for messages from agents. Actually, is this kind of exactly against the point? Is not the whole point that users communicate through proposals and counterproposals, and NOT via unstructured text?

+ Let's add an indicator on the username bubble that will indicate that the user has new items to look at. The indicator should be a small number in a circle, present if not zero, representing the number of "unread" items. These would currently be any new (unseen) proposals pertaining to the user's parcels. We need to have a list of the user's notYetSeen proposals. Whenever a new proposal is created, if at least one of the user's parcels is a part of it the id of the proposal is pushed/added (once, no duplicates) to the notYetSeen list/map. This list is where the count comes from. When Agent Dialog opens for the user, there should be a new section "Proposals pending" (we need to add this) and it should be the first list on the Agent Dialog panel, above Owned Parcels. This is the list of proposals for the user's parcels, sorted by proposal creation datetime descending. Proposals should have an "unread" indicator in the list if they are also on the notYetSeen list. When user clicks on a proposal in the list the proposal id is removed from the notYetSeen, Agent Dialog is closed and the user is taken to the proposal on the map, which is highlighted but not selected (proposal details dialog does not pop up until/unless user clicks the proposal)

--------------------
Index.css - break up
--------------------
- index.css is too large. Let's start to break it up. Think hard and suggest, based on its current content and the application logic and best practices, the names and content of the smaller topic-specific .css files we could use.

- I feel there is a lot of duplication, especially when it comes to Dialogs/Modals/Panels. These should essentially all use the same basic style with very few differences because their content is different.

-----------
No category
-----------
- Add Proposal Log to Proposal Details and Parcel Log.

- Add a "proposal screenshot" to the proposals when they are created (by either agents or user). This should be a section of the map only (no UI elements) zoomed and focused on the proposal bounding box (so all proposal's parcels fit) with only the proposal's parcels highlighted. The image need not be big, it will mostly be shown as thumbnail. Let's add a moderately-sized versions of the image to the Proposal Details and Proposal Info dialogs (in the top part of the dialog). Let's add a small version of the image to the:
- proposal lists in the Agent Details dialog
- proposal list in the Parcel Proposals dialog
- proposal list in the Parcel Info proposals tab

- Add avatar heads to the lists of parcels in the Proposal Info parcel list. Also remove the arrow, the clickability is implicit.

- Proposals checkbox should be checked (if it isn't) when Show Proposal List is pressed

+ We need to add "Break block up" button to the parcel blocks section. It should only appeal when a block is selected. Clicking it will break the block up into two blocks by creating a new road through the middle of the block. A block is never perfectly square. We will be breaking it by figuring out the longes side, then find its middle, then create a new road perpendicular to that point. Use the same road width as when drawing a road manually, and also use the same functionality to update affected parcel shapes.
[ ] In two along the longest side.
[ ] by maximum side length.

+ Let's add more information to each block's Block Info modal. Let's split the information into two columns. Let's add: 1) circumference in meters 2) how many minutes it takes to walk the circumference assuming 5 km/h speed, 3) avg parcel area, 4) avg parcel length, 5) number of landlocked parcels (those not touching a road), 6) area ov landlocked parcels

0 When counting blocks using the "From selected parcel", focus on each parcel that is being added.

+ Selecting blocks is very slow, parcels are higlighted yellow and selecting seems to be additive, the previously selected block's parcels remain highlighted. First check why it would be so slow. For each block we already have a list of parcels, higlighting them, and the outer border, should be very quick.
+ Then we also need 1) clicking a block should select that block only, 2) selected block's parcels should be highlighted in blue.

+ In block info popup/modal, sort the list of parcels by area descending

+ We still have a problem with blocks being formed by treating the map edge as a valid road/border. It is not. Only blocks that have all parcels fully visible on the map should be formed. Currently, more are being formed.

- I NEED TO BE ABLE TO LINK TO PROPOSALS (! BY THE END OF TODAY !)
- rasterski ili bas na mapu? Ili cak Embed?

+ On page load we should perform a check whether we are on file:// or localhost and if so set a global variable current_environment=development. This will allow us to do some things differently. 1) In the dev mode, close below the sidebar heading "Consensus Builder" there will be a small red button that says Development. 2) if the environment is development, we won't autostart the game loop. 3) if the environment is development, debug mode will be switched on by default. 4) In the development enviroment, we will be able to choose Data Source and the picker for that will be a dropdown shown in the sidebar in a new section called "Data". The dropdown will have three options: oss.uredjenazemlja.hr (default in prod), localhost (default in development), api.urbangametheory.xyz (visible but not used for now). Build the dropdown for now but don't wire it up with the rest of the code.

+ I want to introduce a major new feature. Currently we are getting parcels from an online source. I want to keep that, but to add additional option to fetch from our own server/api. We will build a simple backend node.js/express server that will expose the api. The first endpoint we will have is GET parcels. The path/params need to be such that we can have an almost identical call to the one we are doing towards oss.uredjenazemlja.hr (we don't need the token, for example, but we need the bbox). Then, the api server needs to fetch the parcels from the parcels postgres database table and respond in a format identical to the format the oss.uredjenazemlja.hr is responding. Which data source is used will be controlled by the dropdown in the Data section of the sidebar, depending on the environment user is on and what he chooses. Wire it up.

- We have something I would call a "viewport issue". We are sometimes not correctly detecting that more parcels need to be loaded. Initially parcels load and our map view is covered by parcels. But after some panning and zooming in and out, more parcels get loaded. But somehow we can end up with a view of the map that shows part of the map not covered in parcels and with no loading of new parcels going on at the moment. Typically bottom 20% of the map, or rightmost 20% of the map, or both, will not be covered. Meaning the detection of when we have to trigger more fetching and drawing is not always correct. Can we analyze that part of the code.

+ We need to add a button (toggle) overlaid on the map (somewhere in the bottom right corner) saying 3D, like google map has. When clicked the button changes to saying 2D, enabling switching back to the 2D mode. When the 3D button is clicked it will switch the main map view into 3D mode, which we will render using three.js. The view will tilt in an animation, entering 3D mode. The map will be drawn flat while keeping all its features (parcels, roads). We will then draw the 3D objects (buildings, building blocks, etc) where existing and appropriate on top of the map. In the 3D view it will be possible to rotate, pan, zoom, etc. The source for the buildings' shapes is the Buildings layer we already have (it has the 2D outlines). For building heights we will assume 3 stories (10 meters) unless height data is also stored for the building.

+ somewhere along the way I broke manual road drawing. "No parcels affected by this road. Please try drawing the road again." Maybe the algorithm was shared. There should be a separate algorithm for intersecting the drawn polygon with parcels, and it should be simple and work.

+ The proposed buildings should also be rendered in 3d mode along with existing buildings, if the checkbox for showing them is checked in the sidebar. The checkbox for "show existing buildings" should also regulate whether the buildings are shown in 3D.

+ move the 3D button slightly up so as to not cover the legend and leaflet copyright.

+ When switching to 3D mode, we should remain positioned roughly where we are and parcels and buildings should be rendered in 3D at about the same distance from "camera" they are in 2D. Currently entering the 3D view is moving/repositioning the "camera" to a very removed and rotated position.

+ Add a guard so that we don't load parcels if zoomed out too much. Only the default zoom level and two "up" from it (19, 18, 17) should fetch-load and display parcels. Hard to pinpoint where the cutoff should be because "zoom level" actually seems very vague. Can we have an indicator on the map which zoom level we are on? Or maybe better, how far from the ground the "camera" is?

- Road drawing that should never change whether a affected parcels are a road or not. Currently if an existing road polygon is split in two by a new road, one part of it (the part that is split out as new parcel) will not have the "Road" checkbox. It should have it.

- In dev/debug mode, show "Accept" on proposals for all parcels.

- Add chamfer control to the building blocks modal. It will control camfer of vertical edges of building block polygons, applicable if the  is they are 90 degrees or less. Compensate for chamfer thinning the building by changing its shape, adding the area removed by chamfer to the back.

+ Clicking "Show Existing Buildings" in 3D mode triggers the load from source, but not the rendering. After they are loaded, they should be rendered.

- Have some sort of ignore list for proposals. We actually want to remove some proposals, but given they'll be on the blockchain we can't delete stuff. So we have to figure out how to hide/ignore.

- The Data section in the sidebar should be present/visible only on localhost by default and on server only when debug mode is actiated

- Add modal Compare: per parcel benefits of each proposal
+ Generate random proposal amount
+ Generate random road name

+ Add a red button to the sidebar to wipe out entire local storage, all data that we have stored in it. Position it below "Enable debug mode" and show it only in debug/dev mode. Is there a single function that does this without any iterating and knowing what is there? Use it

- Drawing roads, add widths, add examples
- We need more road analysis tools and ways to categorize roads.
- We lost all road widths in Analyze All Roads!

________________________________________________________________________________________
In Parcel Info proposals tab, in each proposal list item we need to add a button called Compare. The Compare button launches a modal which has a table with two columns for each of the rows (row labels) below. The first column is titled Currently, the second Proposed. The rows are: 1) parcel area, 2) building footprint, 3) building height, 4) building floors, 5) square meters, 6) parking spots. 7) estimated market value of the building 
Difference in market value (potential profit): current makret value subtracted from new market value

How to calculate the values:
1) parcel area
current: area of the parcel
new: area of the parcel (should be same for building proposals and different if the proposal is a road splitting or changing the parcel)

2) building footprint
current: the area of the building(s) on the parcel currently
new: area of the part of the block building polygon that intersects the parcel

3) building height
current: put current height, which we assume to be 10 meters
new: depends on the height of the proposed building, pull from there

4) building floors
current: current height / 3, round down
new: proposed height / 3, round down

5) square meters
current: current footprint x current floors
new: new footprint x new floors

6) parking spots
current: 4
new: 0

7) estimated market value of the building
current: current square meters x square meter price in the area (assume 3500 EUR for now)
new: new square meters x square meter price in the area (assume 3500 EUR for now)
________________________________________________________________________________________

- When there are multiple proposals we need a way to choose the best

- Add on hover explanations for Road section of the sidebar:
- detect roads from OSM: Use OpenStreetMap road database to both draw road lines on the map and also mark parcels they intersect as roads in our system.
- draw roads from OSM: Use OpenStreetMap road database to draw road lines on our map.
- draw roads from WFS: Use uredjenazemlja.hr data about land use to draw polygons that correspond to road land uses on our map
- detect roads from WFS: Use uredjenazemlja.hr data about land use to figure out which parcels on our map are roads and mark them as such.
- analyze all roads: Perform an analysis of all roads currently visible in order to determine their widths. Uses OSM data to figure out where roads are.
- draw road manually: As the name says. Tool for drawing a new road parcel on the map.
- clear roads from local storage: clear all road markings from local storage. Parcels remain.
- osm road lines: Show or hide road lines drawn from OpenStreetMap

+ Add on hover explanations for Parcel Blocks section of the sidebar:
(re)form blocks: This will examine all parcels (in the current view only!) and form blocks of parcels where possible. To form a block: 1) it has to be fully surrounded from all sides by road parcels, 2) all parcels of the block have to be visible in the current view. Blocks will not be created if roads surrounding them are not properly marked as roads, nor when some parcels of the block are not visible on the screen.
from selected parcel: Form a block of parcels by starting from the selected parcel and flood filling neighbouring parcels until a block is formed.
break block up: Available when a parcel block is selected. It will attempt to split the block in two by creating a road in the middle of the longest side.

- we have issues in forming blocks, single-parcel block which is not fully enclosed by roads for example but by other parcels

- rethink how we treat and persist buildings from proposals between reloads

- retry building fetching in proposal mode, seems to fail once

- Add splash screen explanation for fist comers

- Add Tools section or tab to Block info panel

- Enable the single building button. It will work very much like the Buildings button, but instead of a building block it will add a single building. It will also have a modal but with somewhat different sliders. It will start by drawing a small (3m x 6m, if it fits) fully enclosed rectangle polygon in the middle of the parcel. Sliders control length and width of the rectangle. We will allow user to drag around the polygon position (which must always be fully within the parcel polygon) with a mouse.

- Add separate setbacks for each parcel polygon side. Figure out setbacks from roads.

- Let's enable parks and squares buttons for parcel blocks. They are enabled only while a block is selected. The park button creates a park. In 2D mode this is dark green, signifying grass, crisscrossed by winding paths and having a blue oval shape for a pond. The square button creates a square, which is grey color, tiled, and has a fountain symbol in the middle. In 3D view we'll draw a simple fountain and some chairs.

- Enabling a sidebar section needs to put/scrool the whole section into view. Currently parcel blocks section doesn't show add buildings buttons so a new user might not know they are there

- when we enter Proposal mode the Parcel Block mode should be exited. Meaning its panels and lists should be closed, block borders and highlighting cleared from map, and also on leaving parcels after hovering the blue higlight should not be used/restored.

- We need to know which part/slice of a 3D building "belongs" to which parcel. Add lines to the 3D buildings that slice them between parcels.

- Fix game so that it doesn't autostart until there are X parcels loaded and ready to be distributed. Maybe just disable autostarting game?

- Major feature: improve UX by switching to context-based menus, eliminating the sidebar completely or almost completely. For example for parcel blocks, remove operations from sidebar into icons and panels that are on the map.

+ When fetching data from servers, let's add 3 retries in case the errors are 500. Small 2 sec pause between retries.

- Separate out expanding a sidebar section from checking it. Section should be able to be checked but not-expanded and expanded but non-checked. The chevron should be able to expand or contract the section (and change shape while doing it) without affecting whether it is checked or not. If section is expanded but not yet checked all the options in it are disabled and grayed out. Initially all sections are collapsed, even those checked.

- After parcel slices manage to work in 3D, add a checkbox to turn them off

- create tables on the server Postgres, at least the proposal ones

- Track the amount of data needed to share via browser link, ... use cestica_id
- calculate the bounding box for the shared proposals, share it so that parcels can be preloaded (what about very long roads?) How are changes applied?

- I have some situations where I think a parcel polygon is being drawn twice on same coordinates. Probably because new parcel was added but old one was not removed or is still marked as current. Need to investigate and solve.

- In parcel mode, on proposals tab, when there are multiple proposals for a parcel, hovering on a proposal should draw it on the map.

- clicking "Proposal list" should enable the proposals mode and check the checkbox if it is not checked, and draw the proposals. Hovering on proposal should highlight it on the map and clicking should also center on it.

- Add a splash screen for first time visitors.

+ We often seem to be stuck on page load on "Fetching data for 6 new grid cells (5/6)..." ... such requests should time out after retries and not hang but draw the parcels that were fetched. Would that mess up future fetching?

-----------------
SHARING PROPOSALS
-----------------

- Let's work on a feature to share proposals. Some initial thoughts follow.

- First we need to solve the situation where there are multiple proposals for the same parcel or same block. We need to make it so only one proposal can be displayed on the map at one time.

- Secondly, there is the fact that proposals can depend on other proposals. For example, a new road is drawn, it forms two blocks of parcels out of one, and on one of the resulting blocks a building is proposed. So the building proposal depends on the road proposal (being accepted and executed first).

- I am thinking the best way to track this is by having each object have a parent_proposal pointer, and each proposal having a list of "parent parcels", that is, parcels affected by the proposal. Initially, all parcels and buildings should have the parent(s) empty or null, because when we load them they represent the current state of the world. But when a new proposal is added, it becomes the parent for the changes it introduced. Example: if we create a road proposal for a road that split a parcel in two, it would have one parent (the old parcel) and three children: the road parcel itself and the two new, smaller parcels that inherited the old parcel.

- Once we have these dependancy chains established in our data, we can add functionality to:
1) select from multiple proposals (to display a proposal we need to check if all its ancestors are visible on the map unaffected)
2) remove proposals from map (for this we need to know if the proposal has any descendants that would also need to be deleted)

- For removing a proposal, we need to know the state before the proposal. The proposal should thus keep a list/array of the parcels it affected ("parent parcels").
When a road is drawn, and parcels are affected, we should always be keeping the old versions of the parcels affected, and keep links to them in the proposal. This allows us to remove a proposal.

- Proposal for a block affects all parcels in a block.

- We should only be drawing proposals on the map if they are the only proposal for the parcels they affect? In other cases

- Proposals are naturally in conflict with proposals for the same underlying parcels.

- Let's use the word "Applied" for the proposal that is currently displayed on the map.

- In Proposal Mode (checkbox checked) all the proposals should be drawn on the map 

- Do we need to store descendants of proposals in the proposal data structure? For easier lookup when removing/deleting? Or should we, when deleting a proposal, parse all proposals to see if any have it in the parent list? Probably the former is better. So every time a proposal is created that depends on another proposal we need to modify the original proposal to to add a reference.

- How will agents' proposals (in Game mode) be drawn on the map?

- Proposals need a [display/activate on map], max one proposal per parcel or one per block

- We need to make "road drawing" become "road proposals"

- Even drawing a road by clicking the Is road [] checkbox is a proposal.

- Every object or parcel on map should have a parent proposal, or null

- sharing: list of proposals to be loaded and shown

- Propoals need to have timestamp of when they were created, so that when we apply them it can be done in the correct order (why? don't ancestor lists solve this?)

- Work through example of 

- Two ways to share proposals need to be investigated:
1) POST /proposal on server that creates and stores it in our system
2) Sharing proposals via URLs. URL would be long and contain all the information needed. It would be a list of proposal and for each proposal all the information needed to display it: ancestor/affected parcels (either IDs if they have them, meaning they are orignal parcels, or geometries if they are not original -- wait, doesn't this mean they are result of another proposal? In which case we actually need to add that proposal to the list of ancestors?), resulting parcels, resulting buildings.

- "Share proposals" button should sit in the Proposals section of the sidebar. It should share the list of currently displayed proposals, plus the current camera position.

- When loading a "shared proposals" link the user should be warned that he will lose all his current work and all parcels will be reloaded... hmm, or should we do that? Can we just treat the proposals as additional proposals and add them? We should be able to, because they will come with the full dependency tree back to originally loaded parcels. Yes, we should 1) add the proposals to the user's memory/storage, 2) draw the proposals and switch off (but not delete) proposals that are in conflict

- Can we also add a preview for sharing proposals? OG tags for socials? A screenshot?

- Add button "Share applied proposals" to the proposals section in the sidebar. Make it a flashy button with linear gradient background. When this button is clicked we create an URL that can be shared and which encodes all the information required to recreate the proposals in another browser. This will be 1) author name, 2) the full list of proposals (applied ones only!) with ancestors and descendants (in case of ancestors cestica_ids only, in case of descendants their full data including geometries), 3) the bbox encompassing the proposals being shared, 4) the current camera position. Keep in mind that the maximum URL length is 32000 characters. If encoded proposal is more present a modal saying "Proposal is too large to share via URL -- working on server side sharing, in the meantime, share screenshots or fewer proposals!"

- Build a system for accepting the said URL's. It needs to decode the shared link, wait for the parcels to load for the bbox and apply the proposals. If there are conflicts (locally missing ancestors), display a message offering two options "Cancel load" or "Lose local work, refresh parcel state from source, apply shared proposals". Applying the proposals should go from the oldest chronologically towards the youngest, so that ancestors for the younger proposals are properly created if needed. Upon successfully appling the shared set of proposals show a small modal saying "Success! You have successfully applied proposal set from ,author>"

- when drawing road we need a picker modal for choosing road width. 

- rename share proposal to share plan

- When creating a proposal, we should apply it and then pop up the proposal details panel

- reloading the browser correctly redraws the map, merging fetched parcel data with our localstorage data, resulting in our roads and related parcel changes being preserved between reloads. This is not the case for buildings created via building proposals, but it should be. After the map is fetched and redrawn they should be redrawn on top of it too. The "show proposed buildings should be checked on reload"

- when creating road instead of name give us full proposal modal

- we have a greyed out "share proposal" button we need to remove

- disable the draw with AI button


Rules for splitting parcels

Whenever we split a parcel:
- if parcel number is an integer (2036), we need to parse all parcels to find the last subnumber (2036/3, for example). If there are no subnumbers, the next number to assign is 2036/1, and if there is more than one part then in +1 increments they are 2036/2, 2036/3, etc.
If there are subnumbers, then the new part, if one, is the last subnumber +1, and if multiple, then +1 for each following.

We shoud not modify parcels we load from server by keeping the cestica_id and changing geometry or similar.

1. Modifying a parcel (by changing geometry or splitting into multiple parcels) should always create a parcel(s) with a new parcel_numbers and cestica_ids
2. Each of the new parcels needs to keep a reference to the parcel that is its parent parcel (can have a maximum of 1 parent parcel)
3. The parcel number is formed by finding the root number (before slash), then finding the last used subnumber and increasing it by one (if no subnumbers are found then last is 0)
4. The cestica_id can be formed by using the root number parcel's cestica_id and adding subnumber after underscore. So if 2036 had id 21462649 the children would be 21462649_1, 21462649_2, etc.
5. The same applies for the newly created road parcel
6. If a parcel is not just having modified geometry, but is split into multiple parts, the parts shuld all be new parcels and follow the rules above
7. The parent parcel should be removed from the map so that it is not drawn when the map is redrawn, but needs to be preserved in memory or storage so that we can bring it back/restore it when/if we roll back proposals
8. If the parcel 2036/2, that was created when 2036 was split into two, is split into two again, the two new parcels should get numbers 2036/3 and 2036/4, but their parent needs to be 2036/2 (not 2036), even though we will remove it from the map. (NOTE: is there a good reason for this? Why not just have 2036 as parent?)

9. When loading parcels from server/source and preparing to draw them, we need to skip drawing and remove from the set those that have child parcels in local storage. The way to figure out those is by maintaining an array of cestica_id in local storage where we add a cestica_id whenever it is modified locally. The local new parcels need to be merged into the set.

When creating/finishing a road, we need to make sure we are creating a proper proposal, like when we create buildings. Instead of just the popup for name, we should get the full proposal modal.

------------------
Building proposals
------------------

**** General Rule: there can always only be one proposal for the same set of ancestor parcels. This applies to both roads and buildings ****

0. We preserve road parcels/proposals between reloads, we need to make sure we do the same with buildings from building proposals.

1. For buildings, a proposal can't be applied/displayed on the map unless all its ancestors are present. This is important because a proposal can be made for a parcel block, then the parcel block can be split by another road proposal, which will remove the previous parcels and make reapplying the previous block proposal impossible.

2. Creating a building/building block proposal should have the same capabilities we've just built for roads. Ancestors and descendants need to be tracked, proposals can be applied and unapplied. Multiple proposals are possible for same ancestors (same block of parcels), but only one can be applied on the map at one time.

3. Applying a building proposal does not create new parcels. Because of the rule 1, applying a building proposal to a block of parcels must find and unapply the previous proposal.